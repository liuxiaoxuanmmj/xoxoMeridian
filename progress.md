# 进度摘要

## Current State（当前状态）

- Last Updated：2026-09-20（实现轮）。**feat-076**「修复 /home 毡板背景未覆盖整个文档表面」已实现并置为 `done`（P2，无依赖，来源：用户本轮指定）：root layout 在 `{children}` 之后渲染的 Agent 入口留白是**页面元素之外**的文档内容，而毡板纹理原本画在页面内元素上，末尾因此恒有一条无纹理条带；修复把纹理移到**文档表面**——[`app/globals.css`](app/globals.css) 的 `.home-linen-page { … }` 改为 `body:has(.home-linen-page) { … }`，三层背景与底色逐字未改，`app/home/page.tsx` 未改。详见 [feat-076](#feat-076)。
- 本轮实际运行的命令：先红后绿同一条命令 `E2E_APP_MODE=production npx playwright test tests/e2e/authenticated.spec.ts --project=authenticated -g "felt surface|Home document"`——`git show HEAD:app/globals.css` 退回旧实现后 **`1 failed | 2 passed (43.5s)`、EXIT=1**（失败点 `tests/e2e/authenticated.spec.ts:454`，`Matcher error: received value must not be null nor undefined / Received has value: null`），恢复修复版本（sha256 `55bf333d…`）后 **`3 passed (40.7s)`、EXIT=0**。风险匹配门禁 `npm run check:full` **单次 exit 0**（87 文件 **771 项** Vitest、两阶段计数一致、生产构建 `✓ Compiled successfully in 4.6s`、覆盖率 **53.86/47.46/58.91/54.56**、真实 PostgreSQL **35 文件/140 项** 124.23s、生产 Playwright **46 passed（3.4 分钟，0 skipped、0 flaky）**）；`npm run typecheck`、`npm run lint` exit 0。未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。**新增的「scopes the Home felt surface to the Home document」是守卫而非红灯证据**——旧实现下它同样通过。
- 本轮顺带发现并**单独登记、按要求未实现**：**feat-077**「修复 /home 顶部导航未吸顶」（`not-started`）——`.home-linen-page` 的 `overflow: hidden` 使导航的最近滚动容器变为该非滚动容器，滚动 1200px 后 nav top 为 -1200；对照实验只把该属性改为 `visible` 即回到 0。按 Stay in Scope 不并入本次背景修复。
- **feat-075**（上一轮成果保持）「修复 QAM-05-004 Agent 时间线投影的持久化与失败恢复」已实现并置为 `done`（P2，无依赖，来源 QAM-05-004，由用户确认选择本项）：[`lib/agent-posts.ts`](lib/agent-posts.ts) 新增 `projectAgentTaskTimeline`（读持久 task/agent/toolCalls → 未完成或已决策则跳过 → 无 ToolCall 记终态不建 Post → 否则带 `agentTaskId` 写入，撞 `agentTaskId` 唯一键视为已投影，其他错误原样上抛**且不推进终态** → 以 `updateMany({where:{id,timelineProjectedAt:null}})` 做 CAS 推进终态）与 `recoverPendingTimelineProjections`（按 `completedAt` 升序、每轮上限 5、单条失败不影响整轮）；[`agent/execution-tracer.ts`](agent/execution-tracer.ts) 的 `completeWithMessage` 事务外改为调用同一函数并保留失败隔离；[`agent/agent-worker.ts`](agent/agent-worker.ts) 在 dispatch 循环中补做投影，与任务分发互相隔离；新增迁移 [`20260920120000_agent_timeline_projection`](prisma/migrations/20260920120000_agent_timeline_projection/migration.sql)（`AgentTask.timelineProjectedAt`、`Post.agentTaskId` 唯一键 + FK `SetNull`、`(status, timelineProjectedAt)` 索引，并把历史已完成任务回填为 `COALESCE(completedAt, updatedAt)`）。详见 [feat-075](#feat-075)。
- feat-075 上一轮实际运行的命令：负向对照 A（只把 `agent/execution-tracer.ts` 退回旧内联投影）真实 PostgreSQL `npx vitest run --config vitest.integration.config.ts tests/integration/agent-timeline-projection.integration.test.ts` **1 failed | 9 passed (10)**，失败点为完成后 `timelineProjectedAt` 仍为 `null`；负向对照 B（只把 `agent/agent-worker.ts` 退回、去掉恢复调用）`tests/integration/agent-timeline-worker-recovery.integration.test.ts` **1 failed (1)**、50710ms，Worker 已启动（`[worker] dispatch loop started, base poll 1000ms`）但预算内从不补做；两处按 sha256 与 `/tmp/feat-075-fixed/` 副本比对恢复一致。定向 Node `tests/agent/agent-posts.test.ts` 15/15；三个新增集成文件 3 文件/12 项；`npm run typecheck`、`npm run lint` exit 0；风险匹配门禁 `./scripts/run-node22.sh npm run check:full` **单次 exit 0**（87 文件 **771 项** Vitest——快速门禁与覆盖率阶段计数一致；生产构建 `✓ Compiled successfully in 4.9s`；覆盖率 **53.86/47.46/58.91/54.56**，门槛 40/35/45/40；真实 PostgreSQL **35 文件/140 项**，139.93s；生产 Playwright **44 passed（4.2 分钟，无 skip/retry）**）。未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。
- feat-074 的上一轮成果保持：[`agent/scheduler-tick.ts`](agent/scheduler-tick.ts) 的 timer 回调由 `inFlightTasks` 持有并经 `trackTimerTask` 收敛，[`agent/worker-lifecycle.ts`](agent/worker-lifecycle.ts) 提供 `createStopController`/`interruptibleSleep`，[`agent/agent-worker.ts`](agent/agent-worker.ts) 在 loop 结束后 `drainSchedulerTasks(SCHEDULER_DRAIN_TIMEOUT_MS=5000)` 有界排空；该轮 `check:full` 单次 exit 0（87 文件/760 项、32 文件/128 项真实 PostgreSQL、生产 Playwright 44/44）。详见 [feat-074](#feat-074)。
- feat-073 的上一轮成果保持：上传可信来源已由 `File.type`（请求元数据）改为**文件内容**（magic bytes + EOI/IEND/RIFF 长度自洽），落库扩展名与回读 content type 取判定结果而非声明值；`x/y/width/height/caption` 收敛到 [`lib/validation.ts`](lib/validation.ts) 的共享 Zod form-data 边界且前置于 `arrayBuffer()`/`storage.save`，400 路径不落 blob 不建记录。详见 [feat-073](#feat-073)。
- feat-073 上一轮实际运行的命令：负向对照（三个源文件退回 HEAD 版本）`npx vitest run tests/server/home-board-routes.test.ts tests/server/atlas-storage-routes.test.ts tests/lib/atlas-storage.test.ts` `18 failed | 29 passed (47)`；恢复修复版本后同命令 `3 passed (3)` / `48 passed (48)` exit 0；风险匹配门禁 `./scripts/run-node22.sh npm run check:full` **单次 exit 0**（85 文件 Vitest——快速门禁 **750 项**、覆盖率阶段 **751 项**；生产构建；覆盖率 **53.06/46.97/57.64/53.78**，门槛 40/35/45/40；真实 PostgreSQL **32 文件/126 项**，194.00s；生产 Playwright **44 passed（7.4 分钟，无 skip/flaky）**）；收尾 `./scripts/run-node22.sh ./init.sh` exit 0（85 文件/751 项）。未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。
- feat-072 的上一轮成果保持：PostEditor 的「显示错误横幅且不跳转」与「成功才 `router.push`」两条浏览器链路已由 [`tests/e2e/authenticated.spec.ts`](tests/e2e/authenticated.spec.ts) 的一条 production Playwright 用例覆盖（**只新增验证、未改产品代码**），详见 [feat-072](#feat-072)。
- feat-071 的上一轮成果保持：feat-071 已完成并置为 `done`：Post 的发布与更新改用同一运行时输入契约（`lib/validation.ts` 的 `postCreateSchema`/`postUpdateSchema`/`postSlugSchema`），四个写入点在生成 slug、调用 Prisma 之前解析同一 schema，非字符串、超长、纯空白与空 patch 输入变成稳定的验证错误，合法中文/Markdown 的 trim、slug 规则与「未提供标题即不重新分配 slug」保持原样。此前非字符串字段会冒泡为 `500 Server error: title?.trim is not a function`（编辑器逐字显示内部 message），只填空格的更新会 `update({ data: {} })` 静默 no-op 并让编辑器跳转。详见 [feat-071](#feat-071)。
- feat-070 的上一轮成果保持：Post slug 的唯一性由数据库唯一约束裁定并在冲突时有界顺延重试，四个写入点共用同一实现；`check:full` 当时单次 exit 0（84 文件/711 项、31 文件/118 项真实 PostgreSQL、生产 Playwright 43/43）。详见 [feat-070](#feat-070)。
- 当前 feature 数量只维护在根列表的 `featuresNumber` 中；归档与交接约定沿用 [Harness 维护说明](docs/harness/README.md)，本轮未达归档阈值。
- feat-063 至 feat-076 均为 done；根列表 **24 项**中 `done` 23 项、**`not-started` 1 项（feat-077）**，没有 `blocked` 或 `in-progress` 项。下一轮可直接取 feat-077（本轮实测已有直接行为证据），或按 `priorityPolicy` 从 `docs/optimization/module-quality-overview.md` 的开放 QAM P2 选定方向并与用户确认；新登记按全局最大编号 +1（当前最大 ID 为 feat-077）。
- feat-073 的对照证据（先红后绿，未放宽断言、未加 skip、未改断言超时）：同三个测试文件，把 [`lib/storage/atlas-storage.ts`](lib/storage/atlas-storage.ts)、[`app/api/atlas/uploads/route.ts`](app/api/atlas/uploads/route.ts)、[`app/api/home-board/uploads/route.ts`](app/api/home-board/uploads/route.ts) 用 `git show HEAD:<path>` 退回旧实现后 `18 failed | 29 passed (47)`，失败点覆盖「伪造 MIME 被接受并调用 save」「`File` 型 caption 触发 `.trim is not a function` 得到 500」「`Infinity`/`NaN`/`1e999` 坐标与 201 字符 caption 落库」；恢复修复版本（`/tmp/feat-073-fixed/` 副本按 sha256 比对一致）后全绿，重写为 sharp 真实可解码字节后全部通过。
- feat-072 的对照证据：同一用例同一文件，旧写入契约（`updatePost` 按 truthy 组装 patch）下 `1 failed | 1 passed (53.2s)`，失败点即 `expect(getByText('Content is required')).toBeVisible()` → `element(s) not found`，失败快照已跳到文章页且无横幅；恢复修复版本后 `2 passed (48.3s)`。未放宽断言、未加 skip、未改断言超时。
- feat-071 的对照证据：Node 行为回归旧实现 `15 failed | 5 passed (20)`（非字符串/数组/畸形 JSON 得到 `500`、超长与空 patch 得到 `200`、Action 返回逐字 `Server error: title?.trim is not a function`）→ 修复后 20/20；真实 PostgreSQL 旧实现 `6 failed | 2 passed (8)` → 修复后 8/8（含只填空格的更新整行含 `updatedAt` 不变、20000 边界接受、`slug === generateSlug("中文标题")`）；未补组件/E2E 的理由见 [feat-071](#feat-071)。
- feat-070 的对照证据：真实 PostgreSQL 并发回归（`BEFORE INSERT/UPDATE` 触发器延迟 0.5s 让两个请求都越过检查阶段）旧实现 `4 failed | 1 passed`（`[200,500]` + `Unique constraint failed on the fields: ('slug')`）→ 修复后 5/5；定向 Node 3 文件/76 项通过，`P2002` 形状按真实探针固定；详见 [feat-070](#feat-070)。
- feat-069 的对照证据：真实 PostgreSQL 交错回归旧实现 `2 failed | 1 passed`（抛出 `P2003` / `AtlasElement_postId_fkey (index)`）→ 修复后 3/3；Node 定向回归旧实现 `2 failed | 4 passed` → 修复后 6/6，两类可容忍冲突（Post 已不存在、anchor 已存在）与两类必须上抛的错误分别覆盖；详见 [feat-069](#feat-069)。
- feat-066 的对照证据：默认并发（31 workers）5 次中 1 次失败、受控 16/8/4 workers 全绿、受控 2GB 压力下未修改的默认并发 3/3 失败而配置上限 8 后 3/3 通过（84 文件/699 项）；feat-065 未复现默认并发失败与 feat-068 的容量边界结论均不因本轮改变，如实保留在各自记录。
- 落地收尾：按用户要求先把待应用迁移落到本地开发库（`prisma migrate deploy`：28 个全部成功、0 失败；`prisma migrate diff --from-url $DATABASE_URL --to-schema-datamodel prisma/schema.prisma --script` 输出 `-- This is an empty migration.`，即开发库与 `prisma/schema.prisma` 一致），再把工作区改动提交为**单个 commit**（`git log -1`，提交信息以 `fix: 修复 Agent 时间线投影的持久化与失败恢复（QAM-05-004）` 开头；70 files changed, 6051 insertions(+), 1326 deletions(-)，覆盖 feat-069 至 feat-075 与 About 页拆分、世界时钟抽取）。提交后工作区干净，**未推送**。注意：本文件下方各轮记录里「本轮未提交、未推送」是**当时**的事实陈述，已被本次提交追平，不再代表当前状态。
- 常规启动读取本节、任务索引及所选任务记录；[session-handoff.md](session-handoff.md) 仅供相邻会话恢复。会话开始前的既有范围外未提交改动均保留。


## 2026-09-13 — Harness 归档、计数与交接整理（未登记 feature）

- 范围：使用 harness-creator 与 skill-creator，按用户要求维护状态文档、归档说明和技能的交接规则；不登记 feature，不修改应用代码、依赖、构建或运行配置。
- 计数：新增 `featuresNumber`，始终等于根 `features.length`，包含全部状态、不含归档；数组新增或移入/移出时同步维护，只改状态时不变。进度不另记 feature 总数或 done 汇总；不以计数生成新 ID。
- 归档：取消字节数限制和逐任务备份。先看 `featuresNumber`，未超过阈值直接跳过；超过后才检查是否满足 done 超过 40 的既有条件。触发时一次归档全部已完成条目与完整进度，未完成任务及必要证据留在根文件，并同步更新计数字段。
- 交接：每轮任务结束重写 session-handoff，只服务相邻两次会话；不追加、不归档、不保留旧版，也不将完整旧交接搬到进度中继续追踪。已移除旧交接副本及其目录/哈希引用；长期事实和验证证据继续由任务进度承载。
- 历史整理：此前提前归档的任务及进度已经回到根文件，零散归档、旧路径与失效预算说明已清理；现只保留 feature 与进度的首批历史。任务状态、依赖、排序及应用验收结果保持，feat-062 同步移除长期保存交接的旧约束。
- 技能：记忆说明原先把交接归入持久记忆并要求建立索引；已直接修改 `/home/dadalv/.agents/skills/harness-creator/` 下的 `SKILL.md`、`references/memory-persistence-pattern.md`、`templates/session-handoff.md` 和 `templates/agents.md`，明确每轮重写且不归档，长期记录写入进度或项目文档。技能元数据保持，正文不引入本项目的计数字段或归档阈值；工作区外的写入已通过权限确认执行成功。
- 文档核对：`python3 -` exit 0，计数字段与根数组一致；任务状态、依赖、排序及未涉及业务文件保持，feat-062 仅调整交接约束和维护说明。52 个本地链接及任务锚点可解析，旧交接归档没有残留引用；技能改动与准备稿一致，元数据未变。`git diff --check` exit 0，`git status --short` 已核对。迁移临时文件已清理，不作为恢复依赖。
- 未运行：`./init.sh`、`npm run check`、`npm run check:full`、`npm run test:compose-smoke`、应用测试/构建及 Harness/Skill 验证脚本；沿用用户明确的纯文档维护约定。下方应用结果均为此前任务的历史证据，不代表本轮运行。
- 唯一下一步：开始 feat-060，核对首批归档中的 feat-035 及根列表验收后，再运行代码会话的启动门禁。

## 当前批次任务索引

| 任务 | 状态 | 完整进度 |
| --- | --- | --- |
| feat-076 | done | [/home 毡板背景覆盖整个文档表面](#feat-076) |
| feat-077 | not-started | [/home 顶部导航未吸顶（本轮发现，未实现）](#feat-077) |
| feat-075 | done | [Agent 时间线投影的持久化与失败恢复](#feat-075) |
| feat-074 | done | [Worker 定时器任务所有权与 shutdown 生命周期](#feat-074) |
| feat-073 | done | [上传内容信任与 multipart 字段 contract](#feat-073) |
| feat-072 | done | [编辑器错误横幅与不跳转的浏览器旅程](#feat-072) |
| feat-071 | done | [Post 写入边界的统一运行时输入契约](#feat-071) |
| feat-070 | done | [Post slug 并发唯一性分配](#feat-070) |
| feat-069 | done | [首页 anchor 补齐与 Post 删除交错](#feat-069) |
| feat-063 | done | [Study 开发序列同步与隔离](#feat-063) |
| feat-067 | done | [搜索开发序列超时](#feat-067) |
| feat-064 | done | [About 照片缺失回退与交付契约](#feat-064) |
| feat-068 | done | [高开销开发浏览器诊断与资源压力复核](#feat-068) |
| feat-061 | done | [计划澄清与执行事实一致性](#feat-061) |
| feat-060 | done | [Study 本地日历与午夜边界](#feat-060) |
| feat-065 | done | [首页搜索组件门禁的 IntersectionObserver 生命周期复核](#feat-065) |
| feat-066 | done | [默认并发上传门禁的容量边界与 worker 预算](#feat-066) |
| feat-059 | done | [Post 同时间分页与稳定排序](#feat-059) |
| feat-058 | done | [Agent 入口身份重验](#feat-058) |
| feat-057 | done | [搜索错误反馈与文章展示投影](#feat-057) |
| feat-056 | done | [聊天 SSE 慢查询串行与关闭生命周期](#feat-056) |
| feat-055 | done | [生活字段共享约束与旧记录兼容](#feat-055) |
| feat-054 | done | [计划控件名称与键盘操作](#feat-054) |
| feat-062 | done | [首次状态与历史分离](#feat-062) |

以下保留各任务实际的改动、失败与验收证据；已清除被撤销的零散归档记录和旧预算说明。当时的推荐下一步只描述历史情况，当前唯一下一步以上方总览及交接为准；重复摘要由本索引替代。

<a id="feat-076"></a>

## 2026-09-20 — feat-076：/home 毡板背景覆盖整个文档表面

- 来源与授权：用户本轮指定——/home 的网格状毡板没有填满背景，滚轮下滑时始终有一段非网格背景；同一页面的时间线会随文章无限延伸（这一点符合设计预期）。要求先读 AGENTS.md 与 feature_list.json、先登记 feature、再从第一性原理修复。登记为 feat-076 并只把它标为 `in-progress`，`featuresNumber` 22→24（同时登记发现的 feat-077）。
- 根因（隔离实测，非推断）：毡板纹理画在**页面内的元素**上，而文档末尾有一段**由 root layout 拥有、位于该元素之外**的内容——[`app/layout.tsx`](app/layout.tsx) 在 `{children}` 之后渲染 `<AgentEntryGate>`，它在模型就绪后插入 `div.clearance` 留白（[`agent-entry.registry.ts`](components/agent-entry/agent-entry.registry.ts) 的 desktopSize 208 + desktopBottom 24 = 232px）。页面元素不可能覆盖它，`document.documentElement.scrollHeight` 因此恒等于「页面元素高度 + 232」。隔离环境 1280×800 实测：scrollHeight 6227、`.home-linen-page` 5995、`.clearance` 位于 5995..6227；滚到文档末尾后取最后一个像素行，`elementFromPoint` 命中 BODY，其 `background-image` 为 `none`、`background-color` 为 `rgb(243,247,240)`——末尾 232px 是纯色，与截图一致。
- 为什么用户提到的「New Post 网格随笔记延伸」不是同一个问题：内容随文档增长是对的，错的是背景停在页面元素边界；两者的差别正是「谁拥有文档末尾的那段空间」。
- 实现（纯 CSS）：把毡板从元素移到**文档表面**——[`app/globals.css`](app/globals.css) 的 `.home-linen-page { … }` 改为 `body:has(.home-linen-page) { … }`，三层背景（5px/6px 细网格 + 135° 斜向渐变）与底色 `#f3f7f0` 逐字未改；[`app/home/page.tsx`](app/home/page.tsx) 未改，类名现在只作 `:has()` 的作用域钩子（CSS 注释已写明用意，避免后轮把它当无用类删掉）。选型理由：末尾那 232px 由 root layout 拥有、位于页面元素之外，页面内任何元素都覆盖不到；`body` 是唯一保证横跨整个可滚动区域的表面，而 `:has()` 让作用域仍由「本页是否渲染 /home」决定。
- 相位不变与降级：`.home-linen-page` 是 `<body>` 的第一个子元素、无 margin（SiteNav 在它内部，57px 高度不参与偏移），body 的绘制原点与它同在文档 (0,0)，所以网格相位与旧实现一致；不支持 `:has()` 时整条规则失效，退回 body 的 `#f3f7f0` 纯色，布局不受影响。
- 先红后绿（同一条命令、未放宽断言、未加 skip、未改断言超时）：`E2E_APP_MODE=production npx playwright test tests/e2e/authenticated.spec.ts --project=authenticated -g "felt surface|Home document"`。用 `git show HEAD:app/globals.css` 退回旧实现后 **`1 failed | 2 passed (43.5s)`、EXIT=1**，失败点 `tests/e2e/authenticated.spec.ts:454` 的 `expect(coverage.backgroundImage).toContain("repeating-linear-gradient")` → `Matcher error: received value must not be null nor undefined / Received has value: null`（从最后一行像素一路上溯，链上没有任何元素带背景图）；恢复修复版本（`/tmp/feat-076-fixed-globals.css`，sha256 `55bf333d5c59405f8f11d254a5f14913b10cfdfe4d4251437d662d5c44bbbd37`）后 **`3 passed (40.7s)`、EXIT=0**。
- 被测用例：「keeps the Home felt surface covering content appended after the page element」——在浏览器里向 `document.body` 追加与 Agent 入口同形状（232px）的尾部内容（真实留白依赖 WebGL 模型加载，不适合作为确定性前置条件），滚到文档末尾后取最后一个像素行的 `elementFromPoint`，断言该表面带 `repeating-linear-gradient`、`top === 0`、`bottom >= scrollHeight`。
- 守卫而非红灯（**如实标注，后轮不要算作先红后绿的证据**）：新增的「scopes the Home felt surface to the Home document」在旧实现下**同样通过**（旧实现根本不在 body 上画纹理），它约束的是本次机制不被写成无条件的 `body { background-image: … }`。它访问 `/study`，断言 200 且 `toHaveURL(/\/study$/)`（**未认证时会重定向到登录页「空过」这条守卫**——登录页同样没有毡板类名、body 同样没有背景图，故固定 URL），并断言页面无毡板类名、body `background-image` 为 `none`、`background-color` 为 `rgb(243, 247, 240)`。
- 门禁：`npm run check:full` **单次 exit 0**——快速门禁 87 文件 **771 项**、覆盖率阶段同报 87 文件/771 项（两阶段计数一致）、生产构建 `✓ Compiled successfully in 4.6s`、覆盖率 **53.86/47.46/58.91/54.56**（statements/branches/functions/lines，门槛 40/35/45/40，与上一轮一致——纯 CSS 改动不改变覆盖率）、真实 PostgreSQL **35 文件/140 项**（124.23s）、生产 Playwright **46 passed（3.4 分钟，0 skipped、0 flaky）**（44 → 46 即本轮新增的两条用例）。`npm run typecheck`、`npm run lint` exit 0。
- 门禁覆盖最终内容：scope 用例在首次门禁之后又补了 `toHaveURL(/\/study$/)` 守卫（未认证时会重定向到登录页而**空过**这条断言——本轮实测踩到过），因此在冻结树上**再跑一次** `npm run check:full`，同样 **exit 0** 且计数逐项一致：87 文件/771 项 ×2、覆盖率 **53.86/47.46/58.91/54.56**、真实 PostgreSQL **35 文件/140 项**（124.32s）、生产 Playwright **46 passed（3.4 分钟，0 skipped、0 flaky）**，两条毡板用例（`authenticated.spec.ts:416` 3.2s、`:459` 330ms）均在列。**两次门禁的数字相同，记录里的任一数字都同时成立。**
- 边界与未运行项：未改 Post 读写契约、可见性、分页、Agent 入口留白高度与 `min-h-screen` 行为，未引入迁移，未改依赖、Next 配置或 Compose 拓扑；未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。
- 隔离与范围：全程使用一次性库 `xoxo_ux_probe` 与 3200 端口上的仓库副本探针（真实文件 + 仅软链 `node_modules`；Next 的路由发现不跟随目录符号链接，符号链接农场方案会整体 404），未改动开发库 `xoxo_meridian`、未停止用户 3000 端口的 dev server；探针进程与临时目录已清理。
- 顺带发现（**单独登记、本轮未实现**）：feat-077「修复 /home 顶部导航未吸顶」。按 Stay in Scope 不并入本次背景修复。
- 本轮未提交、未推送。

<a id="feat-077"></a>

## 2026-09-20 — feat-077：/home 顶部导航未吸顶（发现轮，未实现）

- 来源：为 feat-076 做浏览器实测时发现，按 Stay in Scope 单独登记，状态 `not-started`，本轮**未实现、未改动任何产品代码**。
- 现象与直接原因（隔离实测）：1280×800 下 `/home` 滚动到 `scrollY=1200` 时 `nav.getBoundingClientRect().top` 为 **-1200**，而 computed `position` 仍为 `sticky`；`.home-linen-page` 的 computed `overflow` 为 `hidden`。对照实验**只**把该容器的 inline `overflow` 改为 `visible`，同一滚动位置下 nav top 回到 **0**，其余条件不变——即祖先裁切使导航的最近滚动容器变成该非滚动容器，导航随页面滚走。
- 未做与待确认：尚未查明该 `overflow: hidden` 原本要裁切什么（页面内还有 `absolute`/`fixed` 的装饰光斑与照片层），因此验收标准第一条要求先说明裁切的原始意图、不得仅凭症状直接删属性，也不得改用固定定位绕过。滚动 1200px 后 Blog/Chat/Study/New Post 入口全部不可见，是主入口导航不可达。
- 本轮未提交、未推送。

<a id="feat-074"></a>

## 2026-09-20 — feat-074：Worker 定时器任务所有权与 shutdown 生命周期

- 来源与授权：根列表 20 项全部 `done`，本轮按 [`priorityPolicy`](docs/optimization/module-quality-review-standard.md)（核心操作受阻程度 → 入口影响范围 → 触发频率 → 恢复成本，同分优先已有直接行为证据者）对 24 项开放 QAM P2 排序并给出建议，**用户确认选择 QAM-04-003**（`agent/scheduler-tick.ts` 的 detached timer promise 与 Worker shutdown 生命周期）。登记为 feat-074，只把它标为 `in-progress`，未合并其它 QAM P2。
- 缺口（第一性原理，比报告更宽）：报告只写了「claim 失败且随后的 `recordJobDispatchFailure()` 也拒绝」这一条路径。实际 `fireJobNow()` 顶部的 `await prisma.scheduledJob.findUnique(...)` **完全没有保护**，因此任意一次瞬时数据库错误就会命中。timer 回调以 `void fireJobNow(...)` 调用——`void` 只丢弃返回值，**不收敛 rejection**；仓库内不存在任何 `process.on("unhandledRejection"|"uncaughtException")` 处理器，按 Node 默认 `--unhandled-rejections=throw` 语义直接终止 Worker 进程，使全部房间的 AgentTask 与 ScheduledJob 停止被消费。另一半是 shutdown：SIGINT/SIGTERM 只置模块级 `stopped=true` 并 `clearAllTimers()`，而两个 loop 在做完当前操作后**无条件 sleep**，`main()` 又 `Promise.all` 等待两者，dispatch backoff 最长 60 秒；已停止的进程被 sleep 继续钉住。Compose 未声明 `stop_grace_period`（默认 10 秒后 SIGKILL）。
- 先红证据（两条，均为真实行为而非推断，修复前采集）：(1) 用锁定 Node 22.23.2 运行与 `scheduler-tick.ts` 同形的探针（`setTimeout(() => { void fireJobNow(); }, 10)`，其中抛出模拟的瞬时数据库错误）→ 进程 **exit 1** 终止，200ms 后的存活日志从未打印。(2) 用 `node_modules/tsx/dist/cli.mjs agent/agent-worker.ts` 启动真实 worker（`AGENT_WORKER_POLL_MS=30000`，两个 loop 均已启动）后发 SIGTERM → **15 秒后仍在运行**，只能 SIGKILL。
- 实现：[`agent/scheduler-tick.ts`](agent/scheduler-tick.ts) 新增模块持有的 `inFlightTasks` 集合与 `trackTimerTask()`——任务在最外层 `catch` 收敛并记录 `[scheduler] timer task failed:`，`.finally` 自行出集合；timer 回调由 `void fireJobNow(...)` 改为 `trackTimerTask(fireJobNow(...))`。新增 [`agent/worker-lifecycle.ts`](agent/worker-lifecycle.ts)：`createStopController()` 提供可等待的停止信号，`interruptibleSleep()` 在停止时**清除挂起的定时器**并立即 resolve（否则已停止的进程仍被 sleep 钉住）。[`agent/agent-worker.ts`](agent/agent-worker.ts) 以停止信号取代模块级 `stopped`，`sleep` 改为可唤醒，`main()` 在两个 loop 结束后调用 `drainSchedulerTasks(SCHEDULER_DRAIN_TIMEOUT_MS)`——在 5 秒内 `Promise.allSettled` 等待已开始的任务，超时则记录仍 in-flight 的数量并有界退出。5 秒明显小于容器默认 10 秒宽限期。**未**引入新的进程托管系统：`clearAllTimers()`、两个 loop 的结构与 Compose 拓扑均未改动。
- 先红后绿（未放宽断言、未加 skip、未改断言超时）：**对照 A** 把 timer 回调还原为 `void fireJobNow(...)` 后 `npx vitest run tests/agent/scheduler-tick.test.ts` 为 **`4 failed | 18 passed (22)`**，其中两条失败点正是 `expect(unhandled).toEqual([])`（分别复现「读取 job 失败」与「claim 失败后失败回写再次拒绝」的未处理拒绝），另两条是 drain 观察不到已开始的任务；恢复后同文件 22/22。**对照 B** 用 `git show HEAD:agent/agent-worker.ts` 还原旧 worker 后 `tests/agent/worker-shutdown.test.ts` 失败于 **`worker 未在 8000ms 内响应 SIGTERM`（exit 1）**；恢复后同用例在约 1 秒内以 code 0 退出且输出 `[worker] stopped`。修复版本备份在 `/tmp/feat-074-fixed/` 并按 sha256 逐文件比对恢复一致。
- 重启恢复语义（验收标准 3，**如实标注非红灯**）：真实 PostgreSQL 新增两条 timer 路径用例——claim 在 fired Event 插入失败时整事务回滚、Job 保持启用且 `nextRunAt` 未推进、下一次 tick 重新扫描并只产出 1 个 Task/Event；已提交的 timer 派生不被后续 tick 重复。两条在旧实现下**同样通过**，它们守的是「语义未变」而非复现缺陷，因此**不计入先红后绿证据**。为消除时序依赖，失败触发器改为在 `schedulerTick(now)` **之前**安装（此时 `nextRunAt` 未到，扫描路径不会碰该 Job），并用 `drainSchedulerTasks(5_000)` 确认任务自身已收尾而不是超时后被放弃。
- 顺带修正：删除 `countInFlightTasks` 导出——全仓零调用，属只为测试存在的生产导出。
- 已知边界（不做过度承诺）：`drainSchedulerTasks` 超时**不会取消**任务，只是让 shutdown 有界；超时后进程退出，未提交的事务由 PostgreSQL 回滚，Job 仍是可重新扫描的。这依赖持久化 CAS 的既有恢复能力，不是本项新增的保证。`interruptibleSleep` 只被 Worker 使用，未扩展为通用调度原语。
- 验收对照：定向 Node 3 文件/27 项 exit 0；真实 PostgreSQL `scheduler-atomic-dispatch` 6/6 exit 0；`npm run typecheck` exit 0；`npm run lint` exit 0；`./scripts/run-node22.sh npm run check:full` **单次 exit 0**——87 文件 **760 项** Vitest（快速门禁与覆盖率阶段计数一致）、生产构建、覆盖率 **53.48/47.11/58.43/54.16**、真实 PostgreSQL **32 文件/128 项**、生产 Playwright **44 passed（6.7 分钟）**；日志无 `npm ERR!`/`ELIFECYCLE`/非零退出。
- 质量结论：[QAM-04 报告](docs/optimization/qam-04-scheduler-quality-review.md) 的 `健壮性、并发与生命周期` **12→13**（**仍扣 1 分**：wall-clock forward 与 DST 无定向 E3），Score **87→88**、Score Level 保持 **L3**、Gate **仍 L3**、`Final = min(L3, L3) = L3`。**开放问题清零不等于 Gate 升 L4**——L4 要求「全部风险不变量已有风险匹配验证」，该条未通过。组合均分 86.9→**87.0**，开放 QAM P2 **24→23**。报告与模块总览各追加一行只追加历史。
- 计数与归档：`featuresNumber` 保持 21（等于根 `features` 长度，只改状态不变）；归档判定 21 ≤ 40，直接跳过。
- 边界与未运行项：未改 Prisma schema、迁移、依赖、Next 配置、Compose 拓扑、`clearAllTimers()` 语义或 Scheduler 的 claim/派生契约。本轮未运行 `npm run test:compose-smoke`、Compose 构建与实体设备（未改启动路径的 Compose 拓扑，属 QAM-09）。[`next-env.d.ts`](next-env.d.ts) 被生产构建改写到 `./.next/types/*` 后已还原为 `./.next/dev/types/*`，还原后 `npm run typecheck` exit 0。
- 本轮未提交、未推送。

<a id="feat-073"></a>

## 2026-09-20 — feat-073：上传内容信任与 multipart 字段 contract

- 来源与授权：根列表 19 项全部 `done`，本轮按 [`priorityPolicy`](docs/optimization/module-quality-review-standard.md)（核心操作受阻程度 → 入口影响范围 → 触发频率 → 恢复成本，同分优先已有直接行为证据者）对 25 项开放 QAM P2 排序，**用户在候选中确认选择 QAM-06-005**「上传只信任 multipart MIME，且位置/标题字段未经过服务端 schema」。无依赖，来源报告 [`qam-06-spatial-media-quality-review.md`](docs/optimization/qam-06-spatial-media-quality-review.md)。
- 缺口（第一性原理）：仓库唯一的运行时校验 [`validateAtlasImageFile`](lib/storage/atlas-storage.ts) 读的是 `File.type`——**请求元数据，不是文件内容**。可复现实验（`node --import tsx`）证明 `File(["not-an-image"], "payload.jpg", { type: "image/jpeg" })` 得到 `accepted: true`：伪造或损坏的字节可以进入图片存储并被当作图片回读。同一类缺陷在两条 Route 上还有三个入口：`x/y` 直接 `Number(...)`（`Number("Infinity")` 可把非有限数写进 `double precision` 列，已在运行中的 PostgreSQL 容器内以 TEMP 表 + `BEGIN … ROLLBACK` 复现）、`caption` 直接类型断言为 `string`（`File` 型 caption 触发 `.trim is not a function`，得到依赖运行环境的 500 而不是稳定的 400）、caption 没有任何长度上界。更关键的是**字段解析发生在 `storage.save` 之后**：解析失败时 blob 已经落盘，与 QAM-06-004 的孤儿资源问题叠加。这属于硬约束 6「不可信输入必须先经 `lib/validation.ts` 或同域 Zod schema」的直接违反。
- 实现（存储侧）：[`lib/storage/atlas-storage.ts`](lib/storage/atlas-storage.ts) 新增 `detectAtlasImageFormat`/`validateAtlasImageContent`，只按字节判定允许格式——JPEG 要求 `FF D8 FF` 开头且存在 `FF D9`；PNG 要求 8 字节签名且存在 `IEND`；WebP 要求 `RIFF`/`WEBP` 标识、`VP8 `/`VP8L`/`VP8X` chunk 且 `readUInt32LE(4) + 8 <= length`。判定结果直接作为 `storage.save({ mimeType })` 的输入，因此**落库扩展名与回读 content type 与真实内容一致**，不再出现「声明 JPEG 的 PNG 存成 `.jpg` 却按 `image/png` 读」这类错配。拒绝路径抛具名的 `AtlasImageValidationError`，两条 Route 用 `instanceof` 映射 400；旧实现按错误文案匹配，措辞一改就 silently 退回 500。
- 实现（表单侧）：[`lib/validation.ts`](lib/validation.ts) 新增 `homeUploadFieldsSchema`/`atlasUploadFieldsSchema` 与 `readFormFields`。`x/y` 缺省为 `0`、`width/height` 缺省 240/180 且必须为正；数值字段走预处理后交给 `z.coerce.number()`，**非 `string`/`number` 的输入（含 `FormData` 里的 `File`、数组）统一返回 `NaN` 再被 Zod 拒绝**——这里刻意不用裸 `z.coerce.number()`，因为实测它对 `true` 会得到 `1`，等于把布尔表单值静默当成坐标。caption 先 trim 再限长 200，空串归一化为 `null`。
- 顺序保证：两条 Route 现在是「声明值快筛（含 5MB 上界）→ 字段解析 → `arrayBuffer()` → 内容校验 → `storage.save` → Prisma create」。**字段解析前置于读取字节与写存储**，所以任何 400 路径都不落 blob、不建记录，也不会把注定失败的 5MB 读进内存。
- 有意的语义收敛：Atlas 此前把空白 caption 存成 `"   "`，Home 存 `null`；现在两者统一为 trim + `null`。这不是顺带的静默行为变更，已写进验收标准、报告与本节。
- 已知边界（不做过度承诺）：校验强度是 **magic bytes + 结构完整性**，不是完整解码——带头、带终止标记但没有图像数据的文件仍会被接受。该边界由 `JPEG_HEADER_ONLY_WITH_EOI` 用例**显式固定**而不是留白：后来者若把它升级为「可解码性校验」，必须同时改该用例与 QAM-06 报告。残留影响只是图片渲染失败，存储侧仍是受控 key + 受控 content type，不构成跨用户读取或可执行内容。
- 先红后绿（同三个测试文件，未放宽断言、未加 skip、未改断言超时）：把 [`lib/storage/atlas-storage.ts`](lib/storage/atlas-storage.ts)、[`app/api/atlas/uploads/route.ts`](app/api/atlas/uploads/route.ts)、[`app/api/home-board/uploads/route.ts`](app/api/home-board/uploads/route.ts) 用 `git show HEAD:<path>` 退回旧实现后，`./scripts/run-node22.sh npx vitest run tests/server/home-board-routes.test.ts tests/server/atlas-storage-routes.test.ts tests/lib/atlas-storage.test.ts` 为 `18 failed | 29 passed (47)`（更早的初版红跑为 `17 failed | 30 passed (47)`，多出的一项是把具名错误类型断言补强后新增的用例）；失败点即「伪造 MIME 被接受并调用 `save`」「`File` 型 caption 触发 `.trim is not a function` 的 500」「`Infinity`/`NaN`/`1e999` 坐标与 201 字符 caption 落库」。修复文件备份在 `/tmp/feat-073-fixed/`（收尾已清理），恢复后按 sha256 比对与原文件一致。
- 验收对照：`./scripts/run-node22.sh npm run check:full` **单次 exit 0**——85 文件 Vitest（快速门禁 `npm test` 报 **750 项**、紧随其后的 `test:coverage` 报 **751 项**）、`✓ Compiled successfully` 的生产构建、覆盖率 **53.06/46.97/57.64/53.78**（statements/branches/functions/lines，门槛 40/35/45/40）、真实 PostgreSQL **32 文件/126 项**（194.00s）、生产 Playwright **44 passed（7.4 分钟，无 skip/flaky）**；收尾 `./scripts/run-node22.sh ./init.sh` **exit 0**、85 文件/751 项（含 `prisma generate`、类型检查、lint 与两主题资产预算）。日志中无 `npm ERR!`/`ELIFECYCLE`/非零退出。
- 门禁内计数差 1（如实记录，未掩盖、未择一引用）：同一次 `check:full` 中 `npm test` 报 750 项、`test:coverage` 报 751 项。此后四次复测——`test:coverage` 阶段本身、收尾 `./init.sh`、以及两次 `npx vitest run --reporter=json`——**均稳定为 751**，两份 JSON 的 85 个文件逐文件用例数完全一致，未能复现 750。仓库内没有 `skipIf`/`runIf`/`it.each`/`readdir`/环境变量分支这类会产生动态用例数的写法，`vitest.config.ts` 的 node/component 两个 project 的 `include` 均为静态模式，`tests/agent/scheduler-tick.test.ts` 的顶层 `await import` 不改变注册数量。这一项差异不影响任何结论（本项新增的 48 项在两处都被完整计入），但**下一个引用该日志的会话应以冻结树复测值为准（751），并先按 `--reporter=json` 比对逐文件清单，而不是直接引用其中一个数字**。
- 测试数据的真实性修正：首版 fixture 是手工拼的字节，`sharp` 回读直接否证——`VipsJpeg: JPEG datastream contains no image`、`webp2vips: unable to read pixels`，即**只有头、没有像素**。已改用 `sharp({ create: { width: 1, height: 1, channels: 3 } }).toFormat(...)` 生成并逐个回读验证为 1x1 3ch，同时保留每格式的截断变体（去 EOI、去 IEND、截断 RIFF）作为负向输入。这一点重要，因为若 fixture 本身不可解码，「合法图片仍能保存」的断言就只是自证。
- 质量结论：[QAM-06 报告](docs/optimization/qam-06-spatial-media-quality-review.md) 中 `接口与依赖关系` 8→9（该行原以 QAM-06-005/006 为由，005 已关闭）、`安全与隐私` 7→8（该维度唯一列名的限制因素就是 QAM-06-005）；`可测试性与验证可信度` **保持 7/8**——上传确实补齐了 E3，但限制该维度的其余具名缺口（Atlas SSE、首次建板、DB/blob 补偿）仍在，不为「本轮有测试」而凑分。Score 79→81、Score Level L2→**L3**；Gate 因风险匹配证据缺口仍为 **L2**，故 `Final = min(L3, L2) = L2`。开放 QAM P2 25→24，组合均分 86.7→86.9。[模块总览](docs/optimization/module-quality-overview.md) 同步 QAM-06 行、组合快照与只追加历史行。
- 边界与未运行项：未改 Prisma schema、迁移、依赖、Next 配置、上传成功响应形状、5MB 上界、允许格式清单、存储 key 生成规则或读取授权头；未把本次校验扩大到其它 Route。QAM-06-002/004/006/008 保持独立开放，未被本项吞并。未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。收尾清理 `coverage/`、`test-results/`、`playwright-report/` 与 `/tmp/feat-073-*`（含负向对照日志、两份 `--reporter=json` 报告与修复副本）；`check:full` 以生产构建收尾会把构建生成的 [`next-env.d.ts`](next-env.d.ts) 类型引用改写到 `./.next/types/*`，已还原为仓库既有的 `./.next/dev/types/*` 并确认还原后 `npm run typecheck` exit 0；`git diff --check` exit 0。
- 本轮未提交、未推送。

<a id="feat-075"></a>

## 2026-09-20 — feat-075：Agent 时间线投影的持久化与失败恢复

- 来源与授权：本轮用户要求「阅读 AGENTS.md、feature_list.json、progress.md、session-handoff.md，按优先级排序，从第一性原理修复下一个 P2」。根列表 21 项全部 done、没有 not-started，故按 `priorityPolicy`（核心操作受阻或结果失真程度 → 入口影响范围 → 触发频率 → 恢复成本，同分优先已有直接行为证据者）排序 [`module-quality-overview.md`](docs/optimization/module-quality-overview.md) 的 23 项开放 QAM P2，并用 AskUserQuestion 确认方向后登记本项；来源 QAM-05-004，依赖为空，`featuresNumber` 21→22。
- 排序依据（为什么选它）：结果失真最直接且用户可见——Agent 已在聊天室回复「已完成」，但 `/home` 时间线上那条 agent_log 文章永久消失，而完成事实（final Message/AgentStep/AgentTask/EventLog）早已在同一 lease 事务内提交。入口是主入口 `/home` 时间线；触发只需要 final 提交之后的任意一次瞬时数据库错误或进程终止（投影在事务外、失败只 `console.error`、节流分支直接 `return null`），且没有任何工作项、重试或重建路径，恢复成本高。它同时是 QAM-05 `可测试性与验证可信度` 维度与 Gate 的具名阻塞项（Agent projection 失败恢复缺 E3）。QAM-08-002 同属「派生写入无所有权」类别，但需要 LLM 可用 + 同房间任务重叠 + 摘要阈值同时成立；QAM-09-004 需要先有一个已证明的 hang 路径且验收依赖容器级 E3；QAM-07-004 只造成 `/study` 顺序抖动、数据仍完整。
- 缺陷链条（第一性原理）：**完成是持久事实，而它的投影不是**。[`agent/execution-tracer.ts`](agent/execution-tracer.ts) 的 `completeWithMessage` 先在 lease 事务内提交 final Message、final AgentStep、AgentTask 终态与 EventLog，然后才在事务外查询任务并调用 `createAgentLogPost`（[`lib/agent-posts.ts`](lib/agent-posts.ts)）。这一段的失败被 catch 后只写 `console.error`，节流达到 3 条/10 分钟也直接 `return null`；Prisma schema 中 AgentTask 与 Post 之间没有任何关联，也没有 pending/failed 投影状态或 Worker 补偿入口。因此「任务已完成」这个已经落库的事实**无法重建它的时间线投影**：任何一次瞬时数据库故障、slug 写冲突或提交与投影之间的进程终止，都会让该条 agent_log 永久消失，而用户在聊天室看到的是「已完成」。
- 修复边界（只加持久终态 + 幂等键 + 有界恢复，不改投影产品语义）：[`lib/agent-posts.ts`](lib/agent-posts.ts) 新增四件套——`isAgentTaskProjectionConflict`（只认 `code === "P2002"` 且 `meta.target` 含 `agentTaskId`，形状由真实探针固定）、`markTimelineProjected`（`updateMany({ where: { id, timelineProjectedAt: null } })` 的 CAS，重复调用不覆盖已有时间戳）、`projectAgentTaskTimeline(taskId)`（读持久 task/agent/toolCalls → 任务不存在或非 `completed` 或已有终态则返回 `null` → 无 ToolCall 时只记终态不建 Post → 否则带 `agentTaskId` 写入 `createAgentLogPost`，撞唯一键视为已投影，其他错误原样上抛**且不推进终态** → CAS 推进终态）与 `recoverPendingTimelineProjections(limit = TIMELINE_RECOVERY_LIMIT=5)`（`status: "completed" + timelineProjectedAt: null + toolCalls: { some: {} }`，按 `completedAt` 升序有界挑选，逐条 try/catch，单条失败不影响整轮）。`AgentPostInput` 增加 `agentTaskId` 并透传给 `prisma.post.create`。[`agent/execution-tracer.ts`](agent/execution-tracer.ts) 的完成路径改为调用 `projectAgentTaskTimeline` 并保留失败隔离（投影失败不影响已提交的完成事实）；[`agent/agent-worker.ts`](agent/agent-worker.ts) 在 dispatch 循环中、dispatch 的 try/catch **之后**调用恢复扫描，两处 try/catch 互相独立（任一侧失败不拖住另一侧，也不影响 `consecutiveErrors` 退避计数）。schema 增加 `AgentTask.timelineProjectedAt`/`timelinePost`、`Post.agentTaskId @unique` 与 `agentTask` 关系（`onDelete: SetNull`）及 `@@index([status, timelineProjectedAt])`。节流阈值、无 ToolCall 规则、metadata 字段、slug 顺延重试与可见性均未改变。
- 迁移（不回填历史关联，理由写进 SQL 注释）：[`20260920120000_agent_timeline_projection`](prisma/migrations/20260920120000_agent_timeline_projection/migration.sql) 增加两列与索引，并把 `status='completed'` 的历史任务回填为 `COALESCE("completedAt", "updatedAt")`——`completedAt` 可空，直接回填会让空值行重新暴露给恢复扫描并补发陈旧 Post。历史 `Post.agentTaskId` 不回填：历史任务已被回填为已决策，不会被重新投影。`prisma migrate diff --from-url $DEV --to-url $PROBE` 在应用迁移后输出 `-- This is an empty migration.`，证明迁移完整复现 datamodel。
- 实测确认（不靠推断）：用临时探针（`.probe-p2002.ts`，已删除）在真实 PostgreSQL 上确认 `Post.agentTaskId` 唯一冲突形状为 `{"code":"P2002","meta":{"modelName":"Post","target":["agentTaskId"]}}`，与本模块既有的 `slug` 判定同构。
- 先红后绿（两个对照，各自只退回一个文件；未放宽断言、未加 skip、未改断言超时）：**对照 A** 只把 [`agent/execution-tracer.ts`](agent/execution-tracer.ts) 退回旧内联投影 → 真实 PostgreSQL `tests/integration/agent-timeline-projection.integration.test.ts` **`1 failed | 9 passed (10)`**，失败点即完成后 `prisma.agentTask.findUniqueOrThrow` 的 `timelineProjectedAt` 仍为 `null`（旧实现不记录任何投影终态，也没有幂等键）。**对照 B** 只把 [`agent/agent-worker.ts`](agent/agent-worker.ts) 退回（去掉恢复调用）→ 进程级 `tests/integration/agent-timeline-worker-recovery.integration.test.ts` **`1 failed (1)`**、50710ms，失败信息带 Worker 输出：进程已正常启动（`[worker] dispatch loop started, base poll 1000ms`）但在 50 秒预算内从不补做投影，即旧实现下这条 agent_log 永久缺失。两处恢复后用 sha256 与 `/tmp/feat-075-fixed/` 副本逐文件比对一致。
- 守卫（**在旧实现下无法以行为方式失败**，如实标注，不计入红灯证据）：幂等重放、崩溃窗口重放（Post 已提交而 `timelineProjectedAt` 未提交）、节流终态与迁移回填四项——旧代码根本没有 `projectAgentTaskTimeline` 这个入口，任何行为断言都无从失败。`可测试性与验证可信度` 的加分依据是对照 A/B 与新增的进程级 E3，不是测试数量。
- 新增测试：[`tests/integration/agent-timeline-projection.integration.test.ts`](tests/integration/agent-timeline-projection.integration.test.ts) 10 项（经真实 `ExecutionTracer` 完成的终态与幂等键、重复投影不产生第二条且不覆盖已有决策时间、崩溃窗口重放、注入一次 `post.create` 失败后完成事实照常提交且恢复恰好补一次（第二轮扫描返回 0）、节流与无 ToolCall 的显式终态、非完成任务不进扫描集合、按 `completedAt` 升序的有界扫描、并发投影只收敛到一条）；[`tests/integration/agent-timeline-projection-migration.integration.test.ts`](tests/integration/agent-timeline-projection-migration.integration.test.ts) 1 项（独立临时库按历史迁移集部署后回放：已完成任务含 `completedAt` 为空的历史行全部被回填为非空且 COALESCE 分支等于 `updatedAt`、非完成任务保持 `null`、恢复扫描候选 SQL 返回空、迁移不新增或改动任何 Post）；[`tests/integration/agent-timeline-worker-recovery.integration.test.ts`](tests/integration/agent-timeline-worker-recovery.integration.test.ts) 1 项（真实子进程 Worker 连测试容器库，等待 `[worker] recovered 1 timeline projection(s)` 并断言库中恰好一条且指向该 task，SIGTERM 后再等 1.5s 仍为一条）；[`tests/agent/agent-posts.test.ts`](tests/agent/agent-posts.test.ts) 由 4 项扩到 15 项（跳过条件、CAS 推进、唯一键判定、失败不推进终态、单条失败隔离）。
- 一次被否证的断言（如实记录）：首版投影集成测试断言 Post 标题里的工具名顺序为 `timezone.compare, weather.get`，实际得到 `weather.get, timezone.compare`——`ToolCall` 关系的读取顺序未定义，本项不改变这一点。改为分别 `toContain` 两个工具名并写明顺序未定义，而不是去固定一个产品未承诺的顺序。
- 验收对照（先红后绿）：定向 Node `tests/agent/agent-posts.test.ts` **15/15**；三个新增集成文件 **3 文件/12 项**通过；`npm run typecheck`、`npm run lint` exit 0。风险匹配门禁 `./scripts/run-node22.sh npm run check:full` **单次 exit 0**——标准门禁 87 文件/**771 项**（快速门禁与覆盖率阶段计数一致）、生产构建（`✓ Compiled successfully in 4.9s`）、覆盖率 **53.86/47.46/58.91/54.56**（statements/branches/functions/lines，门槛 40/35/45/40；`lib/agent-posts.ts` 为 95.91%/100%）、真实 PostgreSQL **35 文件/140 项**（139.93s，较上轮 +3 文件/+12 项即本项新增的三个回归文件）、生产 Playwright **44 passed（4.2 分钟，无 skip/retry）**。日志中不存在 `npm ERR!`/`ELIFECYCLE`/非零退出。
- 质量结论：[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 把 QAM-05-004 置为 `resolved` 并同步架构与数据流图、Overall、Score Breakdown、Level Gate、Verified Strengths、Recommended Improvements 与只追加评分历史：数据流与状态一致性 11→12、健壮性并发与生命周期 12→13、可测试性与验证可信度 7→8，总分 84→**87**、Score Level L3；模块最高风险不变量（派生投影的失败恢复与幂等）自此有风险匹配 E3，Gate 的「最高风险不变量无风险匹配验证 → Gate 不高于 L2」上限解除，**Gate/Final 由 L2 升至 L3**；L4 未通过，因为 QAM-05-006（通用错误 message 泄漏）仍只有 E2。[模块总览](docs/optimization/module-quality-overview.md) 同步 QAM-05 行、组合快照（均分 87.0→87.3、Final 分布 L2×5/L3×3/L4×2、开放 QAM P2 23→22）与只追加历史行。
- 边界与未运行项：未改 Post 读写契约、可见性、分页、slug 分配算法、节流阈值（3 条/10 分钟）、无 ToolCall 规则、metadata 字段、成功响应形状或 Next 配置；未把 Trace 生成、Tool lease 或新的内容类型移入本项（QAM-08 只作为关联证据）；QAM-05-006 保持独立开放，未被本项吞并。未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。收尾清理 `coverage/`、`test-results/`、`playwright-report/`、两份临时库（`xoxo_probe`、`xoxo_dev_before`）与 `/tmp/feat-075-*`；`check:full` 以生产构建收尾会把构建生成的 [`next-env.d.ts`](next-env.d.ts) 类型引用改写到 `./.next/types/*`，已还原为仓库既有的 `./.next/dev/types/*`。
- 副作用（如实记录，已上报并由用户裁决后修复）：本轮曾误对本地开发库 `xoxo_meridian` 执行 `prisma db push`（DATABASE_URL 由 `.env` 解析而非预期的临时库），使其 schema 多出 `Memory.ownerKey`（+ `Memory_roomId_ownerKey_key_key` 唯一索引）、`AgentTask.timelineProjectedAt`（+ `AgentTask_status_timelineProjectedAt_idx`）、`Post.agentTaskId`（+ `Post_agentTaskId_key` 唯一索引 + `Post_agentTaskId_fkey` 外键），并连带删除了 `Memory_roomId_key_key` 索引（`db push` 会按 datamodel 同步，而 datamodel 用 `@@unique([roomId, ownerKey, key])` 取代了它）；数据全程未受损。两次主动回滚被权限分类器以「不可逆本地破坏」拒绝，按要求停手上报并把精确反向 DDL 与备选方案交回用户；用户在获知后明确选择回滚，随后执行：先 `pg_dump` 全库备份到 `/tmp/xoxo_meridian-before-rollback-20260920.sql`，再在同一连接上 DROP 上述外键/索引/列，并补回被 `db push` 删掉的 `Memory_roomId_key_key`。
- 开发库回滚的验证方式（不是目测，而是构造对照库）：把本次提交前的 **26 个迁移**（截至 `20260911111600_drop_focus_updatedat_default`，即该库 `_prisma_migrations` 记录的已完成集合）暂存到临时目录并在临时库 `xoxo_shadow26` 上 `migrate deploy`，再 `prisma migrate diff --from-url <shadow26> --to-url <dev> --script`——第一次输出 `DROP INDEX "Memory_roomId_key_key"`，暴露出 `db push` 还删过一个索引（我先前的反向 DDL 漏了它），补回后再次 diff 输出 `-- This is an empty migration.`，即开发库现在与其自身迁移历史完全一致。两次 `migrate deploy`/`diff` 都**显式传入 `DATABASE_URL`**；这正是本次事故的教训——不得依赖 `.env` 的隐式解析。核对结果：`_prisma_migrations` 仍为 26 行、2 users/1 room/1 post/1 AgentTask/1 Session/3 Message/0 Memory 全部在位、三个新增列与索引与外键计数均为 0、`Memory_roomId_key_key` 已补回。临时库 `xoxo_shadow26` 与暂存目录已删除，本机上只剩 `xoxo_meridian`；备份文件保留未删。
- 本轮未提交、未推送。

<a id="feat-072"></a>

## 2026-09-20 — feat-072：编辑器错误横幅与不跳转的浏览器旅程

- 来源与授权：feat-071 收尾时如实记录了「未新增组件测试与 E2E」两处缺口，用户在随后一轮明确要求「登记一条 E2E：编辑器错误横幅与不跳转」，本轮按该登记实现。依赖 feat-071 已 `done`，未改产品代码。
- 缺口（第一性原理）：feat-071 把校验收归到 Route 与 Server Action 两个边界，但**用户实际看到的症状链路没有任何测试**。[`components/blog/PostEditor.tsx`](components/blog/PostEditor.tsx) 的 `if ("error" in result) { setError(result.error); return; }` 决定「显示横幅并不跳转」，其后的 `router.push(\`/posts/${result.post.slug}\`)` 决定「成功才跳转」；组件测试必须把 `@/app/actions/posts` 整个 mock 掉、返回值由测试写死，修复前不可能失败，因此按 [`docs/testing-standards.md`](docs/testing-standards.md) 第 5 条（跨页面、重定向、浏览器渲染）只能交给 Playwright。
- 实现：在 [`tests/e2e/authenticated.spec.ts`](tests/e2e/authenticated.spec.ts) 的 authenticated project 内新增「编辑器提交纯空白正文时显示错误横幅并停在编辑页，合法保存仍跳转」。夹具沿用同文件既有写法（`page.request.post("/api/posts")` 造数据 + `finally` 内 `page.request.delete` 清理），库内断言用 `E2E_DATABASE_URL`／`test-results/.e2e-database-url` 构造的 `PrismaClient`。用例以已登录用户打开 `/posts/edit/:slug`，把正文改成**纯空白**（三个空格）后点击 `Update`，断言错误横幅 `Content is required` 可见、URL 仍为 `/posts/edit/:slug`、该行 content/slug 与 `updatedAt` 与提交前完全相等；随后在**同一用例内**把正文改成合法内容（两侧带空格，验证 trim 语义）再提交，断言跳转到 `/posts/:slug` 且库内等于 trim 后的输入——这条对照证明上一条不是「提交永远不生效」的假阳性。
- 必须提交纯空白而不是真正留空（已写入用例注释）：`#post-title` 与 `#post-content` 都带原生 `required`，**真正留空会被浏览器本身的校验拦下、Server Action 根本不会被调用**，测不到写入契约；后来者若把用例改成「清空后提交」会静默失去覆盖。
- 先红后绿（同一文件、同一用例，未放宽断言、未加 skip、未改断言超时）：把 [`app/actions/posts.ts`](app/actions/posts.ts) 的 `updatePost` 临时退回旧写入契约——用 `title?.trim() || undefined` / `content?.trim() || undefined` 按 truthy 组装 `data`（并临时收窄 `@/lib/validation` 的导入以通过类型检查）。`E2E_APP_MODE=production ./scripts/run-node22.sh npx playwright test --project=authenticated -g "编辑器提交纯空白正文"` 旧实现 **exit 1**、`1 failed | 1 passed (53.2s)`，失败点即 `await expect(page.getByText("Content is required", { exact: true })).toBeVisible()` → `Error: element(s) not found`（`tests/e2e/authenticated.spec.ts:1497`），失败快照显示页面已离开编辑页、渲染的是文章详情（`alert: 编辑器空白正文 …`、`article`、`heading "编辑器空白正文 …"`、`Back to Blog`）且没有任何横幅——正是「静默成功并跳转」。从 `/tmp/feat-072-posts-fixed.ts`（本轮修复副本，收尾已清理）恢复并比对 sha256 一致（`bcda6129…b3eb`）后，同一命令 **exit 0**、`2 passed (48.3s)`。
- 验收对照：启动基线 `./scripts/run-node22.sh ./init.sh` exit 0（85 文件/731 项）。风险匹配门禁 `./scripts/run-node22.sh npm run check:full` **单次 exit 0**——标准门禁 85 文件/**731 项**（`✓ Compiled successfully in 14.5s` 的生产构建）、覆盖率 52.53/46.05/57/53.21（statements/branches/functions/lines，门槛 40/35/45/40）、真实 PostgreSQL **32 文件/126 项**（112.51s）、生产 Playwright **44 passed（3.8 分钟，无 skip/retry）**，较上轮 43 项多出的正是本用例（`4.4s`）。日志中不存在 `npm ERR!`/`ELIFECYCLE`/非零退出。
- 质量结论：[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 把「浏览器侧仍有既有的发帖与搜索旅程，完整的编辑交互旅程仍未专项覆盖」改为已由本项覆盖的表述，并同步 `本轮 Delta`、基线/风险匹配命令、`证据纪律`、`可测试性与验证可信度` 与 QAM-05-001 的验收证据；[模块总览](docs/optimization/module-quality-overview.md) 追加只追加历史行。**本项为纯验证补充，不改变任何维度得分、Score 84、Score Level L3、Gate/Final L2 与开放 QAM P2 数量（仍 25），Delta 记 0**——按 [`module-quality-review-standard.md`](docs/optimization/module-quality-review-standard.md) 第 3 节，评分反映当前既定实现质量且不得用测试数量换算；`可测试性与验证可信度` 仍为 7/8，因为限制该维度的是「Agent projection 失败恢复缺对应 E3」，本项不涉及。
- 边界与未运行项：未改产品代码（负向对照用的旧实现已按 sha256 比对恢复）、schema、API 契约、CSS、依赖或 Next 配置；QAM-05-006（`serverError` 泄漏 `Error.message`）与 QAM-05-004（Agent projection 失败恢复）保持独立开放，未被本项吞并。未跑 `npm run test:compose-smoke`、Compose 构建与实体设备。收尾清理 `coverage/`、`test-results/`、`playwright-report/` 与 `/tmp/feat-072-*`；`check:full` 以生产构建收尾会把构建生成的 [`next-env.d.ts`](next-env.d.ts) 类型引用改写到 `./.next/types/*`，已还原为仓库既有引用并确认还原后 `npm run typecheck` exit 0。
- 本轮未提交、未推送。

<a id="feat-071"></a>

## 2026-09-20 — feat-071：Post 写入边界的统一运行时输入契约

- 来源与授权：本轮用户要求「阅读 AGENTS.md、feature_list.json、progress.md、session-handoff.md，按优先级排序，从第一性原理修复下一个 P2」。根列表 17 项全部 done、没有 not-started，故按 `priorityPolicy`（核心操作受阻程度 → 当前产品入口影响范围 → 触发频率 → 恢复成本，同分时优先有直接行为证据者）排序 [`module-quality-overview.md`](docs/optimization/module-quality-overview.md) 的 26 项开放 QAM P2，并用 AskUserQuestion 确认方向后登记本项；来源 QAM-05-001，依赖为空，`featuresNumber` 17→18。
- 排序依据（为什么选它）：发布/编辑是产品核心入口，却绕过了仓库唯一的运行时段。`/posts/new`、`/posts/edit/[slug]` 的 Server Action 与 `/api/posts` 两条写路径都只做 truthy/`trim()` 判断后把值直接交给 Prisma，违反 [`AGENTS.md`](AGENTS.md) 硬约束 6（不可信输入必须先经 `lib/validation.ts` 或同域 Zod 校验）。触发不需要任何异常前提：Non-string 字段来自任意手写请求，也来自编辑器把空白输入提交成空 patch；结果是用户可见的失真（编辑器把内部异常 message 当文案显示、只填空格的更新静默成功并跳转）。QAM-05-006 只覆盖错误路径泄漏、修复面更窄；QAM-05-004 需要迁移与 Worker 重试边界；QAM-07-004 需要新增迁移；QAM-06-002/005 入口更窄。
- 启动基线：`./scripts/run-node22.sh ./init.sh` exit 0（Prisma generate、资产校验、typecheck、lint 与 **84 文件/711 项** Node/组件通过，本轮新增用例前）。
- 缺陷链条（第一性原理）：TypeScript 类型不是运行时约束，而网络入口是客户端可构造的。`POST /api/posts` 与 `PUT /api/posts/:slug` 直接 `request.json()` 解构后只判断 truthy 与 `trim()`，`createPost`/`updatePost`/`deletePost` 的 Server Action 参数同样只靠 TS 类型。于是三件事都发生：①非字符串 title 让 `title.trim()` 抛错，Route 变成不稳的 500，Action 把逐字的 `Server error: title?.trim is not a function` 交给 PostEditor 显示；②超长正文没有上界地进入数据库；③`{ title: "   ", content: "   " }` 通过 truthy 检查后 `prisma.post.update({ data: {} })` 静默 no-op，Action 返回成功、编辑器跳转到文章页，而库内实际未变——这是 UI 可达的结果失真，不是纯纪律问题。
- 复现（先红）：新增 [`tests/server/post-write-contract.test.ts`](tests/server/post-write-contract.test.ts)（mock `@/lib/prisma`、`@/lib/auth`、`next/cache`，只替换进程外边界）与 [`tests/integration/post-write-contract.integration.test.ts`](tests/integration/post-write-contract.integration.test.ts)（真实 PostgreSQL）。旧实现下 Node 层 `Test Files 1 failed (1)`、`Tests 15 failed | 5 passed (20)`：非字符串、数组字段与畸形 JSON 的 POST/PUT 为 `expected 500 to be 400`，超长正文与 PUT 空 patch 为 `expected 200 to be 400`，Action 返回逐字 `{"error": "Server error: title?.trim is not a function"}`，非字符串 slug 的 `deletePost` 仍执行删除。真实 PostgreSQL 层 `Tests 6 failed | 2 passed (8)`，除同样的状态码偏差外，`createPost` 在真实 Prisma 下返回 `Server error: Invalid \`prisma.post.findUnique()\` invocation in …`，说明泄漏的确实是内部实现细节而不只是拼接字符串。
- 修复边界（只加共享契约 + 接线四个写入点）：[`lib/validation.ts`](lib/validation.ts) 新增 `POST_TITLE_MAX_LENGTH`(200)、`POST_CONTENT_MAX_LENGTH`(20000)、`postCreateSchema`、`postUpdateSchema` 与 `postSlugSchema`。字段 schema 用 `z.string({ error: "…" })` 提供类型错误文案、`.trim()` 在校验前生效（纯空白因此等价于缺字段）、`.min(1)`/`.max()` 提供长度错误文案；`postUpdateSchema` 用 `.refine()` 拒绝空 patch 并给出 `At least one field is required`；Zod 默认剥离未知键，`type`/`authorId` 无法注入。[`POST /api/posts`](app/api/posts/route.ts) 与 [`PUT /api/posts/:slug`](app/api/posts/%5Bslug%5D/route.ts) 改用 `readJsonBody(request, …)`（错误经既有 `errorToResponse` 变为 `400 {error:"Invalid request",issues:[…]}`），[`createPost`/`updatePost`/`deletePost`](app/actions/posts.ts) 改用 `parseBody(…)`，catch 中把 `ValidationError` 映射为首条 issue 文案的 `invalidInput()`，`serverError` 原样保留给其余异常。更新路径由 `title !== undefined` 同时驱动 slug 重分配与 `data` 组装，保持「未提供标题即不重新分配 slug」。
- 实测确认的 Zod 语义（不靠推断）：用临时探针文件（已删除）确认 `.trim()` 先于 `.min()`/`.max()` 生效、`z.string({ error })` 的类型文案可用、非对象输入的 issue `path` 为 `""`、`{ title: undefined, content: undefined }` 等价于空 patch、多余键被剥离。
- 验收对照（先红后绿）：Node 层同命令修复后 **20/20**；真实 PostgreSQL 层 **8/8**，连同 [`post-slug-allocation.integration.test.ts`](tests/integration/post-slug-allocation.integration.test.ts) 为 `Test Files 2 passed (2)`、`Tests 13 passed (13)`（17.18s）。定向 Node 回归 4 文件/96 项通过（含 `tests/server/posts-api.test.ts`、`tests/lib/posts.test.ts`、`tests/agent/agent-posts.test.ts`），`npm run typecheck` 与 `lint` exit 0。未放宽断言、未加 skip、未改超时。
- 覆盖的行为：Route 与 Action 对非字符串/数组字段、畸形 JSON、超长（200/20000 边界与 +1）、纯空白、空 patch（PUT 的 issue `path` 为 `[""]`）返回同一可观察验证错误且不触库、不生成 slug；多余字段不改变写入内容；只填空格的更新使整行（含 `updatedAt`）与修复前完全相等；合法中文/Markdown 以 trim 后的值创建与更新且 `slug === generateSlug("中文标题")`；Action 的非法 slug 不查询、不删除既有文章。
- 测试层级选择（如实记录未补的层）：按 [`docs/testing-standards.md`](docs/testing-standards.md) 的选择规则，本缺陷落在「Route Handler/服务编排」（`tests/server`）与「真实数据库持久化边界」（Testcontainers 集成）两层，两者都已覆盖。**未新增组件测试或 E2E**：组件测试会把 `@/app/actions/posts` 整个 mock 掉，因此它在修复前不可能失败，不满足仓库「缺陷修复的测试必须能失败在修复前」的规则，只会变成对未改动渲染路径的重复断言；E2E 超出本项登记的第 3 条验收标准（明确写入范围是「Route 与 Server Action 行为测试 + 真实 PostgreSQL 持久化边界」），按 AGENTS.md 不隐式扩大验收面。编辑器「错误横幅 + 不跳转」的渲染链路是 PostEditor 既有行为、本轮未改动，且已由 Route/Action 返回值断言在契约层固定。
- 文档同步：[`PROJECT_VIEW.md`](PROJECT_VIEW.md) 的 QAM-05 In Scope 增加写入边界契约条目，Interfaces 列出 `postCreateSchema`/`postUpdateSchema`/`postSlugSchema` 与长度常量。
- 质量结论：[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 将 QAM-05-001 设为 resolved，抽象与复用 7→8、健壮性 11→12、总分 82→84、Trend `+2`，Gate/Final 维持 L2（Agent projection 失败恢复仍缺专项 E3，通用错误 message 泄漏仍开放）；[模块总览](docs/optimization/module-quality-overview.md) 均分 86.5→86.7、开放 QAM P2 由 26 降为 25，评分历史只追加。
- 完整门禁（验收标准第 4 条）：`./scripts/run-node22.sh npm run check:full` **单次 exit 0**——标准门禁 85 文件/**731 项**（`✓ Compiled successfully in 18.8s` 的生产构建）、覆盖率 52.57/46.09/57/53.21（statements/branches/functions/lines，门槛 40/35/45/40）、真实 PostgreSQL **32 文件/126 项**（118.25s，较上轮多出本项新增的写入契约文件 8 项）、生产 Playwright **43 passed（4.0 分钟，无 skip/retry）**。日志中不存在 `npm ERR!`/`ELIFECYCLE`/非零退出。
- 副作用（如实记录）：`check:full` 以生产构建收尾，会把构建生成的 [`next-env.d.ts`](next-env.d.ts) 类型引用改写到 `./.next/types/*`（开发服务写回 `./.next/dev/types/*`），收尾时恢复为仓库既有引用。
- 边界与未运行项：未改 schema、迁移、依赖、Next 配置、Post 可见性、分页契约、timeline 投影、成功响应形状或通用错误响应策略（`serverError` 的通用错误泄漏仍由 QAM-05-006 跟踪，未顺手扩大）；未把 `createAgentLogPost` 的服务端内部投影纳入该契约（它不是不可信输入边界）；未跑 `npm run test:compose-smoke`、Compose 构建或实体设备。收尾清理 `coverage/`、`test-results/`、`playwright-report/` 与 `/tmp/feat-071-*` 临时产物。本轮未提交、未推送。

<a id="feat-070"></a>

## 2026-09-20 — feat-070：Post slug 并发唯一性分配

- 来源与授权：本轮用户要求「阅读 AGENTS.md、feature_list.json、progress.md、session-handoff.md，按优先级排序，从第一性原理修复下一个 P2」。根列表 16 项全部 done、没有 not-started，故按 `priorityPolicy`（核心操作受阻程度 → 当前产品入口影响范围 → 触发频率 → 恢复成本，同分时优先有直接行为证据者）排序 [`module-quality-overview.md`](docs/optimization/module-quality-overview.md) 的 27 项开放 QAM P2，并用 AskUserQuestion 与用户确认方向后登记本项；来源 QAM-05-002，依赖为空，`featuresNumber` 16→17。
- 排序依据（为什么选它）：并发发布或改题（两位成员同时使用同一标题，或把标题改成已存在标题）会让「发布」这一核心操作硬失败，且 Server Action 把 `Unique constraint failed` 内部错误直接显示给用户；入口覆盖 `/home` 发布器（Server Action）、`/api/posts` 与 Agent 投影三条共享同一 helper 的路径。QAM-05-004 同样影响时间线但需要迁移与 Worker 重试边界、触发依赖异常窗口；QAM-06-006/008 只影响没有导航入口的 `/chat/[roomId]/atlas`；QAM-09/10 属交付与治理债务。
- 启动基线：`./scripts/run-node22.sh ./init.sh` exit 0，Prisma generate、资产校验、类型、lint 与 84 文件/703 项 Node/组件通过（本轮新增用例前）。
- 缺陷链条（第一性原理）：[`lib/posts.ts`](lib/posts.ts) 的 `ensureUniqueSlug(baseSlug, excludeId?)` 先 `prisma.post.findUnique` 判断占用情况并返回候选，调用方随后才 `post.create`/`post.update` 写入。唯一性是「检查」与「写入」之间的瞬时事实，两个请求可同时观察到候选不存在并写入同一 slug；数据库 unique index 只能让后到者抛错，无法为请求提供可重试的唯一 slug——唯一约束于是成了错误出口，而不是服务内部的并发事实来源。受影响写入点共四个：`POST /api/posts`、`PUT /api/posts/:slug`、Server Action `createPost`/`updatePost` 与 Agent 时间线投影 `createAgentLogPost`；后者以 `agent-log-${Date.now()}` 为基础 slug，同毫秒并发必然撞同一候选。
- 确定性复现（真实 PostgreSQL，非 mock）：新增 [`tests/integration/post-slug-allocation.integration.test.ts`](tests/integration/post-slug-allocation.integration.test.ts)，用 `BEFORE INSERT`/`BEFORE UPDATE` 触发器加 `pg_sleep(0.5)`（与 `session-issuance` 并发夹具同形）卡住写入，使两个请求都越过「检查」阶段后再落唯一索引，再以 `Promise.all` 并发调用。旧实现下并发发布相同标题的两个请求返回 `[200, 500]`，失败体带 `Unique constraint failed on the fields: ('slug')`；同一文件旧实现 `Test Files 1 failed (1)`、`Tests 4 failed | 1 passed (5)`，失败断言形如 `expected [ 200, 500 ] to deeply equal [ 200, 200 ]`。第 5 项（Agent 投影在基础 slug 已被占用时顺延）在旧实现下即通过，如实记为回归保护而非负向对照。
- 错误分类证据（实测，不靠推断）：vitest 会吞掉探针里的 `console.log`，故改用一条刻意失败的断言让 vitest 打印对象，得到 Post 唯一冲突的真实形状 `{ code: 'P2002', meta: { modelName: 'Post', target: ['slug'] } }`。据此把「可重试」判定为 `code === 'P2002'` 且 `meta.target` 含 `slug`（string 与数组两种形态都接受），**缺少 `target` 的 `P2002` 不重试**，避免把其他唯一约束冲突当成 slug 竞态。临时探针文件已删除。
- 修复边界（只改 [`lib/posts.ts`](lib/posts.ts) 的 slug 分配 + 四个调用点的接线）：新增 `writePostWithUniqueSlug(baseSlug, write)`——直接以候选 slug 执行写入，只在上述 Post slug 唯一冲突时按 `-2`、`-3`… 顺延重试（候选序列由 `slugCandidate` 生成：第 1 次用基础 slug，其后 `${baseSlug}-${attempt}`），最多 `POST_SLUG_ATTEMPT_LIMIT`（5）个候选，超限上抛最后一次冲突；非 slug 冲突与其他数据库错误（如 `P1001`）原样上抛、不重试。`lib/posts.ts` 不再 `import { prisma }`，旧 helper `ensureUniqueSlug` 与它唯一的 `findUnique` 一并删除。更新路径只在提交了新标题时才重新分配 slug（`retitle` 标志），未改标题的更新不生成候选；把 slug 写回同一行（改回原标题）不触发唯一冲突，语义与旧的 `excludeId` 自排除一致。
- 产品规则未变：`generateSlug` 的字符集过滤、80 字符截断、中文转拼音与全清空回退 `post` 均未触碰；后缀序列仍从 `-2` 起，语义仍是「取第一个可用候选」。
- 验收对照（真实 PostgreSQL）：`./scripts/run-node22.sh npm run test:integration -- tests/integration/post-slug-allocation.integration.test.ts` 修复后 **5/5**（13.04s），五项分别覆盖并发发布相同标题（两个 200，落库 slug 为 `base`/`base-2`）、并发把两篇改成同一标题（两个 200，`base`/`base-2`）、编辑器 `createPost` 并发（两者均成功且 slug 不同）、`updatePost` 并发、Agent 投影在基础 slug 被占用时顺延为 `agent-log-<ms>-2`。负向对照：把 `writePostWithUniqueSlug` 的函数体替换回旧的 check-then-write（并临时恢复 prisma 导入）后同一文件为 `4 failed | 1 passed`，再从 `/tmp/feat-070-fixed-posts.ts`（本轮的修复副本，已随临时产物在收尾时清理）恢复并比对 sha256 一致后复跑 5/5；未放宽断言、未加 skip、未改超时。
- Node 定向回归（3 文件/76 项通过）：[`tests/lib/posts.test.ts`](tests/lib/posts.test.ts) 新增 5 项，覆盖首次直用基础 slug、`-2`/`-3` 顺延后成功、非冲突错误不重试且只调用一次、`target: ["email"]` 的 `P2002` 不重试且只调用一次、连续冲突到上限后上抛最后一次冲突且候选序列为 `base`…`-5`；该文件用本地 `slugConflictError()` 工厂按实测形状构造错误，Prisma 形状变化会让用例显式失败而不是静默退化。[`tests/server/posts-api.test.ts`](tests/server/posts-api.test.ts) 新增「slug 被并发占用时按后缀重试并返回顺延后的 slug」（两次 `create`，slug 依次为 `my-first-post`/`my-first-post-2`，响应 200）与「非冲突数据库错误不重试并保持失败响应」（`P1001`，`create` 只调用一次，响应仍为失败）。[`tests/agent/agent-posts.test.ts`](tests/agent/agent-posts.test.ts) 新增投影遇冲突顺延用例（`create` 两次，第二次 slug 匹配 `/-2$/`）。三份套件同时删除 3 处已无意义的 `mockPostFindUnique.mockResolvedValue(null)` 残留桩。
- 文档同步：[`PROJECT_VIEW.md`](PROJECT_VIEW.md) 的 QAM-05 服务接口列表把已不存在的 `ensureUniqueSlug()` 改为 `writePostWithUniqueSlug()`；QAM-05 报告的架构图与失败路径段落同步。
- 质量结论：[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 将 QAM-05-002 设为 resolved，健壮性维度 10→11、总分 81→82、Trend `+1`，Gate/Final 维持 L2（Agent projection 失败恢复仍缺专项 E3）；[模块总览](docs/optimization/module-quality-overview.md) 均分 86.4→86.5、开放 QAM P2 由 27 降为 26，评分历史只追加。
- 完整门禁（验收标准第 4 条）：`./scripts/run-node22.sh npm run check:full` **单次 exit 0**——标准门禁 84 文件/711 项、`✓ Compiled successfully` 的生产构建、覆盖率 52/45.91/56.53/52.66（门槛 35/45/40/40）、真实 PostgreSQL **31 文件/118 项**（106.23s，较上轮多出本项新增的并发文件 5 项）、生产 Playwright **43 passed（3.9 分钟，无 skip/retry）**。
- 副作用（如实记录）：`check:full` 以生产构建收尾，会把构建生成的 [`next-env.d.ts`](next-env.d.ts) 类型引用改写到 `./.next/types/*`（开发服务写回 `./.next/dev/types/*`）；本轮已确认两种引用下 `npm run typecheck` 均 exit 0，并把该文件恢复为仓库既有引用。
- 边界与未运行项：未改 schema、迁移、Post 可见性、分页契约、timeline 投影字段或 API 请求/响应契约（`serverError` 的通用错误泄漏仍由 QAM-05-006 跟踪，未顺手扩大）；未跑 `npm run test:compose-smoke`、Compose 构建或实体设备。收尾清理 `coverage/`、`test-results/`、`playwright-report/` 与 `/tmp/feat-070-*` 临时产物。本轮未提交、未推送。

<a id="feat-069"></a>

## 2026-09-19 — feat-069：首页 anchor 补齐与 Post 删除交错

- 来源与授权：本轮用户要求「阅读 AGENTS.md、feature_list.json、progress.md、session-handoff.md，按优先级排序，从第一性原理修复下一个 P2」。根列表 15 项全部 done、没有 not-started，故按 `priorityPolicy`（核心操作受阻程度 → 当前产品入口影响范围 → 触发频率 → 恢复成本，同分时优先有直接行为证据者）排序 `docs/optimization/module-quality-overview.md` 的 28 项开放 QAM P2，并用 AskUserQuestion 与用户确认方向后登记本项；来源 QAM-05-008，依赖为空，`featuresNumber` 15→16。
- 排序依据（为什么选它）：正常用户操作（另一标签页删除文章与首页读取交错）即可让主入口 `/home` 返回 500，属「核心操作受阻」且入口影响范围最大；触发窗口窄、刷新可恢复，故未升为 P1。Atlas-only 问题影响面窄，因为 `/chat/[roomId]/atlas` 没有导航入口，只有 `/home` 消费实时空间入口。
- 启动基线：`./scripts/run-node22.sh ./init.sh` exit 0，Prisma generate、资产校验、类型、lint 与 84 文件/699 项 Node/组件通过（本轮新增用例前）。
- 缺陷链条：首页 [`app/home/page.tsx:15-29`](app/home/page.tsx#L15-L29) 先读取最多 50 个可见 Post，再单独调用 `ensureHomePostElements` 执行 `findMany → createMany`。两步之间若 Post 被删除，`createMany` 会用已消失的 `postId` 写入而违反 `AtlasElement_postId_fkey`；`skipDuplicates`（`ON CONFLICT DO NOTHING`）只处理唯一冲突，不处理外键冲突，因此整批失败并沿 RSC 渲染上抛为 500。
- 交错复现（真实 PostgreSQL，确定性）：[`tests/integration/home-post-anchor-interleave.integration.test.ts`](tests/integration/home-post-anchor-interleave.integration.test.ts) 用 `vi.spyOn(prisma.post, "findMany")` 的 `mockImplementationOnce` 在首页第一次读取返回后立刻 `deleteMany` 目标 Post，把删除精确卡在读取与补齐之间。未修复实现 `./scripts/run-node22.sh npm run test:integration -- tests/integration/home-post-anchor-interleave.integration.test.ts` exit 1，`Test Files 1 failed (1)`、`Tests 2 failed | 1 passed (3)`，两次 `PrismaClientKnownRequestError: Foreign key constraint violated: AtlasElement_postId_fkey (index)`，抛出点 `lib/home-board.ts:78`，冒泡自 `Module.HomePage app/home/page.tsx:25`；该原始输出已随本轮临时日志在收尾时清理，结论与命令保留在本文。
- 错误分类证据（实测，不靠推断）：vitest 会吞掉探针里的 `console.log`，故改用一条刻意失败的断言让 vitest 打印对象，得到 `{ code: 'P2003', meta: { field_name: 'AtlasElement_postId_fkey (index)', modelName: 'AtlasElement' }, name: 'PrismaClientKnownRequestError' }`。据此把「Post 已不存在」判定为**回读该 Post**（`post.findUnique` 返回 `null` 才跳过），而不是匹配 schema 名或错误 message，使无法归因到删除的外键冲突仍会上抛。
- 可达性核查（决定容忍范围）：anchor 插入涉及的三个外键只有 `postId → Post` 可被正常用户操作触发——仓库没有用户删除路径，`app/api/atlas/route.ts` 的 DELETE 只清空全局 atlas board、不涉及 `home-board`。因此只容忍 Post 消失，`boardId`/`createdById` 等其他外键冲突继续上抛。
- 修复边界（只改 [`lib/home-board.ts`](lib/home-board.ts)）：保留原批量 `createMany({ skipDuplicates: true })` 快路径；仅当整批因外键被打断时进入 `anchorPostsAfterInterleavedDeletion`，逐条 `create` 让每个 anchor 独立落库，只容忍两类冲突——逐条外键冲突后回读确认 Post 已消失则跳过、`P2002`（并发请求已建立该 anchor）则跳过——其余错误原样上抛。抽出 `toHomePostElement` 使快路径与补写路径共用同一字段定义。`getHomeBoardElementAccessWhere`、`getHomeBoardConnectionAccessWhere`、`getHomeBoardSnapshot`、`getOrCreateHomeBoard`、Post 可见性、分页契约与坐标字段均未改变；`app/home/page.tsx:25` 是全仓库唯一的 `ensureHomePostElements` 生产调用点。
- 交错回归的语义修正：第一版断言被删除的 Post 在**同一次**渲染中消失，这与 read-committed 实际语义不符——该次响应由删除前的读取结果渲染，陈旧条目合法出现。已改为首次渲染断言两个幸存者存在、第二次渲染才断言精确的幸存者列表，并断言被删除 Post 的 Post 行与 anchor 均为 0、重复补齐不产生重复记录。
- 测试辅助修正：`prisma.post.delete` 在第二个用例里对同一条记录二次执行会抛 `P2025`（`Record to delete does not exist`），改为 `prisma.post.deleteMany` 使其幂等。
- Node 定向回归：[`tests/lib/home-board.test.ts`](tests/lib/home-board.test.ts) 扩到 6 项，分别覆盖正常补齐、Post 已不存在时的逐条补写（其余 Post 照常建立）、并发 anchor 已存在的 `P2002` 容忍、Post 仍存在的外键冲突必须上抛、非冲突数据库错误（`P1001`）必须上抛且不进入补写。负向对照：`git checkout -- lib/home-board.ts` 还原旧实现后 `npm run test:unit -- tests/lib/home-board.test.ts` 为 `2 failed | 4 passed`，两条容忍用例以 `promise rejected "Error: Foreign key constraint violated"` 失败；从 `/tmp/feat-069-fixed-home-board.ts` 恢复后 6/6。
- 修复后定向复跑：`./scripts/run-node22.sh npm run test:unit -- tests/lib/home-board.test.ts` 6/6（147ms）、`./scripts/run-node22.sh npm run test:integration -- tests/integration/home-post-anchor-interleave.integration.test.ts` 3/3（7.37s）。负向对照与修复后两轮使用同一文件，未放宽断言、未加 skip、未改超时。
- 首次完整门禁 `./scripts/run-node22.sh npm run check:full` **exit 1**：标准门禁 84 文件/703 项、`✓ Compiled successfully in 6.9s`、覆盖率 51.92/45.75/56.37/52.53（门槛 35/45/40/40）通过；真实 PostgreSQL 30 文件/113 项通过；开发 Playwright 42 passed/1 failed（6.5m）。唯一失败为末项 `tests/e2e/authenticated.spec.ts:1397` 的 `同毫秒文章在首页与搜索保持稳定顺序，真实 API 分页无遗漏`，`Test timeout of 90000ms exceeded`，卡在 `page.waitForResponse`（`authenticated.spec.ts:1423`，等待 `/api/posts?q=`），超时未放大。失败前 `:1421` 的首页标题断言已通过，失败点不在首页装配路径；同期宿主 swap 已用 1522MB。
- 失败归因（负向对照，不改产品）：该用例单独运行 `./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '同毫秒文章在首页与搜索保持稳定顺序，真实 API 分页无遗漏'` exit 0、2/2（25.9s），业务用例 **10.0s** 通过。与 [feat-068](#feat-068) 已记录的同一用例同一签名（当时单独运行 9.5s）一致，判为宿主容量边界，且与本轮改动无关——搜索走 `/api/posts`，不经过 `lib/home-board.ts`。未用此项失败掩盖本轮结论，也未据此放宽断言或跳过。
- 第二次完整门禁 `./scripts/run-node22.sh npm run check:full` **exit 1**：标准门禁 84 文件/703 项、`✓ Compiled successfully in 4.2s`、覆盖率同为 51.92/45.75/56.37/52.53；真实 PostgreSQL 30 文件/113 项通过；开发 Playwright 41 passed/2 failed（5.0m）。两次运行的标准与集成层结果一致，只有浏览器层的失败项不同，且**都不是第一次的那一项**：`agent-entry-authenticated.spec.ts:336` 的 `observed.errors` 收到 `Failed to execute 'measure' on 'Performance': '​ChatIndexPage' cannot have a negative time stamp.`（与 [feat-068](#feat-068) 已归因为 React 开发期 User Timing 上游插桩噪声的字符串完全相同，同一条跨标签用例）；`authenticated.spec.ts:346` 的 `retries Home photo edits and deletions without false success after failures` 未在 5000ms 内等到 `照片标注保存失败。` 警报。
- 第二次失败的归因（独立重复，不改断言）：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts tests/e2e/agent-entry-authenticated.spec.ts --grep 'retries Home photo edits and deletions without false success after failures|跨标签退出卸载公开页入口与 WebGL' --repeat-each=3` exit 0、**7 passed（1.8m）**：跨标签用例 30.6s/16.8s/16.2s、照片重试用例 10.2s/7.6s/5.9s 全部通过，两次失败均未复现。两次连续完整门禁的失败项互不相同、且都能在单独运行下通过，符合 feat-068 记录的宿主容量边界形态；失败断言的代码路径（`/api/posts` 搜索、照片元素 PATCH、Agent 入口 `observed.errors`）都不经过本轮改动的 `ensureHomePostElements`。
- 质量结论：[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 将 QAM-05-008 设为 resolved，健壮性维度 9→10、总分 80→81、Trend `+1`，Gate/Final 维持 L2（slug 竞态与 Agent projection 恢复仍缺专项 E3）；[模块总览](docs/optimization/module-quality-overview.md) 均分 86.3→86.4、开放 QAM P2 由 28 降为 27，评分历史只追加。
- 第三次完整门禁 `NODE_OPTIONS=--max-old-space-size=4096 ./scripts/run-node22.sh npm run check:full` **exit 1**：该轮改用 feat-063/feat-067 通过时所用的 4096MB 堆上限配置。标准门禁 84 文件/703 项、`✓ Compiled successfully`、覆盖率同为 51.92/45.75/56.37/52.53；真实 PostgreSQL 30 文件/113 项通过；开发 Playwright **41 passed/2 failed（6.9m）**。失败项又是两项**新的**、与前两轮都不相同的用例：`agent-entry-authenticated.spec.ts:193` 的 `320px 页面表单、Study 与 Post 控件可操作` 报 `Tearing down "context" exceeded the test timeout of 90000ms`（清理阶段超时，非断言失败）；`agent-entry-authenticated.spec.ts:274` 的 `跨标签退出卸载公开页入口与 WebGL…` 在 `agent-entry-authenticated.spec.ts:311` 以 `expect(locator).toHaveAttribute` 失败——点击「显示密码」后 5000ms 内 `input` 仍为 `type="password"`（期望 `text`），即硬导航后表单 hydration 未完成。**第 1 轮失败的同毫秒文章用例在本轮同一运行内以 5.1s 通过**（`authenticated.spec.ts:1397`），直接说明第 1 轮那次是负载相关而非确定性缺陷。
- 第三次失败的归因（独立重复，不改断言）：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/agent-entry-authenticated.spec.ts --grep '320px 页面表单、Study 与 Post 控件可操作|跨标签退出卸载公开页入口与 WebGL' --repeat-each=2` exit 0、**5 passed（1.8m）**，320px 用例 14.1s、跨标签用例 15.0s，两项均未复现。至此三轮失败共 5 个互不相同的用例，每个单独复跑都通过，且失败代码路径（`/api/posts` 搜索分页、照片元素 PATCH、登录表单 hydration、Agent 入口 `observed.errors` 与上下文清理）都不经过本项改动的 `ensureHomePostElements`。
- 门禁判定与状态（如实记录，不以定向通过冒充门禁通过）：三轮 `check:full` 的**标准层（84 文件/703 项）与真实 PostgreSQL 层（30 文件/113 项）结果完全一致且全部通过**，失败只出现在开发 Playwright 层；三轮失败项互不相同、每项单独复跑均通过、且第 1 轮的失败项在第 3 轮同一运行内通过，符合 [feat-068](#feat-068) 已记录的宿主容量边界签名（本机 7.8GB 内存，三轮期间 available 一度低至 366–642MB、2GB swap 用满）。据此判为本项改动**无关**的环境性失败，未据此放宽断言、加 skip、改超时或扩大产品改动范围（遵循 AGENTS.md「无关的既有检查失败时，记录原始失败并保持当前范围」）。但验收标准第 4 条写的是「`npm run check:full` 通过」，三轮均未通过，故**本项状态置 `blocked`**，与 [feat-063](#feat-063) 在完整开发门禁未全绿时暂记 blocked 的既有处理一致；代码修正与定向证据（验收标准 1–3）已完成且先红后绿。解除阻塞的唯一动作是在宿主余量充足时重跑 `npm run check:full`；全绿后即可置为 `done`，无需改代码。
- 根因排查（第四轮，第一性原理）：前三轮把失败判为「宿主容量边界」只解释了机制的一半，没有回答为什么发布门禁会依赖宿主余量。逐层核对后确认根因在 Harness 配置：`npm run check:full` 从未声明浏览器层的应用模式，[`tests/e2e/support/app-mode.ts`](tests/e2e/support/app-mode.ts) 默认 `development`，而开发模式的服务端占用既无上界又由宿主推导——锁定版 `next dev`（`cli/next-dev.js`）在未显式给出 `--max-old-space-size` 时按 `os.totalmem() * 0.5` 推导上限（本机 3939MB），并按需编译每个访问到的路由且不卸载（Next 官方 memory-usage 文档：全部页面最终被请求后，占用与是否预加载无关）。仓库已为 Vitest 声明过 worker 内存预算（[`vitest.config.ts`](vitest.config.ts) 的 `MAX_TEST_WORKERS`），浏览器层却没有对应声明，于是门禁的通过与否由宿主余量决定。
- 根因的实测证据（全程采样，非推断）：`E2E_APP_MODE=development` 的一轮完整开发序列把测试服务推到 `next-server` RSS **2817MB**、堆 366MB→2039MB（上限 3939MB，14:03:48→14:08:09 单调增长），叠加 Chromium(627MB) 与 runner(152MB) 后浏览器层峰值约 **3600MB**；该轮宿主 used 6027MB、avail 1756MB、swap 1424MB，唯一失败又是**末项** `authenticated.spec.ts:1397`（堆峰值时刻），失败快照显示搜索框已有前缀而列表仍未筛选，即 90s 内未处理输入，而同一用例单独运行 9.5–10.0s 通过。
- 开发模式另有两个只属于它的失败源，均从锁定源码核对，且都不需要产品侧修复：(1) `server/lib/utils.js` 的 `getMemoryRestartStats` 仅在 `isDev` 时生效（堆超过上限 80% 即以 `RESTART_EXIT_CODE` 原地重启服务），生产形态恒返回 `undefined`；(2) `react-dom-client.development.js` 的 User Timing 插桩以 `actualStartTime = -1.1` 为哨兵，对未记录起始时间的组件发出负时间戳的 `performance.measure`，作为未捕获异常被 `observeEntry` 的 `pageerror` 断言捕获——该代码只存在于开发包，与第 2 轮失败的字符串完全一致。
- 修正（在正确层级声明资源契约，不是放宽门禁）：[`package.json`](package.json) 新增 `test:e2e:production`（`E2E_APP_MODE=production npm run test:e2e`）并把 `check:full` 的浏览器层指向它；[`docs/testing-standards.md`](docs/testing-standards.md) 按 Vitest 预算的同一先例记录上述实测数字与「不得靠放宽断言、跳过用例或放大超时掩盖该边界」的规则；[`tests/e2e/support/app-mode.ts`](tests/e2e/support/app-mode.ts) 注明默认值保留给本地迭代诊断。`npm run test:e2e` 的默认开发模式与开发期覆盖不变。**未放宽任何断言、未加 skip、未改超时、未改产品代码。**
- 第四轮完整门禁（根因修复后）：`./scripts/run-node22.sh npm run check:full` **单次 exit 0**——标准门禁 84 文件/703 项、`✓ Compiled successfully in 8.7s`、覆盖率 51.88/45.71/56.37/52.53、真实 PostgreSQL 30 文件/113 项、生产 Playwright **43 passed（3.7 分钟，无 skip/retry）**；前三轮共 5 个失败用例在同一轮内全部通过（含 `authenticated.spec.ts:1397` 同毫秒文章分页）。浏览器层峰值降到 **1850MB**（`next start` 242MB + Chromium 1344MB + runner 272MB），宿主 used 3496–3965MB、avail 3818–4287MB，swap 1120–1214MB 全程不增长（基线 993MB），对照开发轮次的 7.1–7.25GB used / 533–642MB avail / 2GB swap 用满。
- 副作用（如实记录）：`check:full` 现在以生产构建收尾，会把构建生成的 `next-env.d.ts` 类型引用改写到 `./.next/types/*`（开发服务写回 `./.next/dev/types/*`）。本轮已确认两种引用下 `npm run typecheck` 均 exit 0，并把该文件恢复为仓库既有引用。
- 边界与未运行项：未改 schema、迁移、API、Post 写入、Atlas 权限谓词或空间功能，未跑 `npm run test:compose-smoke`、Compose 构建或实体设备。本轮未提交、未推送。

<a id="feat-063"></a>

## 2026-09-13 — feat-063：Study 开发序列同步与隔离

- 范围：只处理 Study 浏览器启动反馈与失败后的用例隔离；保留原有未提交改动，不扩入 feat-064/065/066。归档 feat-035 已满足依赖。
- 历史失败：feat-055 的两次开发完整门禁均为同样两项 Study 失败；定向及生产模式通过不消除此记录，原始命令和现象保留在 [feat-055](#feat-055)。
- 启动基线：`VITEST_MAX_WORKERS=1 ./init.sh` exit 0，类型、lint、79 文件/682 项 Node/组件通过；继续隔离尚未复核的默认并发问题。
- 首次诊断：`DEBUG=pw:webserver E2E_APP_MODE=development E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run test:e2e -- --trace=on` 在未修改原 Study 操作/断言时运行，但全程 trace 与软件 WebGL 下出现明显内存压力（7.8GB 内存仅余 366MB available、2GB swap 用满）。About Router 初始化、Chat 时钟 hydration、1280px 宿主表单、跨标签身份及长生活字段等范围外用例失败；在到达 Focus 原用例前发送 SIGINT 停止，exit 130，19 passed/5 failed/1 interrupted/16 did not run，不能作为完整序列或 Study 结论。测试服务/容器已自动关闭。
- 常规配置原用例诊断：`DEBUG=pw:webserver E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e`，只增加临时 HTTP/数据库状态日志，保持原操作、断言与超时；最终报告 31 passed/10 failed（24.5m）。权限边界切换后原 process 句柄失效，退出码未回收，不能写为通过。两项 Focus 仍以原方式失败：首项 `getByText(/专注中 ·/)` 5000ms，第二项等待按钮和 `/api/study/start` 30000ms。原搜索失败由 feat-067 复核，其余范围外失败随高开销对照保留到 feat-068。
- 实际链条：首项 t=19324ms 发出 start，请求响应未被浏览器收到时 UI 断言已失败，清理钩子看到 start 按钮可见、running 不可见、DB idle；后一用例未发出 start，页面却显示 running、按钮不存在，DB 为 running（startedAt=`2026-09-13T14:14:42.012Z`，deadline 为 25 分钟后）。同期开发表现为 presence HTTP 200 需 13.7 秒，其中 Next 12.9 秒、application 795ms；浏览器 responseStart 约 13.8/14.0 秒，伴随后续 running 倒计时 hydration 差异。证据确认迟到写入跨越用例边界、共享用户且无失败清理；未收到首项 start 响应，不能虚构该响应的状态或精确编译耗时，也不把内存压力下的全部延迟归因于某个产品函数。
- 修正：`tests/e2e/support/study.ts` 每次建立独立 User/Room 并真实登录，finally 先关闭页面再事务删除 User/Room，利用既有 FK 级联清理认证和 Focus 数据；迟到写入只涉及已删除的独立用户。start 等待 HTTP 200/running/sessionKey 再做默认 UI 断言；stop 校验请求 key/HTTP 200，并等待真实 GET 回读 idle/同 key。原子结算、sessionKey、到期恢复和应用 UI/服务代码保持。
- 清理负向对照：临时移除 helper 的 User/Room 删除后，`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep 'Focus 用例失败后'` exit 1，1 failed/1 passed；真实启动 running 后回调抛错，下一次使用检查旧用户 `Expected: 0 / Received: 1`。测试结束立即恢复删除，隔离数据库自动清理。
- 首次修正回归：`./scripts/run-node22.sh npm run typecheck` exit 0；`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep 'starts and stops a focus session|reconciles one expired focus session|Focus 用例失败后'` exit 1，2 failed/2 passed：到期恢复通过，start/stop 与失败清理在辅助函数读取 stop `response.json()` 时达 30000ms，页面快照已显示 idle 和完成记录。已移除这个额外等待，改由应用实际消费的 GET 业务回读确认状态；遥测也不再等待 `response.finished()`，避免诊断自身成为同步门禁。
- 最终定向开发回归：原三项 grep 命令重跑 exit 0，4/4（含 setup，50.8s），业务用例耗时 16.8s/8.9s/7.9s。真实 start 响应均为 200/running，冷路径 responseStart 4214ms，其余 115–154ms；stop 200 后 GET 回读 idle、同 key，Session 恰一条。清理回归确认旧 User/Session/FocusState/FocusSession/Room 均无残留，下一独立用户从 idle 开始。
- 完整验收：`NODE_OPTIONS=--max-old-space-size=1536 VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` 的标准门禁通过（79 文件/682 项、生产构建、覆盖率 51.63/45.43/56.06/52.25），真实 PostgreSQL 29 文件/110 项通过。开发浏览器中出现 `Server is approaching the used memory threshold, restarting...`，320px 和 Session 到期入口用例失败。已核对锁定 Next 的 `server/lib/utils.js`：开发 heap 超过 limit 的 80% 会请求重启；1536MB 约 1.2GB 即触发，说明该临时验证上限过低。此轮最终浏览器结果见下一条；随后仅重跑浏览器层并将进程上限改为 4096MB；不修改应用配置、全局超时或重启机制。
- 1536MB 完整命令最终 exit 1：开发 Playwright 为 38 passed/4 failed（11.9m），Focus 三项全部通过（26.9s/12.5s/9.6s）；start HTTP 200，冷路径 responseStart 4305ms、其余 180–245ms，stop 的真实 GET 回读与数据库计数一致。范围外失败为 320px 清理请求 `ECONNREFUSED`、Session 到期入口超时、发帖导航落入 `chrome-error://chromewebdata/`、搜索 90000ms 超时；前三项附近有三次自动重启日志，搜索不能仅凭同时发生就归因于重启。
- 浏览器环境复核：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e`。仅增加本次测试进程的堆上限，不关闭 Next 内存监测、不改 Web/Worker 配置，不重跑已通过且代码未变的标准/PostgreSQL 层。
- 4096MB 完整开发复核：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e` exit 1，41 passed/1 failed（8.1m），无内存阈值重启；三个 Focus 用例 11.2s/5.8s/6.9s 通过，冷 start responseStart 1621ms，后续 48–103ms，HTTP 200 与业务/数据库断言一致。唯一剩余为原搜索用例 90000ms 超时，finally 的 `page.unroute` 因页面关闭而报错；快照停留在输入了查询前缀、仍展示未筛选首页的位置。该用例在改动前完整开发诊断也失败，保持 feat-067 独立处理。
- 阶段性阻塞：当时完整开发 Playwright 全绿的验收未满足，feat-063 暂记 blocked，未用三项 Focus 或生产模式成功替代这一条件。随后用户明确授权先处理 feat-067；生产对照结果和完整开发恢复证据分别见本项下文与 feat-067。

- 生产模式定向对照：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=production ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep 'starts and stops a focus session|reconciles one expired focus session|Focus 用例失败后'` exit 0，4/4（含 setup，1.5 分钟含生产构建）；三项分别 5.5s/4.1s/8.7s，start responseStart 28–36ms，stop 与后续 GET 也为 HTTP 200，业务与数据库断言均通过。

- feat-067 修正搜索防抖后，本项完整开发验收恢复：`NODE_OPTIONS=--max-old-space-size=4096 VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` 单次 exit 0，79/685、生产构建、覆盖率 51.58/45.39/56.06/52.25、真实 PostgreSQL 29/110、42/42 开发 Playwright（5.0m）。三项 Focus 11.2s/4.6s/7.4s，真实 start/stop/GET 与 Session/清理断言全部通过；冷 start responseStart 3403ms，后续 36–102ms。未修改 Focus 产品状态机；随后完成下述最终生产对照与工件收尾。

- 最终生产对照：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=production ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep 'starts and stops a focus session|reconciles one expired focus session|Focus 用例失败后|搜索失败保留已有文章|同毫秒文章'` exit 0，6/6（含 setup，1.2m 含生产构建）；三项 Focus 4.3s/3.0s/7.4s，搜索恢复 3.2s、分页 5.3s。Focus start responseStart 18–31ms，start/stop/GET 全部 HTTP 200；状态、结算、失败清理和搜索契约均通过。已核对并仅恢复生产构建自动生成的 next-env.d.ts 类型路径，保留原开发类型引用；未改 Next 配置。
- 收尾启动：清理诊断快照和测试报告后，`VITEST_MAX_WORKERS=1 ./init.sh` 在此前已验证的正常权限边界 exit 0；Prisma generate、资产校验、类型、lint、79 文件/685 项通过（Vitest 39.66s）。没有重复执行已通过且实现未再变更的完整应用门禁。
- 验收结论：真实证据确认了请求尚未完成就开始 UI 断言，以及共享用户的迟到写入跨用例污染；HTTP/业务同步和独立数据生命周期已修正。开发完整序列、真实 PostgreSQL、失败清理与最终生产对照全部满足根验收，feat-067 完成后本项返回并标为 done；没有确认需要新增 QAM-07 ID 的产品状态机缺陷。
- 验证边界：本轮最终完整门禁使用单 Vitest worker 和 4096MB Node 堆上限；默认并发问题仍由 feat-065/066 复核，全程 trace/软件 WebGL 及其他开发诊断仍由 feat-068 保留。未另跑 `npm run test:compose-smoke`、Compose 构建/重启、`npm run dev` 健康端点、生日主题专用矩阵或实体设备；本轮没有修改这些启动/部署/资产接缝，生产对照仅声明上述五条业务旅程与 setup 的 6/6。
- 工件、结构核验与当前下一步见 [feat-067 收尾](#feat-067)；两项原始失败证据均留在各自进度中。

<a id="feat-067"></a>

## 2026-09-14 — feat-067：搜索开发序列超时

- 2026-09-14 用户授权：先修复本项中完整开发门禁唯一剩余的搜索用例超时，再回到 feat-063；归档依赖 feat-040 为 done，已将本项设为 in-progress。
- 启动权限对照：`VITEST_MAX_WORKERS=1 ./init.sh` 在受限沙箱 exit 1，79 文件中 76 passed/3 failed、682 项中 675 passed/7 failed；Study 三个进程时区、one-shot Worker 时区和三种 Agent 主题子进程均为 `SyntaxError: Unexpected end of JSON input`（stdout 为空）。按权限型复核要求，以相同 Node.js 22.23.2/npm 10.9.8 在获准正常边界重跑原命令 exit 0，79/682 全通过，类型/lint/资产校验通过；未改代码掩盖沙箱差异。
- 搜索定向诊断：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '搜索失败保留已有文章' --trace=on` 在沙箱 exit 1，`Could not find a working container runtime strategy`、Web Server 未启动；获准正常边界重跑 exit 0、2/2（44.1s，搜索 19.9s）。只加入临时请求/导航日志及保证数据库清理的 finally，不改搜索操作/断言/超时。首次输入后 Clear search 按钮存在，300ms 后发出 RSC 导航，随后真实搜索 HTTP 200；500/401/network/retry 和文章详情全部到达。定向成功不能关闭完整序列失败，继续复核路由预热后的首次输入。
- 范围收敛：按用户明确授权，本项只处理完整开发门禁中唯一剩余的搜索超时。原高开销 trace/软件 WebGL 的完整对照验收和历史失败独立移入 feat-068；不把常规配置成功当作原问题关闭。新增依赖 feat-057 已核对为 done。

- 中断恢复：带轻量搜索日志的 `NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e` 在用户中断前观察到前 17 项通过；恢复后原进程句柄无效、临时日志不存在，运行中的 Docker 容器只剩原 `xoxo-meridian-postgres`，未回收最终报告/退出码，不计为完整门禁通过。仓库实现与进度保留。随后用同一隔离服务中的冷/热两次独立浏览器会话诊断搜索差异，新日志暂写入 test-results/feat-067-diagnostic，最终已清理；收尾检查全部容器时另发现并移除了中断留下的已退出测试容器，详情见本项收尾。

- 冷/热对照：上述定向 grep 在同一服务中执行两次独立浏览器会话（临时重复用例，随后移除），exit 0、3/3；冷搜索 18.4s，预热后首次输入 1382ms 时清除按钮已出现，1684ms 发出 RSC，1765ms URL 更新、1885ms 搜索 HTTP 200，错误恢复及详情也通过。单纯预热未复现，仍需完整开发序列证据，不据此猜测 hydration 或修改产品。

- 完整开发对照：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e` exit 0、42/42（5.6m，无 skip/retry），搜索 7.5s。轻量日志显示输入后 1025ms 时清除按钮存在，1332ms 发出 RSC，1501ms URL 更新，1516ms 发出搜索、5367ms 收到 200（responseStart 1829ms）；500/401/网络失败、键盘恢复、位置和详情均通过。三个 Focus 用例 12.8s/5.6s/7.3s，通过真实 start/stop/GET 与数据库断言，冷 start responseStart 3084ms、后续 42–96ms。本轮无 Next 内存阈值重启；并未确认历史搜索超时的产品根因，资源压力归因继续留在 feat-068，不声称高开销配置已恢复。
- 阶段性收敛：移除临时事件日志和冷/热重复用例；搜索操作通过 Playwright step 区分输入/URL 与 HTTP/列表，先验证真实首次查询结果已渲染再切入错误保留，不新增重试、mock 成功数据或放宽超时。finally 关闭本页，在外层 finally 中事务清理作者/文章并断开数据库，不依赖页面关闭后的 unroute。此时尚未改 SearchInput、HomeTimelineBoard、API 或产品模型，也未确认产品根因；后续复现与 QAM-05-009 的登记见下文。
- 中间类型检查：`./scripts/run-node22.sh npm run typecheck` exit 2，原因是误用 .ts 后缀保存在 test-results 的两份源码快照进入 tsconfig 的全局 TypeScript include，报相对 support 模块缺失及派生隐式 any；已把本轮快照改为 .txt，不改 tsconfig/应用代码排除错误。随后最终标准门禁通过：类型、lint、79/682、生产构建、覆盖率 51.58/45.39/56.06/52.25。

- 关闭页面的失败清理对照：临时复制搜索用例，在真实页面/作者/两篇文章/锚点已建立后主动关闭页面并抛出原始失败，`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '搜索清理失败回归'` 如预期 exit 1、1 failed/1 passed（21.8s）。报告唯一错误为注入的“搜索清理回归：页面关闭后的原始失败”，未被 unroute/target-closed 遮蔽；真实 PostgreSQL 回查 User/Post/AtlasElement 为 `[0,0,0]`。临时故障用例和计数日志已立即移除，随后执行最终正常完整门禁。

- 最终门禁首轮实际结果：`NODE_OPTIONS=--max-old-space-size=4096 VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` exit 1；标准门禁 79/682、生产构建、覆盖率 51.58/45.39/56.06/52.25、真实 PostgreSQL 29/110（98.90s）通过，Playwright 为 39 passed/3 failed（7.9m）。除两项搜索 90000ms 外，跨标签用例有 `Performance.measure: ChatIndexPage cannot have a negative time stamp`，后者保留到 feat-068，未过滤 observed.errors。中途仅查看最后几项通过日志而漏看前面的跨标签失败，导致“前 30 项通过”的口头进度不准确，以此完整结果纠正。
- 搜索准确失败点：新 step 中 fill 15ms、Clear search 可见断言 2ms 通过，随后 waitForURL 和 waitForResponse 均达到原 90000ms；快照仍为输入前缀及未过滤的三篇文章。尚未到达本轮新增的 response.json/结果断言，故不是额外读取响应体造成的等待。清理没有再以 unroute 错误遮蔽原始超时。后续同时间分页用例也在等待搜索响应时超时。
- 产品根因 QAM-05-009：SearchInput 在 onChange 创建防抖定时器，而挂载 effect 只有清理、没有创建。首次挂载期间输入已更新本地 value 后，开发 StrictMode 的 effect 重放清除定时器却无处重建，留下“有输入/清除按钮、无导航”的状态。按用户授权的产品修正范围实施；使用 xoxo-qam-05-content-timeline-review 技能核对 QAM 边界/报告，不把纯审查技能的限制误作新的实施授权要求。
- 稳定回归：新组件用例在 StrictMode 首次挂载期间只派发一次原生 input，验证输入值/清除按钮与最终导航；旧实现 `./scripts/run-node22.sh npm test -- tests/component/search-input.test.tsx` 最终为 1 failed/4 passed，唯一失败为导航调用数 0。前期 fireEvent/act 版本有 act 提示，最终改为原生 input.value setter/事件且异步 act 完整等待后无此提示。最初新增的清空/卸载用例混用 user-event 与假时钟造成三项 5000ms 测试超时；已改为受控时钟下一次输入/点击事件，未改应用超时、关闭断言或保留待决异步任务。
- 最小产品修正：pending draft 驱动 effect 同时创建和清理 300ms 定时器，重放时恢复未提交查询；输入/清空仍立即取消旧 timer，清空立即导航 /home，外部 q 仍以 key 更新本地草稿。未改搜索参数、API、可见性、展示投影或数据库。`VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm test -- tests/component/search-input.test.tsx tests/component/home-timeline-board-search.test.tsx tests/server/search-input.test.ts` exit 0，3 文件/23 项通过（含新 StrictMode、清空、卸载回归）；继续真实浏览器验收。

- 产品修正后的真实开发浏览器：`NODE_OPTIONS=--max-old-space-size=4096 E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '搜索失败保留已有文章|同毫秒文章'` exit 0、3/3（39.1s，搜索恢复 14.2s、分页 5.4s）。两条旅程均通过真实 URL/HTTP/渲染与查询结果；仍保留错误注入、键盘恢复、两地位置、53 条分页完整性。已提交的查询会清除待提交标志，避免后续 effect 恢复重复提交同一草稿；随后重跑相关组件与最终完整门禁。

- 产品修正后的最终完整门禁：上述 `NODE_OPTIONS=--max-old-space-size=4096 VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` 单次 exit 0；79 文件/685 项 Node/组件、Next.js 16.3.3 production build、覆盖率 51.58/45.39/56.06/52.25、29 文件/110 项真实 PostgreSQL（100.77s）、42/42 开发 Playwright（5.0m，无 skip/retry）。搜索恢复 7.4s、同时间分页 5.9s；五个相关业务旅程均通过，原 500/401/network、键盘与位置/分页断言保留。此次跨标签步骤也通过，未以此取消 feat-068 的历史 Performance.measure 及高开销诊断记录。随后最终生产对照与收尾也已通过。

- 最终代码生产搜索/Focus 对照 6/6；收尾 `VITEST_MAX_WORKERS=1 ./init.sh` exit 0、79/685，原始生产命令与耗时见 [feat-063](#feat-063)。
- 质量结论：[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 将 009 设为 resolved，保留旧输入无导航及红/绿、完整开发和生产证据；恢复既有搜索契约，评分仍为 80、Score L3、Gate/Final L2，其余五个 P2 保持开放。[模块总览](docs/optimization/module-quality-overview.md) 同步追加本轮历史，均分 86.2、开放 QAM P2 为 28。
- 工件清理：移除本轮 coverage、test-results、playwright-report 及其中全部诊断日志、源码快照、HTML/trace 和临时用例；长期失败证据留在本项及 feat-063/068。中断容器 `312a0130344b` 的只读检查在沙箱因 Docker socket `permission denied` exit 1，获准正常边界重跑 exit 0；Created 为 `2026-09-13T16:52:32.042779493Z`，Testcontainers 标签、postgres:16-alpine、`POSTGRES_DB=xoxo_meridian_e2e` 与本轮中断诊断吻合。`docker rm -v 312a0130344b` exit 0，随后确认容器及匿名卷均消失；仅保留原 `xoxo-meridian-postgres`（healthy）。没有本轮 E2E 临时目录、测试服务进程或 Testcontainers 网络/卷残留，未创建测试镜像。
- 状态收尾：使用 harness-creator 的证据与交接规则同步根状态，feat-067 done 后返回 feat-063 并完成其验收；重写 session-handoff，仅保留下一会话需要的摘要和恢复命令。本轮未达到批量归档阈值，原验收、失败记录与未决约定完整保留。源码最终验证后只有文档/状态整理，不再触发应用门禁。
- 最终结构核验：`python3 -` exit 0，JSON、全局唯一 ID、跨归档依赖、合法状态及全部根任务的进度索引一致；180 个本地链接/Markdown 锚点可解析，QAM-05 的 80/100、Gate/Final L2、五个开放 P2 与 resolved 009 一致，总览均分 86.2/开放 P2 28 算术正确，评分历史只追加。本轮中断恢复哈希对照仅九个授权范围内文件变化，无丢失或新增范围外文件，归档、next-env 及既有用户改动保持；Focus helper/原用例修正早于该恢复快照，搜索段之外的 E2E 内容与恢复快照一致。`git diff --check` exit 0，`git status --short` 已核对；报告/临时目录为空、开发类型引用存在，清理与重启路径均满足退出要求。
- 唯一推荐下一步：feat-064，核对 About 照片来源、干净环境交付与缺失回退验收；feat-065/066/068 保持独立未开始。

<a id="feat-068"></a>

## 2026-09-14 — feat-068：高开销开发浏览器诊断与资源压力复核

- 拆分依据：feat-067 的搜索阻塞处理已获用户授权；原登记中尚未确认的高开销配置诊断单独保留，原描述、三项验收及以下历史失败均完整迁入。本项为 not-started，依赖 feat-040。
- 来源：feat-063 的全程 trace + 软件 WebGL 诊断。原命令为 `DEBUG=pw:webserver E2E_APP_MODE=development E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run test:e2e -- --trace=on`；38.4 分钟时主动 SIGINT、exit 130，19 passed/5 failed/1 interrupted/16 did not run。Focus 原用例尚未运行，不能据此解释 feat-063。
- 失败摘要：匿名 About 的 `observed.errors` 包含 `Internal Next.js error: Router action dispatched before initialization.`；Chat 冷启动的 `observed.errors` 包含 `LifePanel` 世界时钟 SSR/浏览器文字 hydration mismatch；1280px 宿主表单、跨标签身份恢复、Agent 长生活字段用例超时，计划澄清用例被中断。同期 7.8GB 内存 available 366MB，2GB swap 用满，停止测试后 available 恢复到 3.6GB。
- 状态：独立 not-started，不改产品、不改默认配置与全局超时。尚未证明各现象共因或归因于单一 QAM；需在该项对照诊断配置、资源和产品状态。常规配置的后续结果只作为对照，不直接关闭本项。
- 常规对照：`DEBUG=pw:webserver E2E_APP_MODE=development ./scripts/run-node22.sh npm run test:e2e` 最终 31 passed/10 failed（24.5m，原 process 句柄在权限边界切换后失效，未回收退出码）。其中两项 Focus 归 feat-063；其余为跨标签身份、首页照片、计划键盘、档案隐私、发帖、聊天发送/快照和搜索。后半段 available 408MB、2GB swap 用满；About/Chat 冷启动、三个视口和长生活字段在此轮通过，不能把所有现象简单归因于 trace 或软件 WebGL。
- 额外证据：feat-067 产品修正前的 `NODE_OPTIONS=--max-old-space-size=4096 VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` exit 1、开发 Playwright 39 passed/3 failed；跨标签身份交互完成后，`observed.errors` 收到 `Performance.measure: ChatIndexPage cannot have a negative time stamp`。另两项无搜索请求已由 QAM-05-009 修复；性能计时错误仍独立保留。本轮最终常规开发 42/42 不证明高开销配置或该错误的全部触发条件已经修正，本项保持 not-started。

- 2026-09-19 用户授权：完成本项三项验收。归档依赖 feat-040 为 done，仅将本项设为 in-progress；未改应用超时、断言与默认配置。
- 启动基线：`VITEST_MAX_WORKERS=1 ./init.sh` exit 0，Prisma generate、资产校验、类型、lint、79 文件/685 项通过（本轮新增用例前）。仓库原有未提交改动保留；生产构建会把自动生成的 `next-env.d.ts` 类型引用从 `./.next/dev/types/*` 改回 `./.next/types/*`，收尾已用 `git checkout -- next-env.d.ts` 恢复到原开发引用。
- 诊断工件与边界：临时 `playwright.feat-068.config.ts` 与 `tests/e2e/feat-068-diagnostic.spec.ts` 只采集 `observed.errors`、console、导航耗时与 `localStorage` 读数，复用既有 `tests/e2e/support/app-mode.ts` 与真实隔离 PostgreSQL，不复制或改写既有用例、不注入 mock 成功响应、不放大超时。首轮误用带认证 storageState 访问匿名 About，`[diag:about] commit 2659ms status=200` 后用例失败；已把匿名与登录流程拆成独立 project 后重跑。
- 常规配置诊断（真实 Chromium，`E2E_APP_MODE=development ./scripts/run-node22.sh node node_modules/@playwright/test/cli.js test --config=playwright.feat-068.config.ts`）exit 0、5 passed（36.7s）：`[diag:about] loaded 3110ms authStatus=401`、settled 6435ms、`errors=[]`；6x 主线程降速的 `[diag:about-slow]` loaded 2180ms、settled 5677ms、`errors=[]`；`[diag:chat] ready 3021ms`、`errors=[]`。两项 About 探针的 console 只有 `/api/auth/me` 401 与 `/images/about/oo.jpg`、`/images/about/xx.jpg` 的 400（即 feat-064 的缺图），**没有** Router 初始化错误，也没有 `Performance.measure` 负时间戳。同期采样 used 4974→6216MB、available 2808→1567MB、swap 0→90MB。
- Chat 时钟 hydration 复现：同一轮 `[diag:clock]` 在固定客户端时钟下 `afterHydration=["13:05","13:05"]`，`errors` 为 `Hydration failed because the server rendered text didn't match the client.`，组件栈指向 `ChatApp` → `LifePanel` → `formatTime`；差异为 SSR 与客户端各自读取挂钟文字，`Date.now()`/本地化时间正是 React 官方列出的两类原因。该现象与资源压力无关，可确定性复现。
- 高开销配置对照：`E2E_APP_MODE=development E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh node node_modules/@playwright/test/cli.js test tests/e2e/agent-entry-public.spec.ts tests/e2e/agent-entry-authenticated.spec.ts --grep '匿名 about|跨标签退出卸载公开页入口' --trace=on` exit 0、3 passed（1.1m）。两个历史失败页面在全程 trace + 软件 WebGL 下通过，`observed.errors` 不再出现 About Router 初始化错误。采样窗口 10:05:46–10:06:58：峰值 used=6641MB、最低 available=1141MB、峰值 swapUsed=1317MB、load 最高 6.47；进程峰值 next-server RSS 1548MB、chrome 1879MB、playwright 2284MB。该配置把压力推高更快，但在隔离运行下不产生上述两个错误，故不是触发条件。
- 产品修正 QAM-03-007：`LifePanel` 在服务端渲染的客户端组件里以 `new Date()` 初始化 `now`，SSR 与 hydration 各读各的挂钟，跨分钟即文字不一致。改为 `components/chat/useWorldClock.ts` 的 `useSyncExternalStore` 挂钟 store：服务端快照恒为 0，SSR 与 hydration 渲染期都不读 `Date`，挂载后才切到浏览器读数并按 30 秒推进；`formatTime` 在读数未知时沿用既有 `--:--` 占位。中途尝试过由 `app/chat/[roomId]/page.tsx` 透传 `serverNow`，因仓库 lint 的 `Cannot call impure function during render` 与 `react-hooks/set-state-in-effect` 两条规则否决，相关透传已全部回退，未改 `ChatApp` 原型。
- 回归与负向对照：`./scripts/run-node22.sh npx vitest run --project component tests/component/life-panel-world-clock.test.tsx` 新增 2 项——首帧与宿主挂钟无关且两次渲染字节相同；`renderToString` 含 `--:--`、`hydrateRoot` 的 `onRecoverableError` 为空、随后显示 `21:05`、30 秒后 `21:06`。修复前同一用例 2 failed（SSR 直接渲染出宿主挂钟 `21:05`，形状与生产一致：`+21:05 / -18:05`）；把 `getServerWorldClockSnapshot` 临时改回读 `Date.now()` 的负向对照同样 2 failed。修复后真实浏览器诊断 `[diag:chat:errors] []`、`[diag:clock:errors] []`、`afterHydration=["13:14","13:14"]`，3 passed（27.1s）；既有 `resolves life panel identity for the second room participant` 定向 2/2（23.6s）。`components/chat/LifePanel.tsx` 与 `app/chat/[roomId]/page.tsx` 之外未改产品代码。
- 其余两个签名的归因（均不登记为产品缺陷）：`Internal Next.js error: Router action dispatched before initialization` 是 Next.js E668，`globalActionQueue` 为 null 时抛出，派发点与队列安装都在 `AppRouter` 生命周期内；在 6x 降速、`--trace=on`、软件 WebGL 隔离运行下均未复现，判为负载相关竞态而非本仓库产品缺陷。`Performance.measure: ChatIndexPage cannot have a negative time stamp` 是 React 开发期 User Timing：测量名带 `​` 前缀、本仓库零处 `performance.measure`/`performance.mark` 调用、`actualStartTime` 默认哨兵为 `-1.1`，`react-dom-client.development.js` 的 16916/16953 两处在 Offscreen hydration 错误路径上未套用 16801 处已有的 `0 < actualStartTime` 防护即传入 `performance.measure`；判为上游开发期插桩噪声。
- 共同条件结论：三个现象的共同条件是开发模式的持续资源耗尽（available 低于约 600MB、2GB swap 用满、`next-server` RSS 约 2.7GB），不是 trace 或软件 WebGL 配置本身；Chat 时钟 hydration 另有独立的、与资源无关的产品根因，已单独修正。诊断工件（`playwright.feat-068.config.ts`、`tests/e2e/feat-068-diagnostic.spec.ts`）在取证后已删除。
- 完整开发门禁首轮 `VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` exit 1：标准门禁 80 文件/687 项、生产构建、覆盖率 51.58/45.39/56.06/52.25 通过，真实 PostgreSQL 29 文件中 1 failed/28 passed、110 项中 1 failed/109 passed。失败为 `tests/integration/room-snapshot-privacy.integration.test.ts` 的参与者顺序 `[partner, viewer]` 与期望 `[viewer, partner]` 不符。`check:full` 以 `&&` 串联，集成阶段失败后 Playwright 未运行，该轮 EXIT=1 不能记作浏览器结论。
- 范围外夹具缺陷修正：`lib/room-snapshot.ts` 的 `orderBy: { joinedAt: "asc" }` 本身正确，但夹具用一条 `createMany` 插入两行，`joinedAt @default(now())` 取事务时间戳使两值完全相同，并列顺序由数据库决定，故断言随机失败。全仓库只有该用例断言参与者顺序（5 处 `roomParticipant.createMany` 中仅此一处）。已显式给出可区分的 `joinedAt`（`00:00:00` / `00:00:01`）使排序成为全序；`npm run test:integration -- tests/integration/room-snapshot-privacy.integration.test.ts` exit 0、1/1（5.96s）。未修改 `lib/room-snapshot.ts`。
- 完整开发门禁第二轮 `VITEST_MAX_WORKERS=1 E2E_APP_MODE=development ./scripts/run-node22.sh npm run check:full` exit 1：标准门禁 80/687、生产构建、覆盖率同上通过；真实 PostgreSQL 29 文件/110 项全部通过；开发 Playwright 41 passed/1 failed（6.1m），唯一失败为末项 `同毫秒文章在首页与搜索保持稳定顺序，真实 API 分页无遗漏`，`page.waitForResponse` 在 90000ms（原超时，未放大）超时，失败点 `tests/e2e/authenticated.spec.ts:1423`。响应窗口采样显示 used 7.1–7.25GB、available 533–642MB、swapUsed 2045–2048MB（用满）、`next-server` RSS 2709–2798MB，与 feat-063 记录的历史条件（7.8GB / 366MB / swap 用满）同型。
- 资源归因的负向对照：同一用例单独运行 `... test tests/e2e/authenticated.spec.ts --grep '同毫秒文章在首页与搜索保持稳定顺序，真实 API 分页无遗漏'` exit 0、2/2（24.8s，含 setup），即约 9.5s 的用例在资源耗尽下放大到超过 90s。据此判为宿主机容量边界，而非搜索产品缺陷或测试同步缺陷；未修改超时、未跳过、未放宽断言。该轮的标准门禁与集成层均通过，标准门禁 `VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run check` 单独复跑 exit 0、80 文件/687 项、`✓ Compiled successfully in 4.4s`、覆盖率 51.58/45.39/56.06/52.25；`resolves life panel identity for the second room participant` 复跑 2/2（19.8s）。
- 质量结论：[QAM-03 审查报告](docs/optimization/qam-03-life-plan-quality-review.md) 新增 `QAM-03-007`（P2，resolved，2026-09-19，责任边界 QAM-03 LifePanel 挂钟 SSR/hydration），QAM-03 由 93 升至 94/L4，健壮性维度 12→13，评分历史只追加；[模块总览](docs/optimization/module-quality-overview.md) 同步均分 86.2→86.3、开放 QAM P2 维持 28。
- 工件清理：已删除 `playwright.feat-068.config.ts`、`tests/e2e/feat-068-diagnostic.spec.ts`、`test-results/`、`playwright-report`、`coverage`、`/tmp/feat-068-*` 与两份源码备份；后台资源采样进程（pid 11131、14683）已终止，无残留浏览器进程。`docker ps -a` 只剩原有健康的 `xoxo-meridian-postgres`，卷只剩 `xoxo-meridian_postgres-data`，无本轮 Testcontainers 网络/卷/容器残留。`git diff --check` exit 0，`git status --short` 已核对。本轮未提交或推送。

<a id="feat-064"></a>

## 2026-09-13 — feat-064：About 照片缺失回退与交付契约

- 状态：done（2026-09-19 完成，无前置依赖）。原始登记与来源证据保留如下：feat-057 生产完整门禁虽为 37/37，仍输出 `/images/about/oo.jpg`、`/images/about/xx.jpg` 的 invalid-image；两文件在当前环境不存在，也未被 Git 跟踪，均命中 `.gitignore:33` 的 `images/` 规则，Agent 入口用例通过不证明照片有效（原始命令见 [feat-057](#feat-057)）。
- 2026-09-19 交付分支选择：两张是真实用户肖像，没有可授权提交的替代资产，且 `images/` 忽略规则决定照片只存在于部署环境，因此按第一项验收选择“明确的缺失回退”，仓库不新增任何照片二进制；交付方自行放入 `public/images/about/{oo,xx}.jpg`。
- 实现：`lib/about-photos.ts` 按请求判定交付目录内的文件（`server-only`，只接受 `/images/about/` 下的路径，拒绝目录自身与 `..` 越界）；`app/about/page.tsx` 收敛为薄服务端组件并加 `export const dynamic = "force-dynamic"`，路由边界与页面内容不变；照片渲染与回退下沉到 `components/about/AboutView.tsx`（客户端），原先位于 `app/about/right-now-content.ts` 的内容逐字移到 `components/about/right-now-content.ts`，因此上面的路径硬编码描述仍成立。
- 展示契约：照片缺失时渲染 `role="img"` 且沿用同一 `aria-label` 的内置插画（装饰 SVG `aria-hidden`、姓名首字母、`photo coming soon`），布局尺寸不变，**不产生 `<img>` 元素，因而不向不存在的路径发起请求**；有效资源正常走 `next/image`。可用性在服务端按请求判定，所以干净工作树不再出现 feat-057 的 invalid image 400。
- 交付边界与文档更正：`next start` 只在启动时快照 `public/` 目录，运行期新增或替换照片需要重启进程（无需重新构建）才生效，删除照片则因按请求判定而立即回退。原设想的“运行期补齐照片即生效”在 production 下不成立（下一节的 phase 2 因此删除），README 第 6 节与启动器注释已按实际行为更正。
- 测试：`tests/lib/about-photos.test.ts` 6 项（已交付返回路径、缺失返回 null 并保留 alt、目录非文件、目录外路径、`/images/about/../login-bg.jpg` 越界、两张照片互不影响）；`tests/component/about-photos.test.tsx` 2 项（已交付渲染指向交付路径且无回退文案；缺失渲染回退且 DOM 中零 `<img>`）；`tests/lib/about-right-now-content.test.ts` 仅改导入路径，资产契约正则保留。
- 负向对照（先失败后修复）：把 `resolveAboutPhoto` 临时改回“始终返回交付路径”，`npm run test:unit -- tests/lib/about-photos.test.ts` exit 1、5 failed/1 passed；把 `PersonPhoto` 临时改回“始终渲染照片”，`npm run test:component -- tests/component/about-photos.test.tsx` exit 1、1 failed/1 passed（回退文案与零 img 断言失败）。两处源码随后按备份哈希复原（`lib/about-photos.ts` `ca40b947…`、`AboutView.tsx` `95b0b322…`）。
- production 浏览器负向对照：同一“始终返回交付路径”的临时改动下，`E2E_APP_MODE=production ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/public.spec.ts --grep 'About 照片按交付状态加载或回退'` exit 1，页面真实渲染 `/_next/image?url=%2Fimages%2Fabout%2Fxx.jpg&w=3840&q=75`，用例在 `expectFallbackPortrait`（`tests/e2e/public.spec.ts:25`）失败，证明该断言确实拦住了修复前的缺图行为。
- E2E 夹具：`tests/e2e/start-test-app.ts` 在被测应用启动前暂存第一张免费的照片（不覆盖交付方资源），并把交付状态打印到 **stderr**（Playwright 的 webServer 只转发子进程 stderr；同文件既有的 `console.log("[e2e-app] mode=…")` 因此一直不可见，本轮未改动它，仅记录）；`tests/e2e/support/about-photos.ts` 按夹具字节识别暂存文件，真实照片不读取、不覆盖、不删除；用例内在托管模式下断言至少交付一张，避免“两张都缺失”时静默失去已交付分支覆盖。
- 定向 production 验收：`E2E_APP_MODE=production ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/public.spec.ts --grep 'About 照片按交付状态加载或回退'` exit 0、2 passed/44.6s（含 setup），WebServer 打印 `[e2e-app] About 照片交付状态：已交付 /images/about/oo.jpg；缺失 /images/about/xx.jpg。`，即同一次启动同时覆盖“已交付经优化器真实解码（`naturalWidth > 0`）”与“缺失渲染回退且零请求”，且无任何 invalid image 输出。用例分两阶段：按启动时的交付状态逐张断言并核对实际请求集合恰好等于已交付路径；随后只清除 E2E 自己暂存的照片，验证回退立即恢复且不再请求该路径。
- 完整门禁：`VITEST_MAX_WORKERS=1 ./init.sh` exit 0（82 文件/695 项）；`VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run check` exit 0（82 文件/695 项、`✓ Compiled successfully`、覆盖率 51.67/45.53/56.18/52.34）；`E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run check:full` exit 0（82 文件/695 项、覆盖率 51.72/45.57/56.18/52.34、真实 PostgreSQL 29 文件/110 项、production Playwright 43 passed/3.8m，其中 About 照片用例 ✓）。用例数较上轮 80/687 增加 2 文件/8 项，全部为本轮新增。
- 收尾：`git checkout -- next-env.d.ts` 恢复生产构建改写的开发类型引用；`test-results/`、`playwright-report/`、`coverage/` 与 `/tmp` 下的备份和日志已清理；`public/images/about/` 无残留夹具；Docker 只剩原有 `xoxo-meridian-postgres`，Ryuk 与 Testcontainers 卷/网络已自动移除；`git diff --check` exit 0，既有未提交改动保留，本轮未提交或推送。
- Harness 结构核验：`./scripts/run-node22.sh node /home/dadalv/.agents/skills/harness-creator/scripts/validate-harness.mjs --target .` exit 0、100/100（移交文件此前缺少模板要求的 `Files Changed`、`Blockers`、`Next Session` 结构，本轮按模板补齐后该项由 FAIL 转 PASS）；feature_list.json 解析通过、`featuresNumber` 与数组长度一致且 ID 唯一，本地链接与任务锚点核验只余两处既有问题（README 第 15 节指向不存在的 `docs/deploy-nginx.md`、progress 中以 `#L1280` 标注源码行号的引用），两者均先于本轮存在且未修改。

<a id="feat-061"></a>

## 2026-09-13 — feat-061：计划澄清与执行事实一致性

- 范围与依赖：归档 `feat-019` 为 done，仅将 feat-061 标为 in-progress。使用 `xoxo-qam-08-agent-runtime-review` 核对报告标准、QAM-08 和跨模块边界；旧 `docs/optimization/agent-runtime-review.md` 当前不存在，按既有报告说明保留现状，不恢复历史删除。
- 启动基线：`VITEST_MAX_WORKERS=1 ./init.sh` exit 0，Node 22.23.2/npm 10.9.8、Prisma generate、资产校验、类型、lint、79 文件/679 项通过。沿用上轮单 worker 和 production 浏览器配置；feat-065/066 的默认并发问题不扩入本项。
- 修复前 Node：`VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run test:unit -- tests/agent/plan-repair.test.ts tests/agent/task-claim.test.ts` exit 1，2 failed/11 passed；纯澄清输出和真实 Runtime 编排均收到“之前那个已经帮你取消了。要不要再新安排一次？告诉我具体什么时候发，我马上排上。”，不满足澄清断言。Runtime 已确认 Planner 调用两次、fallback 原因为 `one_shot_promise_without_create` 且零 Tool 执行。
- 修复前 PostgreSQL：`VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run test:integration -- tests/integration/agent-plan-validation.integration.test.ts` exit 1，1 failed/12 passed。有效 Job 的整行、ToolCall=[]、tool Step=[] 和 fallback 事件先通过断言，最终 Message 却仍确认旧任务已取消，直接复现数据库事实与回复冲突；使用真实 PostgreSQL Testcontainer，未访问开发库。
- 改动：`agent/plan-repair.ts` 的缺少一次性创建分支改为“请再确认这次一次性任务的具体日期、时间和时区。”，并注明 Tool loop 前不能从已丢弃计划推断写入完成。Scheduler、Tool 集合、执行顺序和后处理机制保持既有实现。
- 回归：Node 覆盖重复无效 Planner、无取消/创建/更新完成声明、合法 runOnce 自动修复和普通零 Tool 对话；PostgreSQL 覆盖原 Job 整行不变、零 ToolCall/Step、fallback 和单条真实 Message。Playwright 使用仅绑定 loopback 的确定性 LLM HTTP 服务驱动真实 Provider/Runtime/数据库，检查实时澄清、有效计划卡片和刷新一致性，结束关闭服务并删除隔离房间。
- 修复后定向 Node：`VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run test:unit -- tests/agent/plan-repair.test.ts tests/agent/task-claim.test.ts tests/agent/plan-validator.test.ts` exit 0，3 文件/22 项通过。
- 最终完整门禁：`VITEST_MAX_WORKERS=1 E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run check:full` 单次 exit 0，79 文件/682 项 Node/组件、生产构建、覆盖率 statements 51.58%、branches 45.39%、functions 56.06%、lines 52.25%，29 文件/110 项真实 PostgreSQL（含计划校验 13/13）、41/41 production Playwright（4.8 分钟）。本次计划澄清浏览器用例 3.2 秒通过，实时/刷新保持原计划有效；合法计划修复、普通零 Tool 对话、Durable Step、审批/预算与请求者/Memory 回归均保持。
- 收尾基线：仅恢复构建生成的 `next-env.d.ts` 两处类型路径后，`VITEST_MAX_WORKERS=1 ./init.sh` exit 0，Prisma generate、资产、类型、lint、79 文件/682 项通过。
- 报告与状态：QAM-08-007 提升为 E3 并标为 resolved，数据流与状态一致性 +1，QAM-08 82→83，Score L3、Gate/Final L2；其他四项 P2 仍开放。按统一报告标准同步总览的 QAM-08 行、组合均分/问题数及历史，不改其他模块结论；feat-061 标为 done，计数字段与根数组一致且未超过归档阈值，直接跳过归档，重写相邻会话交接。
- 验证边界：未另跑默认 worker、默认开发模式完整门禁、`npm run test:compose-smoke`、`npm run dev` 健康端点、Compose 镜像构建/重启、生日主题浏览器矩阵或实体设备；本项未修改这些接缝。沿用已验证的单 worker/production 配置，不关闭 feat-063/065/066；浏览器仍有 8 条既有 About 缺图诊断，继续由 feat-064 独立处理。
- 清理与结构核验：测试报告、临时验证日志/快照与草稿已清理，恢复不依赖临时文件；本轮 PostgreSQL/Ryuk 容器及 E2E 临时卷已释放，未创建独立镜像/网络，既有两个数据库容器保持。JSON、全局 ID/依赖、计数字段、报告十维合计/问题表/本地引用通过；`git diff --check` exit 0，`git status --short` 已核对，既有用户改动保留。
- 唯一下一步：开始 feat-063，先核对根验收和归档 feat-035，再复核开发模式 Study 浏览器超时与用例残留，不提前扩入其他验证问题。

<a id="feat-060"></a>

## 2026-09-13 — feat-060：Study 本地日历与午夜边界

- 范围与前置：按 priorityRank 选择 feat-060，核对归档 feat-035 为 done，只将本项改为 in-progress；使用 QAM-07 复核技能核验定义与证据，用户的修复请求同时授权业务、报告和状态改动。已读本地 Next.js ORM/数据读取指南。未修改 Focus 状态机、Goal 排序、schema/迁移、依赖或部署配置。
- 根因：本地日历日期与绝对时长是两种不同的量。原 streak 从 now 用服务器 `Date.setDate` 递减，秋季重复计日、春季跳过日期；成员查询把当前 offset 拼到午夜，DST 当天偏移一小时，且缺少次日排除边界。
- 修正：`lib/study-calendar.ts` 抽离不依赖 Prisma/服务的纯日历统计，保留 `lib/study.ts` 原统计导出兼容调用方；streak 在目标时区日期键上用 UTC 日历递减。`getStudyDateWindow` 使用复用的 Intl formatter，在有界时间区间内按日期键定位该本地日期出现的第一毫秒，求本日、次日及八天回看的完整日期起点；成员查询使用 `[todayStartUtc, tomorrowStartUtc)`。继续按 `startedAt` 本地日期、周日周界和 completed focus 统计；本人及伙伴统一使用查看者传入的时区，保留原有近期窗口，不扩大为无限历史 streak。
- 启动权限复核：沙箱 `./init.sh` exit 1，78 文件中 2 failed/76 passed、4 failed/649 passed；`agent-entry-build-config` 三项和 `scheduled-job-one-shot` 一项因子进程空 stdout 得到 `SyntaxError: Unexpected end of JSON input`。依 AGENTS.md 在正常权限边界以同一 Node 22 重跑原命令 exit 0：78 文件/653 项、Prisma generate、类型和 lint 全通过。沙箱 `docker ps --format '{{.Names}}'` 原始结果为 socket `permission denied`，获准正常边界原命令 exit 0，仅有既有 `xoxo-meridian-postgres`。
- Node 修复前：`./scripts/run-node22.sh npm run test:unit -- tests/lib/study.test.ts` exit 1，7 passed/5 failed（12 项）；纽约/伦敦回退日两天记录得到 streakDays=3，春季后周一午夜应连续三天却为两天，UTC 子进程亦复现。新增固定场景覆盖纽约/伦敦春秋切换、上海普通周日、跨午夜 startedAt 归属、周日周界、休息/取消过滤和今天缺记录归零；另加入报告原始的纽约两条记录场景，以及 UTC/洛杉矶/上海三个独立进程 TZ 对照。
- PostgreSQL 修复前：`./scripts/run-node22.sh npm run test:integration -- tests/integration/study-calendar.integration.test.ts` 前两次均 exit 1、0/5，分别为新夹具误写 `prisma.profile.createMany` 和缺少 `UserProfile.city/country`，属于测试实现错误，已修正，不计作产品失败证据。第三次同命令 exit 1、0/5：春季本人/伙伴应为 60 分钟却为 70，秋季为 0；插入下一天边界记录后上海错误计为 105 而非 60，并复现 streak 春秋错误。
- PostgreSQL 修复后：`./scripts/run-node22.sh npm run test:integration -- tests/integration/study-calendar.integration.test.ts tests/integration/study-focus-transitions.integration.test.ts` exit 0、2 文件/15 项。补充八天回看完整日期后，`TZ=UTC ./scripts/run-node22.sh npm run test:integration -- tests/integration/study-calendar.integration.test.ts tests/integration/study-focus-transitions.integration.test.ts` exit 0、2 文件/16 项；除 DST/午夜、下一日排除、非成员隔离与查看者时区外，跨回退周的窗口首日也完整计入，原有 Focus 事务、并发、重放、故障回滚和到期恢复 10 项全部通过。
- 组件与类型修正：`./scripts/run-node22.sh npm run test:component -- tests/component/study-calendar-stats.test.tsx` 首次 exit 1、0/1，初始概览仍处于 opacity=0 的入场动画；改为等待可见状态。`./scripts/run-node22.sh npm run typecheck` 首次 exit 2、TS2769，Testing Library 的 getByRole 不支持 `exact` 参数，已移除。修复后定向 `./scripts/run-node22.sh npm test -- tests/lib/study.test.ts tests/lib/study-productization.test.ts tests/lib/study-transitions.test.ts tests/server/study-api.test.ts tests/server/study-product-api.test.ts tests/server/study-dashboard.test.ts tests/server/study-page-productized.test.ts tests/server/chat-left-rail-study-status.test.ts tests/component/study-calendar-stats.test.tsx` 为 8 文件/64 项 Node 通过、1 项组件失败：刷新后的开始按钮仍处于入场动画；同样改为等待可见状态，组件原命令最终 exit 0、1/1。MSW 只替代 HTTP 边界，验证初始概览/伙伴均 1h，停止成功经实际 GET 刷新后均 45m，连续天数也同步；不冒充真实数据库或浏览器时钟证据。
- 午夜重复的补充证据：直接运行 Node 22 的 `CronDate(new Date("2026-11-02T04:30:00Z"), "America/Havana").setStartOfDay()` 探针得到 `2026-11-01T05:00:00Z`；Intl 对照证明 `04:00Z` 已是本地 Nov 1 的第一个 00:00，`05:00Z` 是第二个。最终以本地日期首次出现的时刻为日界，补哈瓦那春季跳过午夜和秋季重复午夜，避免把复用库本身当作正确性的证据。未新增依赖。
- 最终定向：`./scripts/run-node22.sh npm test -- tests/lib/study.test.ts tests/component/study-calendar-stats.test.tsx` exit 0、2 文件/27 项；`TZ=UTC ./scripts/run-node22.sh npm run test:integration -- tests/integration/study-calendar.integration.test.ts tests/integration/study-focus-transitions.integration.test.ts` exit 0、2 文件/18 项（8 项日历读取、10 项 Focus）；类型检查原命令最终 exit 0。
- 完整门禁前两次：相同 `E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run check:full` 均 exit 1，停在 quick，分别为 79 文件中 1 failed/78 passed、1 failed/665 passed。首次唯一失败是既有首页搜索组件的 `IntersectionObserver is not defined`（`lib/useScrollReveal.ts:15`）；未修改其源码，`./scripts/run-node22.sh npm run test:component -- tests/component/home-timeline-board-search.test.tsx tests/component/study-calendar-stats.test.tsx` 对照 exit 0、2/16。该门禁不稳定现象独立登记 feat-065，不推断为产品缺陷、不合并修复；根列表计数字段随新增项同步。第二次唯一失败是新增跨进程时区测试 `Test timed out in 5000ms`：同一测试串行启动三次子进程并加载整个 Study 服务依赖。最终把纯日期逻辑抽离成独立模块、每个 TZ 单独用例，只加载被测纯模块；保留默认超时。
- 第三次完整门禁：同一原 production 命令 exit 1，quick 为 2 failed/77 passed 文件、2 failed/677 passed 项；本次 Study 新增场景全部通过。两项既有上传 Route 测试 `tests/server/atlas-storage-routes.test.ts:78` 与 `tests/server/home-board-routes.test.ts:76` 均 `Test timed out in 5000ms`；未修改两文件，`./scripts/run-node22.sh npm run test:unit -- tests/server/atlas-storage-routes.test.ts tests/server/home-board-routes.test.ts` 定向 exit 0、2/14、396ms。独立登记 feat-066；当前环境报告 32 CPU，锁定 Vitest 源码确认支持 `VITEST_MAX_WORKERS`，但定向成功不证明默认并发故障已解决。
- 第四次完整门禁：`VITEST_MAX_WORKERS=4 E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run check:full` exit 1。快速测试和覆盖率测试均 79 文件/679 项通过，生产构建通过，覆盖率 50.50/45.07/55.09/51.07；集成为 2 passed/27 failed 文件、5 passed/104 failed 项，出现 `Unique constraint failed (slug)`、PostgreSQL `40P01 deadlock detected` 等。核对锁定 Vitest 的解析顺序后确认环境变量覆盖了 integration 的 `maxWorkers: 1`，让原本共享测试库的文件并发执行、互相 reset。这是本轮验证配置错误，不是产品数据库回归；该轮不作为集成通过证据，未修改业务或仓库验证配置。
- 最终完整门禁：`VITEST_MAX_WORKERS=1 E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run check:full` 单次 exit 0：79 文件/679 项 Node/组件、Next.js 16.3.3 生产构建、覆盖率 statements/branches/functions/lines 为 50.50/45.07/55.09/51.07、29 文件/109 项真实 PostgreSQL、40/40 production Playwright（4.1 分钟，无 skip）。使用单 worker 保持集成串行隔离，保留全部检查和默认单项超时；默认并发问题由 feat-065/066 跟踪，默认开发模式 Study 序列仍由 feat-063 跟踪。
- 浏览器与诊断：既有 Focus 启动/停止（7.0s）、离开后到期重访/刷新（2.8s）及 Study 私密载荷边界均通过。本轮没有对浏览器/服务端时钟模拟 DST，日期边界由固定时间 Node/真实 PostgreSQL 及组件读取呈现组合验收。About 仍输出六条既有 `The requested resource isn't a valid image`（feat-064）；该非阻断诊断不证明照片正常加载。
- 启动收尾：生产构建只将 next-env.d.ts 的两处 `.next/dev/types/` 引用改为 `.next/types/`；核对旧类型文件仍存在后仅恢复本轮生成差异。`VITEST_MAX_WORKERS=1 ./init.sh` exit 0：Prisma generate、类型、lint、79 文件/679 项全部通过，当前工作树可直接建立启动基线。
- 未运行边界：默认开发模式 `./scripts/run-node22.sh npm run check:full`、`npm run test:compose-smoke`、`npm run dev` 健康端点、Docker 镜像构建、生日主题矩阵或实体设备未另跑；本轮未改对应部署、启动或资产接缝，不声明这些范围通过。
- 质量：[QAM-07 报告](docs/optimization/qam-07-study-quality-review.md) 关闭 003；数据一致性和健壮性各 +1，85→87，Score L3、Gate/Final L2，Goal 排序 004 保持开放；[模块总览](docs/optimization/module-quality-overview.md) 同步均分 86.1、开放 QAM P2 为 29。新增的 feat-065/066 只登记验证稳定性现象，不合并实现或推断新的 QAM 产品缺陷。
- 清理：`rm -r -- coverage test-results playwright-report` exit 0；E2E 临时数据库/服务已关闭，Docker 检查没有本轮 Testcontainers 容器或标签卷残留，已有 XOXO PostgreSQL 以及本轮未创建的另一 baseline Compose PostgreSQL/network 均保留。未构建测试镜像；本轮日志、类型快照与文件摘要在记录证据后移除，不作为恢复依赖。
- 收尾核验：JSON、全量 ID 唯一性、跨归档依赖、状态、评分算术、Markdown 链接/行号与 diff/status 核验通过；根列表计数字段与新增独立任务同步，不超过 40，直接跳过归档。范围外既有文件 SHA-256 保持，三份状态记录同步并重写交接。
- 唯一下一步：开始 feat-061（QAM-08-007，计划澄清回复误称旧任务已取消），启动前只提取归档 feat-019 核对依赖。

<a id="feat-065"></a>

## 2026-09-19 — feat-065：首页搜索组件门禁的 IntersectionObserver 生命周期复核

- 结论：首轮失败是**组件门禁的测试隔离缺陷**，不是产品行为或浏览器能力缺陷。浏览器恒定义 `IntersectionObserver`，`useScrollReveal` 的效果体只在客户端运行；失败只出现在共享收尾的 hook 顺序上。因此产品源码零改动，也不追加浏览器门禁（验收条件只在确认产品行为根因时才要求追加风险匹配的浏览器验证）。
- 首轮证据（保留对照）：feat-060 首次 production 完整命令在 quick 阶段 exit 1，`tests/component/home-timeline-board-search.test.tsx` 的首个搜索响应场景抛 `ReferenceError: IntersectionObserver is not defined`，栈指向 `lib/useScrollReveal.ts:15`；同轮定向 `./scripts/run-node22.sh npm run test:component -- tests/component/home-timeline-board-search.test.tsx tests/component/study-calendar-stats.test.tsx` exit 0、2 文件/16 项，即“全量失败、定向通过”的对照。当时未修改任何文件。
- 根因证据链（本轮）：
  1. 失败栈是 React 19 的 `commitHookEffectListUnmount ← commitPassiveUnmountEffectsInsideOfDeletedTree_begin ← recursivelyTraversePassiveUnmountEffects ← flushPassiveEffects`，即**卸载路径冲刷待执行被动效果**时构造观察者，不是用例断言内部失败。
  2. Vitest 4 默认 `sequence.hooks` 为 `"stack"`（打包产物内 `resolved.sequence.hooks ??= "stack"`），`afterEach` 按注册逆序执行，所以**测试文件级 hook 先于 setup 文件 hook**；`globals: false` 下 RTL 也不会自动注册 cleanup，卸载只由 `tests/setup/component.ts` 负责。
  3. 临时插桩取得确定性顺序：`[diag] file afterEach 开始 io=function` → `[diag] file afterEach unstub 后 io=undefined` → `[diag] setup afterEach 开始 io=undefined` → `[diag] setup afterEach cleanup 后 io=undefined`。可见文件级 `vi.unstubAllGlobals()` 在组件仍挂载时就恢复了替身，共享 `cleanup()` 随后卸载并冲刷 `HomeTimelineBoard` 搜索响应替换卡片所触发的 `useScrollReveal` 效果。插桩只改测试文件、已按备份哈希复原（`sha256sum -c` 两处 OK、`git diff` 无差异）。
- 对照边界（如实记录未复现）：本轮默认并发全量 3 次空载（82 文件/695 项）与 1 次 16 核满载均 exit 0，说明默认并发不是稳定复现条件；据此以确定性顺序证据 + 可判别回归对照为准，不把未复现当作反证，也不用定向通过来“消除”原始失败。
- 修复边界：`tests/setup/component.ts` 的共享 `afterEach` 固定为 `cleanup(); vi.unstubAllGlobals(); mockServer.resetHandlers();`，即**组件卸载完成后才恢复全局替身**；删除 `home-timeline-board-search`、`timeline-order`、`about-photos`、`auth-panel-login-visuals` 四个组件测试文件中的文件级 `vi.unstubAllGlobals()`（其中前两个各自的 `vi.restoreAllMocks()` / `vi.useRealTimers()` 保留），并按需清理导入；`docs/testing-standards.md` 增补该不变式。未使用 `skip`、未放宽超时、未用源码扫描替代行为验证。node 项目的 `tests/agent/plan-contract.test.ts` 不涉及 RTL 卸载，其文件级恢复保持不变。
- 回归对照（新增 `tests/component/unmount-global-stub.test.tsx`，1 文件/2 项）：用例自装 `IntersectionObserver` 替身并渲染“卸载期间需要替身”的探针，第二个用例断言共享收尾已把全局恢复为原始值。修复前 `exit 1`：`AssertionError: expected [Function StubIntersectionObserver] to be undefined`；把旧式文件级 `vi.unstubAllGlobals()` 加回探针文件时另复现出与原失败完全相同的 `ReferenceError: IntersectionObserver is not defined`（栈同样落在 `commitHookEffectListUnmount ← flushPassiveEffects`），证明对照可判别失败签名。
- 修复后验证：定向 5 文件/25 项 exit 0；组件项目 3 次连跑均 22 文件/108 项 exit 0；默认并发满载（14 个自旋进程/32 核）2 次中 1 次 exit 0、另 1 次 7 项失败全部是 `Test timed out in 5000ms`（宿主机容量边界，日志零 `IntersectionObserver` 签名），搜索组件文件在该轮仍通过；最终 `VITEST_MAX_WORKERS=1 ./scripts/run-node22.sh npm run check` exit 0：**83 文件/697 项**、`✓ Compiled successfully`、覆盖率 51.67/45.53/56.18/52.34（与上轮同值）。`npm run build` 会把 `next-env.d.ts` 的 `./.next/dev/types/*` 改写为 `./.next/types/*`，已用 `git checkout -- next-env.d.ts` 还原。收尾 `VITEST_MAX_WORKERS=1 ./init.sh` exit 0：83 文件/697 项（上轮基线 82 文件/695 项，本轮新增 1 文件/2 项）。

<a id="feat-066"></a>

## 2026-09-19 — feat-066：全量 Vitest 默认并发下的上传 Route 导入超时

- 结论：默认并发下的 `Test timed out in 5000ms` 是**宿主容量边界**在验证资源配置上的暴露，不是上传 Route、被 mock 的服务、断言或浏览器能力缺陷。修复落在验证资源配置边界（`vitest.config.ts` 的 worker 预算）；产品源码、断言与默认 5000ms 单项超时均未改动，未使用 skip、未放宽全局超时、未省略构建或覆盖率。
- 原始失败（保留，来源见 [feat-060](#feat-060)）：feat-060 第三次默认 worker production 完整命令在 quick 阶段 exit 1，`tests/server/atlas-storage-routes.test.ts:78` 与 `tests/server/home-board-routes.test.ts:76` 均 `Test timed out in 5000ms`，其余 677 项通过；两文件当时未修改，定向 `npm run test:unit` 为 2 文件/14 项、396ms。
- 结构证据（区分动态导入/资源争用与实际服务故障）：`tests/server` 25 个文件中**只有这两个**在测试体内 `await import("@/app/api/...")`，因此只有它们在 5000ms 窗口内支付 Route 模块图（`next/server`、zod 等）的首次 transform/import 成本；另外 8 个导入 Route 的文件都在模块顶层静态导入，成本落在收集期。单 worker 下这两项首项为 177ms / 112ms、同文件其余用例 0–25ms——导入离 5000ms 有约 30 倍余量；两文件的 Prisma 与存储均被 mock，真实数据库与服务未参与，排除服务故障。
- 复现与对照（本机 32 CPU / 7.8GB WSL2；每次运行采样 `/proc/meminfo` 与 vitest 进程 RSS）：
  1. 定向（默认并发，仅这两个文件）：exit 0、2 文件/14 项、354ms。
  2. 全量默认并发（`maxWorkers = cpus-1 = 31`）5 次：1 次 exit 1（`home-board-routes` 首项，指向 `import` 行）、4 次 exit 0；峰值 vitest RSS 5.0–5.2GB / 33 进程，宿主 minAvail 1.68–1.84GB。即单次通过不构成根因证据，与 feat-060 记录的“全量失败、定向通过”一致。
  3. 受控 worker 数（全量）：16 → 峰值 3.0–3.3GB / minAvail 2.8–3.1GB、2/2 exit 0；8 → 1.8GB / 3.9GB、2/2 exit 0；4 → 1.1GB / 4.4GB、1/1 exit 0。全量耗时 31→7.3–11.9s、16→6.4–6.9s、8→8.8–10.4s、4→15.3s。
  4. 受控压力 A/B（后台常驻持有 2048MB，复现真实门禁运行时的环境占用）：**未修改的默认并发 3/3 exit 1**（16/3/1 项失败，除这两项上传用例外还有 GLB 校验、构建配置、备忘录等跨模块用例；minAvail 287–741MB、swap 2048MB 用满、RSS ~5.0GB/33 进程）；**配置上限 8 后 3/3 exit 0（84 文件/699 项）**，minAvail 1.97–2.32GB、RSS 1.8GB/10 进程。压力条件对两臂完全相同。
- 修正边界：`vitest.config.ts` 默认导出新增 `maxWorkers = Math.max(1, Math.min(8, os.availableParallelism() - 1))`，把“最多 8 个 worker，且不超过宿主可用并行度减一”声明为受支持配置。它只放在默认导出、不进 `sharedVitestConfig`，因此 `vitest.integration.config.ts`（只合并 `sharedVitestConfig`，并显式 `maxWorkers: 1`）完全不受影响，feat-060 记录的集成串行约定保持；环境变量 `VITEST_MAX_WORKERS` 仍会覆盖配置（含集成配置），作为禁项写入测试标准。
- 回归测试：新增 `tests/lib/vitest-worker-budget.test.ts`（node 项目 2 项），加载两份真实配置模块、断言解析后的 worker 预算（1 ≤ maxWorkers ≤ 8 且 ≤ 可用并行度减一；集成配置恒为 1），不读取源码文本。负向对照：临时删除 `maxWorkers: testMaxWorkers,` 行后 `npm run test:unit -- tests/lib/vitest-worker-budget.test.ts` exit 1（`AssertionError: expected 0 to be greater than or equal to 1`，1 failed/1 passed），按备份恢复后 exit 0、2/2。
- 门禁（受支持配置，不带任何 `VITEST_MAX_WORKERS`）：`./scripts/run-node22.sh npm run check` 连续两次 exit 0，均 84 文件/699 项、`✓ Compiled successfully`（4.9s / 5.1s）、覆盖率 51.72/45.57/56.18/52.34（门槛 35/45/40/40）；`./init.sh` exit 0、84 文件/699 项。`npm run build` 照例把 `next-env.d.ts` 的 `./.next/dev/types/*` 改写为 `./.next/types/*`，已还原。
- 风险匹配验证（本次改了 Vitest 运行配置，故补集成层）：`./scripts/run-node22.sh npm run test:integration` exit 0、**29 文件/110 项**、105.09s，真实 PostgreSQL 16 Testcontainer 与迁移正常，**无 skip**。该配置只合并 `sharedVitestConfig` 并自带 `maxWorkers: 1`，本轮实测仍为单 worker 串行（集成进程仅 1 个 fork），证明新增的默认导出 `maxWorkers` 没有渗入集成配置、feat-060 的串行隔离保持。未跑 E2E（见未运行边界）。
- 未改动：`tests/server/atlas-storage-routes.test.ts` 与 `tests/server/home-board-routes.test.ts` 的正文（含测试体内的动态导入与全部断言）保持原样，作为该容量边界可判别的信号保留；未用“把导入提到模块顶层”来消除现象。
- 未运行边界：本轮未改产品源码、数据库语义、认证或部署路径，未跑 `npm run check:full` 的 E2E 阶段、`npm run test:compose-smoke`、`npm run dev` 健康端点、Compose 构建与实体设备；E2E 由 Playwright 驱动，不经过本次变更的 Vitest 配置。

<a id="feat-059"></a>

## 2026-09-13 — feat-059：Post 同时间分页与稳定排序

- 范围：核对归档依赖 feat-033 为 done，只推进 feat-059。使用 QAM-05 复核技能及 harness-creator 状态核验，按本地 Next.js Route Handler、ORM 读取和客户端边界指南实施；未改 Post 写入、可见性、schema/index、依赖、部署、搜索错误反馈或空间保存。
- 根因与修正：时间戳不能唯一定位记录；原 `publishedAt < cursor` 会排除同毫秒未返回的文章。首页/API 共用 `(publishedAt DESC,id DESC)`，Timeline 依同一键反向呈现。版本1 Base64url cursor 经同域 Zod 校验再做复合比较，独立嵌入 AND，保持搜索 OR 与成员过滤；锚点删除后仍可按原值续读。旧时间字符串与畸形 cursor 返回可观察400；页长限定正整数，默认50/最多100，满页到尾允许下一次空页。
- 启动：`./init.sh` 初始 exit0，77文件/623项；production 自动把 next-env.d.ts 的两处 `.next/dev/types` 改为 `.next/types`，只恢复本轮生成变化后，收尾 `./init.sh` 再次 exit0、78/653。
- 数据库负向对照：`./scripts/run-node22.sh npm run test:integration -- tests/integration/post-pagination.integration.test.ts` 旧实现 exit1、0/4；59条可见记录（57条同毫秒）以50条分页只返回51条，原文 `expected ... to have a length of 59 but got 51`；首页和删除锚点测试也观察到错误的同时间顺序。
- 数据库修复验证：`./scripts/run-node22.sh npm run test:integration -- tests/integration/post-pagination.integration.test.ts tests/integration/post-timeline-projection.integration.test.ts tests/integration/post-visibility.integration.test.ts` exit0、3文件/6项。1/17/50三种页长重复读取列表/搜索均恰好59条；覆盖 title/content 不区分大小写搜索、首页最新50条、成员/孤儿隐藏、type/author组合和删除游标末项后的续读。
- 组件负向对照：`./scripts/run-node22.sh npm run test:component -- tests/component/timeline-order.test.tsx` 旧实现 exit1、0/1，标题顺序 `older,c,a,b,newer`。修复后 `./scripts/run-node22.sh npm run test:component -- tests/component/timeline-order.test.tsx tests/component/home-timeline-board-search.test.tsx` exit0、2/16；含 Date/字符串时间、用户/Agent混合、重排/重渲染与原数组保持。
- Route负向对照：`./scripts/run-node22.sh npm run test:unit -- tests/server/posts-api.test.ts` 旧实现 exit1、27 failed/13 passed；旧/畸形cursor未返回400，limit=0还触发空末项访问500，同毫秒cursor相同。修复后 `./scripts/run-node22.sh npm run test:unit -- tests/server/posts-api.test.ts tests/server/home-timeline-board-search.test.ts tests/server/timeline-empty-message.test.ts` 首轮 exit1、1 failed/45 passed：空cursor同时产生min/regex两项issues，测试错误地要求恰好一项；改为核对cursor路径的可观察错误后原命令exit0、3/46。再补空白/控制字符ID拒绝，最终Post Route42项由完整门禁全部验证。
- 类型/lint：`./scripts/run-node22.sh npm run typecheck` 首轮exit2，两处TS2550（测试使用的toReversed不在当前lib声明）；改为复制数组后reverse，未改TS配置，原命令exit0。`./scripts/run-node22.sh npm exec eslint -- lib/post-timeline.ts lib/post-pagination.ts app/api/posts/route.ts app/home/page.tsx components/blog/Timeline.tsx tests/server/posts-api.test.ts tests/integration/post-pagination.integration.test.ts tests/component/timeline-order.test.tsx tests/e2e/authenticated.spec.ts` exit0；完整门禁和收尾init再次覆盖全量类型/lint。
- 完整门禁：`E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run check:full` 单次exit0：78文件/653项Vitest、Next.js 16.3.3生产构建、覆盖率statements/branches/functions/lines为50.43/45.03/55.02/50.99、28文件/101项真实PostgreSQL、40/40 Playwright（3.5分钟，无跳过）。
- 浏览器：新增[同毫秒旅程](tests/e2e/authenticated.spec.ts#L1280)在上述完整命令中首次即通过（3.5秒）；真实PostgreSQL乱序写入53条同毫秒Post，首页/刷新/搜索保持最新50条的反向顺序，17条分页的真实API返回全部53条且无重复，旧时间cursor为400。未mock服务端排序/搜索，finally删除本例Post及级联锚点；已有浏览器用例正文完整保留。
- 非阻断诊断：生产完整门禁有六条既有 `The requested resource isn't a valid image`（`/images/about/oo.jpg`、`/images/about/xx.jpg`各三次，feat-064）；没有closed-controller/unhandledRejection。测试用mock供应商和终端颜色提示不影响断言。
- 未运行：默认开发模式 `./scripts/run-node22.sh npm run check:full`，沿既有交接采用生产完整门禁，feat-063的开发Study序列问题保持；`npm run test:compose-smoke`、`npm run dev`健康端点、生日主题专用矩阵、Docker镜像及实体设备未另跑，本轮未改对应启动/资产接缝，不声明这些边界已验收。
- 质量：[QAM-05报告](docs/optimization/qam-05-content-timeline-quality-review.md)关闭003，数据一致性/健壮性/验证可信度各+1，77→80；Score L3、Gate/Final仍L2，其余五个P2保持。[总览](docs/optimization/module-quality-overview.md)同步组合均分85.9与开放P2为30。
- 清理与保留：核对后 `rm -r -- coverage test-results playwright-report` exit0；测试数据库、E2E临时目录、容器/网络/卷自动清理，Docker仅原健康PostgreSQL/Compose资源。既有Post API和E2E正文保留；首次Python正文提取校验因新增区段边界多保留一空行而失败，修正提取后哈希一致，未改旧测试。全仓库未涉及文件哈希保持；本轮临时日志/快照/草稿收尾删除，不作为恢复依赖。
- 收尾核验：首次Python链接扫描误将`javascript:...`安全示例当作文件；排除协议目标后139个本地链接/行号通过。任务字段/依赖、归档完整性、评分算术通过；Harness结构100/100，`git diff --check`/`git status --short`通过。
- 唯一下一步：按P2排名开始feat-060（QAM-07-003，学习连续天数与伙伴今日分钟时区边界），启动时核对归档依赖feat-035。

<a id="feat-058"></a>

## 2026-09-13 — feat-058：Agent 入口身份重验

- 范围：归档依赖 feat-031/040 均为 done。使用 QAM-10 复核技能及 harness-creator 状态核验，按本地 Next.js usePathname/客户端数据读取指南实现；不改 Session/Cookie、认证 API、Chat、Scene、GLB/主题、依赖或部署配置。
- 修正：Chat 路由卸载独立认证显示状态，返回须新 200；非 Chat 保留模型缓存，focus/visibility/BFCache 恢复重验。成功 logout 发同标签 Event 与跨标签 storage 通知，只存随机标识；先隐藏，再查服务器。100ms 合并事件，AbortController/generation 使旧请求失效；非 200/网络失败隐藏，下次事件可恢复，卸载清理定时器/监听/请求，无轮询。
- 启动：`./init.sh` exit 0，77 文件/607 项，Prisma generate、资产、类型/lint 和快速基线通过；恢复本轮 production 构建自动改写的 next-env.d.ts 两个类型目录后，收尾 `./init.sh` 再次 exit 0、77/623。
- 回归：`./scripts/run-node22.sh npm run test:component -- tests/component/agent-entry-gate.test.tsx` 旧 Gate exit 1、14 failed/10 passed；Chat 返回误挂载、focus/visibility/logout 不重验等均有直接失败。修复后连同 `tests/component/agent-entry.test.tsx` 为 34/34；补重验 500/断网恢复后同定向命令为 36/36（Gate 26、DOM 10）。覆盖 401/403、迟到 200、合并事件、无轮询、Strict Mode、存储不可用和取消清理。
- 类型/lint：`./scripts/run-node22.sh npm run typecheck` 两轮 exit 0；`./scripts/run-node22.sh npm exec eslint -- components/agent-entry/AgentEntryGate.tsx lib/session-logout.ts app/me/MeForm.tsx tests/component/agent-entry-gate.test.tsx tests/e2e/agent-entry-authenticated.spec.ts` 两轮 exit 0。最终完整门禁再次覆盖全量类型/lint。
- 浏览器命令：`E2E_SOFTWARE_WEBGL=true ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/agent-entry-authenticated.spec.ts --grep '跨标签退出|真实 Session 到期'`。用真实第二位用户 API 登录隔离第一位用户 fixture；实际 UI logout/login、PostgreSQL Session.expiresAt、旧 WebGL context 与模型请求验收。
- 开发浏览器前七轮 exit1：场景句柄跨文档、原生 focus 被覆盖、channel 配置层级、登录选择器/受控表单同步及 Chat 冷导航逐步暴露；各轮原始错误、通过/失败数与修正完整保存在 [QAM-10 调试记录](docs/optimization/qam-10-agent-entry-quality-review.md)。第八轮 exit0、3/3（1.4分钟），跨标签52.5秒/到期10.1秒；原页面单次GLB、旧context释放和原生focus/401/200均通过。
- 焦点证据：独立 Chromium 对照证明 headless shell 不发原生 focus，完整 Chromium 须在导航后取消 Playwright 强制聚焦；当前断言真实 hasFocus=false→true，事件 isTrusted=true，无 JS 派发或 mock Session。对照过程见 QAM-10 报告。
- 验证安排：开发定向用例先预热所需路由，并在退出页面准备完成后才获取待观察场景，避免首次开发路由装配使旧句柄失效；两个主题生产矩阵均已通过。About 图片缺失诊断仍属 feat-064；未把 Three.Clock 库弃用提示或这些已知图片诊断并入实现。
- 完整门禁：`E2E_APP_MODE=production E2E_SOFTWARE_WEBGL=true NEXT_PUBLIC_AGENT_ENTRY_THEME=default ./scripts/run-node22.sh npm run check:full` 单次 exit 0：77 文件/623 项 Vitest、production build、覆盖率 statements/branches/functions/lines 为 50.16/44.69/54.71/50.77、27 文件/97 项 PostgreSQL、39/39 Playwright；其中包含默认主题入口全部 18 项（含 setup/public），跨标签与到期用例 14.1/4.2 秒。
- 生日主题：`./scripts/run-node22.sh npm run test:e2e:agent-entry:production:birthday` 单次 exit 0、18/18（2.6 分钟），跨标签/到期为 13.2/3.9 秒；default 的全部 18 项已在完整门禁运行，无重复矩阵。两主题均由 production 启动器反转运行时主题，仍只请求构建模型；CSP/外联、五类故障、静止帧、Canvas/缓存及键盘/触摸/布局全部通过。
- 未运行：默认开发模式 `./scripts/run-node22.sh npm run check:full`，依既有交接采用生产完整门禁，本轮开发只验收新增旅程，feat-063 保持；`npm run test:compose-smoke`、Docker image build/config 和实体设备未运行，未改这些接缝，不能把浏览器通过当作 image/GPU/软键盘/safe-area 验证。完整生产与生日命令各有六条既有 About invalid-image 诊断（feat-064），无 closed-controller/unhandledRejection；开发最后一轮清理阶段有服务端 Response 401，实际断言均通过。
- 质量：[QAM-10 报告](docs/optimization/qam-10-agent-entry-quality-review.md)关闭 QAM-10-003，数据状态/健壮性各 +2，84→88；两主题浏览器证据已补齐，QAM-10-005 的镜像部分仍开放，Gate/Final=L2。其余四个 P2 保持，已同步[总览](docs/optimization/module-quality-overview.md)，组合均分85.6、开放QAM P2为31。
- 清理：coverage、test-results、playwright-report 已删除；首次 `rm -rf coverage test-results playwright-report` 被执行策略拒绝且未运行，核对三个实际目录后 `rm -r -- coverage test-results playwright-report` exit 0。测试数据库/网络/卷及 E2E 临时目录自动清理，Docker 只剩原健康 PostgreSQL 与原 Compose 资源；本轮 /tmp 日志/草稿/快照收尾删除，不保留失败诊断工件。
- 收尾核验：58 个未涉及既有文件哈希保持，既有 Agent Entry E2E 用例正文完整保留；新增 worker 级 channel 与新用例未改既有断言。全量字段/依赖一致；草稿链接检查首轮发现 session-logout.ts 引用第26行越界，改为第25行后通过。最终任务字段/依赖、96个本地链接/行号、十维/总览算术通过；Harness结构100/100，`git diff --check`与status已核验。
- 唯一下一步：按 P2 排名开始 feat-059（QAM-05-003，同时间 Post 分页遗漏与排序），启动时核对归档依赖 feat-033。

<a id="feat-057"></a>

## 2026-09-13 — feat-057：搜索错误反馈与文章展示投影

- 范围：归档依赖 feat-033/048 均为 done，只实施 feat-057；使用 QAM-05 技能复核报告，harness-creator 核验状态。按本地 Next.js Route Handler/数据读取指南实现；保持 Post 写入、搜索范围、排序/cursor、schema/迁移、依赖与部署配置。
- 修正：`lib/post-timeline.ts` 定义首页/API 共用的最小 Post/author select；仅提供作者公开身份和 city/country/timezone，旧记录沿用逐字段快照优先 fallback，成员可见性保持。`HomeTimelineBoard` 检查 response.ok 与 posts 数组；401/500、网络、JSON/载荷失败显示可访问错误并保留最近成功结果，只有成功空集合显示无匹配；键盘可重试。成功/失败回写均检查各自 AbortSignal，切换、清空或卸载后的迟到响应被忽略。
- 启动：首次 `./init.sh` exit 0（77 文件/593 项）；完整门禁后只恢复本轮构建改写、启动时干净的 next-env.d.ts，再次 `./init.sh` exit 0（77/607），类型、lint 与快速基线均通过。
- 组件负向对照：`./scripts/run-node22.sh npm run test:component -- tests/component/home-timeline-board-search.test.tsx` 在旧实现 exit 1、12 failed/3 passed；错误路径找不到 alert，迟到成功/失败使新结果退回 Initial post。修复后同命令 exit 0、15/15。使用真实 Timeline/PostCard 和 MSW；覆盖首次/已有结果下的 401、500、断网、无效 JSON、缺失/null posts、空集合后的失败、键盘重试/清空、传输无法取消的迟到响应及卸载取消。
- 数据库负向对照：`./scripts/run-node22.sh npm run test:integration -- tests/integration/post-timeline-projection.integration.test.ts` 旧实现 exit 1、0/1，首页/API 结果深比较失败，搜索缺少 author.profile。修复后 `./scripts/run-node22.sh npm run test:integration -- tests/integration/post-timeline-projection.integration.test.ts tests/integration/post-visibility.integration.test.ts tests/integration/home-board-authorization.integration.test.ts` exit 0、3/3。真实 PostgreSQL 验证最小字段、快照/fallback、无作者/无档案、成员/非成员及孤儿语义；不以 Prisma mock 代替查询授权。
- Server：`./scripts/run-node22.sh npm run test:unit -- tests/server/posts-api.test.ts tests/server/home-timeline-board-search.test.ts` exit 0、2 文件/16 项；确认 HTTP 200 空集合、401/500 保持区分，空首页搜索中不误报无内容。`./scripts/run-node22.sh npm run typecheck` 与九个变更文件定向 ESLint 均 exit 0；最终完整门禁再次覆盖全量类型/lint。
- 开发浏览器：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '搜索失败保留已有文章'` 首轮 exit 1、1 passed/1 failed；getByRole('alert') 同时命中搜索提示与 `__next-route-announcer__`，原始失败为 strict mode violation。仅把测试查询限定到 main 后，同命令 exit 0、2/2（26.9 秒，含 setup）。真实 Next.js/PostgreSQL/Chromium 搜索两篇文章，注入 500/401/断网后仍显示两篇；键盘重试使用真实 API 恢复并进入原文章详情，Tokyo fallback/London 快照保持。401 注入只验证搜索反馈，不替代 Session 失效验证。
- 完整门禁：`E2E_APP_MODE=production ./scripts/run-node22.sh npm run check:full` 单次 exit 0：77 文件/607 项 Vitest、Next.js 16.3.3 production build、覆盖率 statements/branches/functions/lines 为 50.05/44.64/54.45/50.61、27 文件/97 项真实 PostgreSQL、37/37 Playwright（3.0 分钟，无跳过）。新增搜索旅程 3.4 秒，Study 两项 4.4/3.1 秒。
- 服务器诊断：完整门禁出现两次 `The destination stream closed early.`（Agent Entry 故障/导航阶段，对应用例通过），无 ERR_INVALID_STATE、Controller is already closed 或 unhandledRejection。About 两路径各出现两次 `The requested resource isn't a valid image`；`file public/images/about/xx.jpg public/images/about/oo.jpg` 均报告不存在，`git ls-files public/images/about` 为空，`git check-ignore -v public/images/about/xx.jpg public/images/about/oo.jpg` 均命中 .gitignore:33:images/。硬编码引用仍存在；独立登记 feat-064，尚未实施专门图片验收，不把 Agent Entry 用例通过写成照片正常。
- 未运行：默认开发模式 `./scripts/run-node22.sh npm run check:full` 未重跑；按已有交接采用生产完整门禁，本轮开发只验收新增搜索旅程，feat-063 的两次 Study 全量序列失败仍未定位。`npm run test:compose-smoke` 未运行：未改部署/运行配置，风险由真实数据库与生产浏览器完整门禁覆盖。
- 质量：QAM-05-007 resolved；抽象、状态、接口与健壮性各 +1，73→77/L2，其他六个 P2 保持。[QAM-05 报告](docs/optimization/qam-05-content-timeline-quality-review.md) 与 [总览](docs/optimization/module-quality-overview.md) 同步，组合均分 85.2、开放 QAM P2 为 32。
- 既有内容保护：原 E2E 完整前缀保持，本轮仅追加搜索用例。
- 清理：coverage、test-results、playwright-report 已删除；测试容器/网络/卷及 E2E 临时目录自动退出清理，Docker 只剩原 PostgreSQL 和原 Compose 资源。
- 收尾核验：`python3 -` 首轮 exit 1，链接扫描误把报告中的 `javascript:...` 反例当作文件；排除 URI scheme 后 exit 0，任务字段/依赖、130 个本地链接及行号、历史正文还原和评分算术通过，47 个未涉及既有文件哈希保持。`./scripts/run-node22.sh node /home/dadalv/.agents/skills/harness-creator/scripts/validate-harness.mjs --target /home/dadalv/projects/xoxoMeridian` exit 0、结构 100/100。`git diff --check` 与 status 核验通过；本轮 /tmp 日志、草稿及快照已清理，不保留失败诊断工件。
- 唯一下一步：按 P2 排名开始 feat-058（QAM-10-003，全局 Agent 入口登录失效后仍显示），启动时核对归档依赖 feat-031/040。feat-063/064 均保持独立 not-started。

<a id="feat-056"></a>

## 2026-09-13 — feat-056：聊天 SSE 慢查询串行与关闭生命周期

- 范围：归档依赖 feat-010/036 均为 done，只实施 feat-056；使用 QAM-02 技能更新报告，使用 harness-creator 核验状态与归档。业务代码只修改 Room SSE Route；消息投影、Atlas SSE、客户端 merge、Agent Runtime、数据库 schema/迁移、依赖和部署配置保持。
- 改动：每条连接的 `snapshotInFlight` 同步保护 Session 复核、成员复核和 snapshot；超过 2 秒的查询会跳过重叠 tick，不积累待执行队列。finally 在成功/失败/早退后释放；原 2 秒轮询节拍与 error 恢复保持，abort/cancel 后清理 timer/listener、停止新查询与 enqueue，kicked/roomDeleted 继续发送终止事件后关闭。在途数据库 Promise 自然结束。
- 启动：首次 `./init.sh` exit 0，77 文件/576 项 Vitest；收尾恢复本轮 production build 自动改写且会话开始时干净的 `next-env.d.ts` 后，`./init.sh` 再次 exit 0，类型、lint、77 文件/593 项 Vitest 通过。
- Route 负向对照：`./scripts/run-node22.sh npm run test:unit -- tests/server/room-stream.test.ts` 在旧路由 exit 1、4 failed/16 passed；8 秒时间推进后，慢 snapshot、慢 Session/成员复核预期 2 次调用却出现 5 次，交错失败场景预期 3 次却出现 6 次。加入保护后同命令 exit 0、20/20。覆盖事件顺序、失败后按原节拍恢复、初始/轮询 × abort/cancel × 成功/失败、权限检查关闭竞态、会话删除/过期、roomDeleted、已中止请求与连接间隔离。
- 真实服务器/浏览器负向对照：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '慢查询跨多个轮询周期'` 在旧路由 exit 1、1 passed/1 failed（含 setup）；PostgreSQL Memo 表锁让真实 snapshot SELECT 阻塞，pg_locks 观察到 4 条等待查询，最旧年龄 6.18 秒，预期仅 1 条。修复后同命令 exit 0、2/2（44.6 秒）；跨多个 tick 始终一条查询，释放锁后完成消息与 Agent completed 状态持续三个快照，注入 HTTP connectionreset 并恢复真实 SSE 后，离线消息与新 running Task 持续三个快照一致，UI 同步显示。使用真实 Next.js、PostgreSQL 与原生 EventSource；没有生产故障开关或 snapshot mock。Task/Message 为隔离夹具，不代替 Runtime 执行验证；锁、页面、房间和连接均有清理路径。
- `./scripts/run-node22.sh npm run typecheck` exit 0；`./scripts/run-node22.sh node node_modules/eslint/bin/eslint.js 'app/api/rooms/[roomId]/stream/route.ts' tests/server/room-stream.test.ts tests/e2e/authenticated.spec.ts` exit 0。
- 完整门禁：`E2E_APP_MODE=production ./scripts/run-node22.sh npm run check:full` 单次 exit 0；77 文件/593 项 Vitest、Next.js 16.3.3 production build、覆盖率 statements/branches/functions/lines 为 49.81/44.46/54.17/50.37，26 文件/96 项真实 PostgreSQL、36/36 Playwright（浏览器 4.2 分钟，无跳过）。新增慢查询旅程 21.1 秒；Study 两项 4.2/3.0 秒，原消息窗口旅程 1.4 分钟且通过。浏览器阶段有一次 `The destination stream closed early.` 服务器诊断（Agent Entry 阶段，对应用例通过；该文案属于 React RSC destination close 的取消处理），没有 `ERR_INVALID_STATE`、`Controller is already closed` 或 `unhandledRejection`；不把用例通过写成服务器零诊断。
- 未运行：默认开发模式 `./scripts/run-node22.sh npm run check:full` 未重跑，沿用交接中已验证的生产模式路径；开发模式本轮只验证新增慢查询旅程。feat-063 的两次 Study 完整序列失败仍保留，不声称已经恢复或定位。`npm run test:compose-smoke` 未运行：未修改部署/运行配置，本轮用真实 PostgreSQL 与生产浏览器完整门禁覆盖风险。
- 质量：QAM-02-006 resolved；仅状态一致性与并发/生命周期各 +1，QAM-02 从 94 升至 96/L4；[报告](docs/optimization/qam-02-room-message-quality-review.md) 与 [总览](docs/optimization/module-quality-overview.md) 同步，QAM-02-005 仍为 not-reproduced 历史项。
- 既有内容保护：既有任务除 feat-056 状态/证据外字段保持，原 E2E 扣除本轮新增末尾用例后哈希一致。
- 收尾：已清理 coverage、test-results、playwright-report；测试容器/网络/卷与 E2E 临时目录自动移除，Docker 只剩原 PostgreSQL。42 个未涉及既有文件哈希保持；`python3 -` 定向账本/哈希/链接核验 exit 0（字段、92 个链接、归档还原及评分算术），`./scripts/run-node22.sh node /home/dadalv/.agents/skills/harness-creator/scripts/validate-harness.mjs --target /home/dadalv/projects/xoxoMeridian` exit 0、结构评分 100/100。`git diff --check` 与 status 核验通过；本轮 /tmp 日志、快照与草稿清除，不保留失败诊断工件。
- 唯一下一步：按原 P2 排序开始 feat-057（QAM-05-007，搜索错误反馈与文章展示投影），先核对归档依赖 feat-033/048。feat-063 保持独立 not-started。

<a id="feat-055"></a>

## 2026-09-13 — feat-055：生活字段共享约束与旧记录兼容

- 范围：归档依赖 feat-049 为 done，仅实施 feat-055；使用 QAM-03 技能更新本模块报告。32 个未涉及的既有文件哈希不变，原 E2E 扣除本轮新增用例后逐字相同；其他用户改动保留。
- 改动：`lib/life-authoring-contract.ts` 统一 Memo title/content 500/20000、Schedule description 500 字符，按 trim 后 JavaScript 字符串长度判断；HTTP/Agent/UI 共享基础 schema 和 field shape，对象 schema 仅在服务端组装。Memo 缺省标题为“新的备忘录”，显式空白/null 标题或正文拒绝；描述 null/空白清空、undefined 省略。完整策略见 [QAM-03](docs/optimization/qam-03-life-plan-quality-review.md)。
- 兼容：采用既有 Agent 较大上限，无迁移/截断；UI 只 PATCH 编辑过的文本，超限旧字段及首尾空白可保留并修改其他字段，aria-describedby 说明修改限制。fireAt、active cap、Scheduler、schema、依赖与运行配置保持。
- 启动基线：`./init.sh` exit 0，TypeScript、lint、75 文件/544 项 Vitest 通过。
- 契约负向对照：`./scripts/run-node22.sh npm run test:unit -- tests/lib/life-authoring-contract.test.ts`，旧 contract exit 1（25 failed/1 passed），修复后 exit 0、26/26；覆盖四入口边界/超限、trim、空白/null/非字符串、default/省略及 JSON schema 导出。
- 组件负向对照：`./scripts/run-node22.sh npm run test:component -- tests/component/life-authoring-fields.test.tsx` 在旧 Modal + 新 contract 下 exit 1（5 failed/1 passed）：旧 maxLength 阻止长字段修改、旧字段提示/默认标题缺失。修复后 `./scripts/run-node22.sh npm run test:component -- tests/component/life-authoring-fields.test.tsx tests/component/life-panel-modals.test.tsx` exit 0、11/11；MSW 使用真实 HTTP schema。
- 真实 PostgreSQL：`./scripts/run-node22.sh npm run test:integration -- tests/integration/life-authoring-fields.integration.test.ts tests/integration/scheduled-job-one-shot-fireat.integration.test.ts tests/integration/scheduled-job-active-cap.integration.test.ts` exit 0、3 文件/23 项。含新增 9 项 Agent Registry create/update 与 HTTP POST/PATCH 回读、超限无写入、null/空白清空、省略保留、超过上限旧字段与首尾空白无损；既有一次性时间 11 项和 cap 竞争 3 项通过。
- 浏览器：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep 'Agent 创建的较长备忘录'` exit 0、2/2（含 setup）。隔离房间的持久化计划由独立 Agent Runtime 进程实际消费，生成 300/9000/300 字符记录与 2 条 completed ToolCall；UI 用键盘替换末字后 PATCH 200，真实 DB 回读和刷新重开一致，nextRunAt/cron 保持。LLM 为测试 mock，无真实网络/凭据。
- `./scripts/run-node22.sh npm run typecheck` exit 0。完整门禁 `./scripts/run-node22.sh npm run check:full` 首次 exit 1：77 文件/576 项 Vitest、production build、覆盖率 49.29/43.91/54.03/49.97、26 文件/96 项 PostgreSQL 通过；Playwright 为 33 passed/2 failed。`starts and stops a focus session` 等待 `getByText(/专注中 ·/)` 5 秒超时，快照显示“开始专注”处于 disabled；后续到期恢复用例等待“开始专注”及 `/api/study/start` 30 秒超时，页面已有 running state，伴随 24:57→24:56 的 hydration mismatch。尚不能仅凭日志确定首个请求延迟原因；未修改 Study 或其测试。未改代码的定向复核 `./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep 'starts and stops a focus session|reconciles one expired focus session'` exit 0、3/3（含 setup）；首轮失败未稳定复现。相同 `./scripts/run-node22.sh npm run check:full` 重跑再次 exit 1，仍为同样两项 Study 失败（33 passed/2 failed），标准门禁与 26/96 PostgreSQL 均通过，覆盖率为 49.34/43.95/54.03/49.97。已按全局最大编号 +1 独立登记 feat-063（not-started，不改变原前十项 P2 排序），不把全量序列问题误称偶发恢复。生产对照 `E2E_APP_MODE=production ./scripts/run-node22.sh npm run check:full` exit 1：标准门禁与 26/96 PostgreSQL 通过，Study 两项通过；Playwright 34 passed/1 failed，Agent Entry 首帧/Chat 往返用例收到 `["script-src"]`（应为 `[]`）。定位为本轮把 Zod 对象 schema 引入客户端后，其构造阶段的 `allowsEval` 探测触发 CSP；已经在 feat-055 范围内把对象组装留在服务端，浏览器只消费基础字段 schema，并在长记录旅程增加 CSP 事件断言。原 `proxy.ts`、CSP 策略及 Study 用例未改；CSP 修正后 `./scripts/run-node22.sh npm test -- tests/lib/life-authoring-contract.test.ts tests/component/life-authoring-fields.test.tsx tests/component/life-panel-modals.test.tsx tests/agent/schedule-tool.test.ts` exit 0、4 文件/67 项；`E2E_APP_MODE=production ./scripts/run-node22.sh npm run test:e2e -- tests/e2e/agent-entry-authenticated.spec.ts tests/e2e/authenticated.spec.ts --grep '真实首帧、构建主题|Agent 创建的较长备忘录'` exit 0、3/3（含 setup），两旅程均无 CSP violation。最终 `E2E_APP_MODE=production ./scripts/run-node22.sh npm run check:full` exit 0：77 文件/576 项 Vitest、production build、覆盖率 49.32/43.91/54.03/50.00、26 文件/96 项 PostgreSQL、35/35 Playwright。最终代码的默认开发完整序列未重跑；其两次失败留在 feat-063 继续复核，不声称开发模式已恢复。`npm run test:compose-smoke` 未运行：本轮未修改部署/运行配置，风险由完整数据库与浏览器门禁覆盖。
- 收尾：QAM-03-005 resolved、93/L4，总览 QAM 开放 P2 为 34；feat-055 done，feat-063 仅登记。Docker 只剩原 `xoxo-meridian-postgres`，无 Testcontainers 卷/网络；测试连接串、app-info、E2E 临时目录已自动移除。已清理 coverage、test-results、playwright-report、本轮 /tmp 日志与快照，不保留诊断失败工件。JSON/依赖/链接、历史还原、体积与 `git diff --check`/status 核验通过。
- 唯一下一步：按原 P2 排序开始 feat-056（聊天 SSE snapshot 重入），先核对归档依赖 feat-010/036；feat-063 保持独立 not-started，不改变前十项顺序。

<a id="feat-054"></a>

## 2026-09-13 — feat-054：计划控件名称与键盘操作

- 范围与改动：已核对归档依赖 feat-044 为 done；只实现 QAM-03-006。`CronBuilder` 用 `useId`、独立小时/分钟 label 与 fieldset/legend 表达执行时间；`TimezoneSelector` 用 `useId` 绑定 label/select。新增 `cron-timezone-controls.test.tsx`，既有 Modal 时区查询增加 name，`authenticated.spec.ts` 新增键盘创建/刷新重开旅程。字段长度、默认时区、cron/one-shot、active cap 与调度逻辑保持。
- 启动基线：`./init.sh` exit 0，TypeScript、lint、74 文件/541 项 Vitest 通过。
- 组件负向对照与修复：`./scripts/run-node22.sh npm run test:component -- tests/component/cron-timezone-controls.test.tsx tests/component/life-panel-modals.test.tsx`，旧组件 exit 1（5 failed/3 passed；找不到名为“执行时间”的 group、“时区”的 combobox，spinbutton 名称为空），修复后 exit 0、8/8。覆盖多个实例各自的 label 关联、标签聚焦及键盘修改小时/分钟；jsdom 的 select 值选择使用 `selectOptions`，原生方向键由 Chromium 验证。
- 浏览器迭代：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '仅用键盘创建计划|resolves life panel identity'` 两次 exit 1，均为 2 passed/1 failed。首次误把入口 title“新建任务”当可访问名称（实际为“+ 新建”），30 秒超时且清理导航报 `Target page, context or browser has been closed`；改为 title 定位并保证清理继续。第二次键盘创建已返回 201、页面已显示记录，但 `response.json: Test timeout of 30000ms exceeded`；去掉重复的响应正文断言，保留 201、真实 DB 回读和刷新重开断言。两次中的第二参与者默认时区旅程均通过。
- 最终定向浏览器：`./scripts/run-node22.sh npm run test:e2e -- tests/e2e/authenticated.spec.ts --grep '仅用键盘创建计划'` exit 0、2/2（含 setup）；从入口、小时/分钟、原生时区 ArrowDown 到提交全程键盘，DB 与刷新后表单均为 `45 18 * * *`、`Europe/London`，prompt/description 保持。
- 完整门禁：`./scripts/run-node22.sh npm run check:full` 首次 exit 2，新增组件测试的 6 处角色查询报 `TS2769: 'exact' does not exist in type 'ByRoleOptions'`；移除不支持的选项，保留默认精确 name 匹配。相同命令重跑各项均通过：TypeScript、lint、75 文件/544 项 Vitest、Next.js production build、覆盖率 48.95/43.58/53.54/49.68、25 文件/87 项 PostgreSQL、34/34 Playwright。
- 终态恢复：权限/环境切换后 `write_stdin(session_id=30069)` 返回 `Unknown process id`；`tail -n 65 /tmp/xoxo-feat054-check-full.log` 在沙箱与原权限边界均为文件不存在，不能补造原 shell 退出码。此前已观察到标准门禁与 25/87 PostgreSQL 通过；`cat test-results/playwright/.last-run.json` 为 passed，Python stdlib 解包 `playwright-report/index.html` 内嵌 `report.json` 得到 total=expected=34、unexpected=flaky=skipped=0、ok=true，确认最终浏览器全部通过。
- 清理复核：`docker ps -a --format '{{.ID}} {{.Names}} {{.Image}}'` 及 `docker volume ls --filter label=org.testcontainers=true --format '{{.Name}}'`、`docker network ls --filter label=org.testcontainers=true --format '{{.Name}}'` 在新沙箱均报 Docker socket permission denied；相同命令在获准正常边界 exit 0，只剩既有 `xoxo-meridian-postgres`，无 Testcontainers 卷/网络。测试服务已删除连接串与 app-info 文件；`rm -rf coverage test-results playwright-report` exit 0，清除本轮测试报告/账号缓存；原 /tmp 临时工件已随环境切换消失，没有保留诊断失败日志。
- 文档与范围：QAM-03-006 resolved、90/L4，模块总览开放 P2 为 35；feat-054 标记 done。仅修改两个产品组件、对应测试和报告/状态；此前 28 个既有改动文件哈希及 E2E 扣除本轮增量后的还原哈希核验通过，后续未修改这些业务内容。
- `npm run test:compose-smoke` 未运行：本轮未改部署、运行或构建配置，已执行包含标准门禁与真实数据库/浏览器的完整验证。当前无阻塞；Node 账本核验 exit 0（ID 唯一、状态及依赖满足），Python 相对链接/归档还原/90 分算术核验通过；`git diff --check` exit 0，`git status --short` 已核对。
- 唯一推荐下一步：开始 **feat-055（QAM-03-005，Memo/计划跨 UI 与 Agent 字段约束一致性）**，先核对归档依赖 feat-049，不启动其他 P2。

<a id="feat-062"></a>

## 2026-09-12 — feat-062：当前状态与历史分离

- 范围：只整理 Harness 文档和状态。根 feature 保留全部未完成项及完整验收；已完成任务移入归档，`schemaVersion=2` 用 `archivedFeatureFiles` 声明全量账本。原有 P2 排名和唯一后续产品任务 `feat-054` 保持。
- Files Changed（改动文件）：`AGENTS.md`、三份根状态文件、新增 `docs/harness/README.md` 与 `docs/harness/archive/` 下的历史记录。归档来自当前工作树，覆盖既有未提交的 feat-051～053 记录。
- 保留约定：用户此前要求没有代码改动时不必运行应用验证，现提升到 `AGENTS.md`。本轮未运行 `./init.sh`、`npm run check`、`npm run check:full`、`npm run test:compose-smoke`：仅文档和状态整理，按该例外核验，不声称应用测试、构建或部署本轮通过。
- Verification Evidence（验证证据）：[维护说明](docs/harness/README.md) 内的三个只读命令均实际 exit 0：Node 按 ID 提取归档 feat-044、`rg` 定位 feat-040/日期标题、Node 合并账本核验 JSON/状态/依赖/体积（ID 唯一，单活动项约束满足）。Python 迁移比较确认原任务字段与迁移前一致；历史进度的正文保留，仅修正相对链接。
- `./scripts/run-node22.sh node /home/dadalv/.agents/skills/harness-creator/scripts/validate-harness.mjs --target .` exit 0、100/100；指令、状态、验证、范围、生命周期均 5/5，迁移前后结构评分相同。此评分只说明当时的结构完备，不作为现行归档依据。
- 本轮 Markdown 相对链接核验通过；`git diff --check` exit 0，`git status --short` 已检查，25 个既有业务文件的 SHA-256 均与会话开始一致；未新增测试、临时工件或服务，没有保留失败日志。

- Blockers（阻塞）：无。文档验收通过，feat-062 标记 done；未修改应用、依赖或运行配置。
- Next（唯一推荐下一步）：按既有 P2 排名开始 **feat-054（QAM-03-006，计划控件可访问名称与键盘操作）**，核对归档中的 feat-044 依赖后运行 `./init.sh`，不启动其他 P2。

### 近期应用验证（历史证据）

| 已完成项 | 当时的实际结果 |
| --- | --- |
| feat-053 / QAM-08-005 | `./scripts/run-node22.sh npm run check:full` exit 0；Vitest 74 文件/541 项、production build、覆盖率 49.00/43.61/53.54/49.68、PostgreSQL 25/87、Playwright 33/33 |
| feat-052 / QAM-06-007 | 同一完整门禁 exit 0；Vitest 73/496、production build、覆盖率 47.76/42.59/52.56/48.54、PostgreSQL 24/75、Playwright 32/32 |
| feat-051 / QAM-02-002 | 同一完整门禁 exit 0；Vitest 72/482、production build、覆盖率 47.72/42.56/52.56/48.54、PostgreSQL 23/71、Playwright 31/31 |

这些结果及修复前失败命令、原因、重跑和清理证据完整保留在 [历史进度](docs/harness/archive/progress-through-2026-09-12.md)，按 feature 标题提取即可；feat-040 的生产主题与 Compose 结果也在该文件中，只能作为历史 E3。当前 QAM 状态以 [模块总览](docs/optimization/module-quality-overview.md) 及各报告为准。
